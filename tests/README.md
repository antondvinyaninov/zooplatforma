# Тесты ЗооПлатформы

Эта папка содержит все скрипты для тестирования производительности и функциональности проекта.

## 📊 Тесты производительности

### 1. Базовый тест производительности
Проверяет скорость ответа backend API и загрузки frontend страниц.

```bash
./tests/performance-test.sh
```

**Что проверяет:**
- Backend API endpoints (health, root)
- Frontend страницы (главная, авторизация, организации)
- Время ответа и размер страниц

**Результат:**
- ✅ Отлично: <100ms
- ⚠️ Хорошо: 100-500ms
- ❌ Медленно: >500ms

---

### 2. Сравнение с VK.com
Сравнивает производительность с VK.com для оценки конкурентоспособности.

```bash
./tests/compare-with-vk.sh
```

**Что проверяет:**
- Скорость загрузки VK.com (production)
- Скорость загрузки ЗооПлатформы (dev)
- Размер страниц
- Сравнительный анализ

**Важно:** Сравнение не совсем честное, так как:
- VK.com работает в production с CDN
- ЗооПлатформа работает локально в dev режиме

---

### 3. Web Vitals (Core Web Vitals)
Измеряет ключевые метрики производительности с помощью Lighthouse.

```bash
./tests/web-vitals-test.sh
```

**Требования:**
```bash
# Установить Lighthouse CLI
npm install -g lighthouse

# Установить jq для парсинга JSON (опционально)
brew install jq
```

**Что измеряет:**
- **Performance Score** (0-100)
- **FCP** (First Contentful Paint) - первый видимый контент
- **LCP** (Largest Contentful Paint) - загрузка основного контента
- **TBT** (Total Blocking Time) - блокирующее время
- **CLS** (Cumulative Layout Shift) - стабильность макета

**Результаты:**
- Отчеты сохраняются в `tests/reports/*.json`
- Можно открыть в браузере для детального анализа

**Альтернатива:**
Если не хотите устанавливать CLI, используйте Chrome DevTools:
1. Откройте DevTools (F12)
2. Вкладка "Lighthouse"
3. Выберите "Performance"
4. Нажмите "Analyze page load"

---

## 🔐 Тесты безопасности и авторизации

### 4. SSO (Single Sign-On) тест
Проверяет работу единой авторизации между микросервисами.

```bash
./tests/sso-test.sh
```

**Требования:**
- Запущенный Main Backend (localhost:8080)
- Запущенный Admin Backend (localhost:8081)
- jq для парсинга JSON: `brew install jq`

**Что проверяет:**
- Вход на основном сайте
- Проверка токена через `/api/auth/verify`
- Наличие ролей в JWT токене
- Доступ к админ-панели с правами и без
- Выход из всех сервисов одновременно

**Результат:**
- ✅ SSO работает корректно
- ⚠️ Пользователь не является суперадмином (нужно назначить)
- ❌ Ошибки авторизации

**Назначение суперадмина:**
```bash
cd admin/backend
./create-superadmin.sh <user_id>
```

---

## 🧪 Функциональные тесты (будущее)

### Планируется для версии 0.1.0:

#### Unit тесты
```bash
# Backend (Go)
cd backend && go test ./...

# Frontend (Jest)
cd frontend && npm test
```

#### Integration тесты
```bash
# API тесты
./tests/api-integration-test.sh
```

#### E2E тесты
```bash
# Playwright/Cypress
cd frontend && npm run test:e2e
```

---

## 📈 Нагрузочное тестирование (будущее)

### Apache Bench
```bash
# Установка (macOS)
brew install httpd

# Тест: 1000 запросов, 10 одновременно
ab -n 1000 -c 10 http://localhost:8080/api/health
```

### k6 (рекомендуется)
```bash
# Установка
brew install k6

# Запуск теста
k6 run tests/load-test.js
```

---

## 🔧 Требования

### Для запуска тестов производительности:
- ✅ curl (предустановлен)
- ✅ bc (предустановлен на macOS)
- ✅ bash

### Для запуска функциональных тестов:
- Go 1.25.5+
- Node.js 25.2.0+
- npm или yarn

---

## 📝 Добавление новых тестов

### 1. Создайте скрипт в папке tests/
```bash
touch tests/my-new-test.sh
chmod +x tests/my-new-test.sh
```

### 2. Добавьте описание в этот README

### 3. Обновите CHANGELOG.md

---

## 🎯 Целевые показатели

### Версия 0.1.0:
- API Response Time: <100ms (p95)
- Page Load Time: <2s
- FCP: <2s
- LCP: <3s
- TBT: <300ms
- Unit Test Coverage: >50%

### Версия 1.0.0:
- API Response Time: <100ms (p95)
- TTFB: <200ms
- FCP: <1.8s
- LCP: <2.5s
- TBT: <200ms
- CLS: <0.1
- Performance Score: >90
- Unit Test Coverage: >70%
- E2E Test Coverage: критичные сценарии

---

## 📚 Документация

Подробные результаты и анализ в [docs/PERFORMANCE.md](../docs/PERFORMANCE.md)

---

**Последнее обновление:** 12 декабря 2025
