---
inclusion: manual
---

# Критические сервисы системы

## ⚠️ КРИТИЧЕСКОЕ ПРАВИЛО: Два сервиса без которых система не работает

**Если любой из этих сервисов упадет - вся система перестанет работать!**

---

## 🔐 Auth Service (порт 7100)

**Назначение:** Единая авторизация (SSO) для всех микросервисов

### Почему критичен:
- Все микросервисы используют Auth Service для проверки JWT токенов
- Без него невозможна авторизация пользователей
- Если Auth Service упадет - никто не сможет войти в систему
- Все защищенные endpoints станут недоступны

### Технические детали:
- **Порт:** 7100
- **Путь:** `auth/backend/`
- **Главный endpoint:** `/api/auth/me`
- **База данных:** `auth/backend/auth.db`
- **Таблицы:** `users`, `user_roles`

### Зависимости:
- Main Backend (8000) - авторизация пользователей
- Admin Backend (9000) - проверка прав суперадмина
- PetBase Backend (8100) - доступ к справочникам
- Shelter Backend (8200) - управление приютами
- Owner Backend (8400) - кабинет владельца
- Volunteer Backend (8500) - кабинет волонтера
- Clinic Backend (8600) - кабинет клиники

### API:
```
POST   /api/auth/login     - Вход в систему
GET    /api/auth/me        - Проверка токена (используется всеми сервисами)
POST   /api/auth/logout    - Выход из системы
GET    /api/auth/profile   - Получение профиля
PUT    /api/auth/profile   - Обновление профиля
```

---

## 🐾 PetBase (порт 8100/4100)

**Назначение:** Единый источник данных о животных (ЗооБаза)

### Почему критичен:
- Хранит справочную информацию: виды, породы, карточки животных
- Все остальные сервисы читают данные о животных из PetBase
- Без него невозможна работа с животными во всех сервисах
- Единственный источник правды (Single Source of Truth) для данных о животных

### Технические детали:
- **Backend порт:** 8100
- **Frontend порт:** 4100
- **Путь:** `petbase/backend/`, `petbase/frontend/`
- **База данных:** `database/data.db` (общая)
- **Таблицы:** `species`, `breeds`, `pet_cards`

### Зависимости:
- Main Backend (8000) - отображение питомцев пользователей
- Shelter Backend (8200) - карточки животных в приютах
- Owner Backend (8400) - информация о питомцах владельцев
- Clinic Backend (8600) - медицинские карты животных

### API:
```
GET    /api/species              - Список видов животных
POST   /api/species              - Создать вид
GET    /api/species/:id          - Конкретный вид
PUT    /api/species/:id          - Обновить вид
DELETE /api/species/:id          - Удалить вид

GET    /api/breeds               - Список пород
GET    /api/breeds/species/:id   - Породы конкретного вида
POST   /api/breeds               - Создать породу
PUT    /api/breeds/:id           - Обновить породу
DELETE /api/breeds/:id           - Удалить породу

GET    /api/cards                - Список карточек животных
GET    /api/cards/breed/:id      - Карточки конкретной породы
POST   /api/cards                - Создать карточку
PUT    /api/cards/:id            - Обновить карточку
DELETE /api/cards/:id            - Удалить карточку
```

---

## Правила работы с критическими сервисами

### ✅ При разработке:

1. **Всегда проверяй что Auth Service и PetBase запущены**
   - Перед началом работы: `./run` запускает все сервисы
   - Проверка: `http://localhost:7100/api/health` и `http://localhost:8100/api/health`

2. **Не изменяй их API без согласования**
   - Любое изменение API критических сервисов влияет на ВСЕ остальные сервисы
   - Перед изменением: обсуди с командой
   - После изменения: обнови документацию и протестируй все зависимые сервисы

3. **Любые изменения требуют тестирования всей системы**
   - Запусти все сервисы: `./run`
   - Проверь авторизацию во всех сервисах
   - Проверь работу с животными во всех сервисах

### 📊 При мониторинге:

1. **Auth Service и PetBase выделены на странице мониторинга**
   - Секция "Критические сервисы" на `http://localhost:4000/monitoring`
   - Auth Service - фиолетовая рамка 🔐
   - PetBase - синяя рамка 🐾

2. **Их статус проверяется в первую очередь**
   - Параллельная проверка всех сервисов
   - Таймаут: 1 секунда
   - Статусы: online (✅), slow (⚠️), offline (❌)

3. **При падении - критический алерт**
   - Если Auth Service offline → никто не может авторизоваться
   - Если PetBase offline → невозможна работа с животными

### 🚀 При деплое:

1. **Auth Service и PetBase деплоятся первыми**
   ```bash
   # 1. Auth Service
   cd auth/backend && go run main.go
   
   # 2. PetBase
   cd petbase/backend && go run main.go
   cd petbase/frontend && npm run dev
   
   # 3. Проверка
   curl http://localhost:7100/api/health
   curl http://localhost:8100/api/health
   
   # 4. Остальные сервисы
   ./run
   ```

2. **Остальные сервисы деплоятся только после проверки критических**
   - Убедись что Auth Service отвечает
   - Убедись что PetBase отвечает
   - Только потом запускай остальные

3. **Порядок запуска:**
   ```
   1. Auth Service (7100)      ← КРИТИЧЕСКИЙ
   2. PetBase (8100/4100)       ← КРИТИЧЕСКИЙ
   3. Main (8000/3000)
   4. Admin (9000/4000)
   5. Shelter (8200/5100)
   6. Owner (8400/6100)
   7. Volunteer (8500/6200)
   8. Clinic (8600/6300)
   ```

---

## 🔧 Troubleshooting

### Auth Service не отвечает:

**Симптомы:**
- Все сервисы возвращают 401 Unauthorized
- Невозможно войти в систему
- Страница мониторинга показывает Auth Service как offline

**Решение:**
```bash
# 1. Проверь что процесс запущен
ps aux | grep auth

# 2. Проверь логи
tail -f logs/requests/auth.log

# 3. Перезапусти Auth Service
cd auth/backend
go run main.go
```

### PetBase не отвечает:

**Симптомы:**
- Невозможно загрузить список пород
- Ошибки при создании карточек животных
- Страница мониторинга показывает PetBase как offline

**Решение:**
```bash
# 1. Проверь что процесс запущен
ps aux | grep petbase

# 2. Проверь логи
tail -f logs/requests/petbase.log

# 3. Перезапусти PetBase
cd petbase/backend
go run main.go
```

---

## 📝 Checklist перед коммитом изменений в критические сервисы

- [ ] Изменения обсуждены с командой
- [ ] API документация обновлена
- [ ] Все зависимые сервисы протестированы
- [ ] Миграции БД (если есть) протестированы
- [ ] Обратная совместимость сохранена
- [ ] Логирование добавлено для новых endpoints
- [ ] Мониторинг проверен
- [ ] CHANGELOG.md обновлен

---

## ⚠️ ЗАПОМНИ:

**Auth Service (7100) и PetBase (8100/4100) - это фундамент всей системы!**

Без них:
- ❌ Нет авторизации
- ❌ Нет данных о животных
- ❌ Система не работает

**Всегда проверяй их статус первым делом!**
