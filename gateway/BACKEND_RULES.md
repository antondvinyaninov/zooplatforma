# 📋 Правила работы с Gateway

## Git правила

### ✅ Работаю ТОЛЬКО с веткой `gateway`
- Все изменения делаю только в ветке `gateway`
- НЕ трогаю другие ветки (main, develop и т.д.)
- НЕ коммичу файлы из других директорий (../main/, ../docs/ и т.д.)

### Коммиты
- Коммичу только файлы из текущей директории gateway
- Использую понятные commit messages на русском
- Делаю push в origin/gateway

## Структура проекта

```
gateway/
├── main.go              # Точка входа
├── auth.go              # JWT авторизация
├── auth_handlers.go     # Handlers для auth endpoints
├── middleware.go        # CORS, Rate Limiting, Logging
├── proxy.go             # Проксирование к backend
├── services.go          # Конфигурация сервисов
├── gateway.md           # Документация
└── .env                 # Переменные окружения
```

## CORS правила

- Gateway управляет CORS (единственное место)
- Backend сервисы НЕ устанавливают CORS
- ProxyHandler фильтрует Access-Control-* заголовки от backend

## SSO правила

- Gateway = SSO Provider для всей платформы
- Один JWT токен работает для всех сервисов
- Backend сервисы доверяют Gateway и читают X-User-ID
