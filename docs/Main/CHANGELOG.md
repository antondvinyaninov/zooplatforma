# Changelog - Main Service

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/),
и этот проект придерживается [Semantic Versioning](https://semver.org/lang/ru/).

## [Unreleased]

### Added
- **Относительные URL для гибкого деплоя**
  - Создан `lib/urls.ts` с утилитами для работы с URL
  - Функции: `getApiUrl()`, `getMediaFileUrl()`, `getUserAvatarUrl()`, `getUserCoverUrl()`, `getPetPhotoUrl()`, `getOrgLogoUrl()`
  - Автоматическое определение окружения (development/production)
  - В development используются localhost URL
  - В production используются относительные пути и S3 CDN
  - Файлы:
    - `main/frontend/lib/urls.ts` (создан)
    - `main/frontend/.env.local` (создан)
    - `main/frontend/.env.production` (создан)

- **Документация по деплою**
  - `main/DEPLOYMENT.md` - полное руководство по деплою
  - `main/QUICK_DEPLOY.md` - быстрая инструкция для деплоя
  - `docs/Main/DEPLOYMENT_NOTES.md` - заметки по деплою

- **Функция выбора животного в мессенджере**
  - Кнопка "Животное" в меню вложений мессенджера
  - Модальное окно для выбора питомцев пользователя
  - Красивая карточка животного в чате с фото и информацией
  - Клик по карточке открывает страницу питомца `/pets/{id}`
  - Загрузка животных из PetBase API (`http://localhost:8100/api/pets/user/{userId}`)
  - Отправка животного как JSON сообщения в чат
  - Отображение животного в списке чатов (левая колонка) как `🐾 Имя`
  - Файлы:
    - `main/frontend/app/(main)/messenger/page.tsx` - модальное окно и логика отправки
    - `main/frontend/lib/utils.ts` - функция `getMediaUrl()` для загрузки фото

### Changed
- **Обновлены messenger компоненты для использования относительных URL**
  - `ChatList.tsx` использует `getUserAvatarUrl()` вместо хардкодных URL
  - `MessageList.tsx` использует `getMediaFileUrl()` для всех медиа файлов
  - Файлы:
    - `main/frontend/app/(main)/messenger/components/ChatList.tsx`
    - `main/frontend/app/(main)/messenger/components/MessageList.tsx`

- **Оптимизирована загрузка животных в мессенджере**
  - Загружаются только животные, которыми владеет пользователь
  - Раздел "На попечении" отключен до добавления правильного endpoint в PetBase
  - Файл: `main/frontend/app/(main)/messenger/page.tsx`

### Fixed
- **Исправлена авторизация (проблема с портами 8000 и 7100)**
  - Frontend теперь использует Main Backend (8000) для авторизации вместо прямого обращения к Auth Service (7100)
  - Cookie `auth_token` устанавливается с `Domain: "localhost"` для работы на всех портах localhost
  - Пользователь больше не выкидывается после обновления страницы (F5)
  - Cookie теперь корректно передается между портами 3000 (Frontend) и 8000 (Backend)
  - Убрана логика получения токена через Authorization header - используется только HttpOnly cookie
  - Исправлены ошибки 401 при попытке получить токен от Auth Service напрямую
  - Файлы:
    - `main/frontend/lib/api.ts` - изменен `authApi` с `authClient` на `apiClient`, убрана функция `getAuthToken()`
    - `main/backend/handlers/auth.go` - добавлен `Domain: "localhost"` в cookie

- **Исправлена проверка авторизации в AuthContext**
  - Убрана проверка cookie через `document.cookie` (HttpOnly cookies не доступны в JavaScript)
  - Теперь проверка авторизации делается через API запрос `/api/auth/me`
  - Браузер автоматически отправляет HttpOnly cookie с запросом
  - Добавлен таймаут 5 секунд для запроса профиля
  - Добавлено подробное логирование для отладки
  - Файл: `main/frontend/contexts/AuthContext.tsx`

- **Исправлена синхронизация пользователей при регистрации**
  - При регистрации через Auth Service пользователь теперь автоматически синхронизируется с основной БД
  - Исправлена ошибка "User not found" при попытке зайти в профиль после регистрации
  - Используется `INSERT OR IGNORE` для безопасной синхронизации
  - Файл: `main/backend/handlers/auth.go`

- **Исправлена загрузка фото питомцев в мессенджере**
  - Использование `getMediaUrl()` для правильной работы с кросс-серверными запросами
  - Фото питомцев загружаются с PetBase (порт 8100)
  - Fallback на иконку 🐾 если фото не загружается
  - Файл: `main/frontend/app/(main)/messenger/page.tsx`

- **Исправлено отображение последнего сообщения в списке чатов**
  - Если последнее сообщение содержит животное (JSON), показывается `🐾 Имя` вместо JSON
  - Правильная обработка JSON в контенте сообщения
  - Файл: `main/frontend/app/(main)/messenger/page.tsx`

---

## [1.0.0] - 2026-01-31

### Initial Release
- Основной функционал Main Service
- Лента постов с фото и видео
- Система друзей
- Личные сообщения (мессенджер)
- Уведомления
- Организации
- Лайки и комментарии
- Опросы
- Избранное

---

**Дата обновления:** 3 февраля 2026
