# Задачи Main Service

## 🔴 ВЫСОКИЙ ПРИОРИТЕТ

### Задача 1: Добавить endpoint для загрузки животных на попечении
**Статус:** TODO  
**Дата создания:** 31 января 2026

**Описание:**
Нужно добавить правильный endpoint в PetBase для загрузки животных, которые находятся на попечении пользователя (где `curator_id` = userId).

**Проблема:**
- В мессенджере нужно показывать животных на попечении
- Текущий endpoint `/api/pets?curator_id={userId}` не работает правильно
- Возвращает всех животных вместо фильтрации по curator_id

**Решение:**
1. Открыть основной проект (весь репозиторий)
2. Добавить в `petbase/backend/handlers/pets.go` функцию `getPetsByCurator()`
3. Зарегистрировать endpoint в `petbase/backend/main.go`
4. Endpoint должен быть: `GET /api/pets/curator/:curatorID`
5. Протестировать на всех сервисах

**Файлы для изменения:**
- `petbase/backend/handlers/pets.go` - добавить функцию getPetsByCurator
- `petbase/backend/main.go` - зарегистрировать новый endpoint
- `petbase/README_API.md` - обновить документацию

**Использование в Main:**
После добавления endpoint, в `main/frontend/app/(main)/messenger/page.tsx` нужно обновить:
```typescript
const curatorResponse = await fetch(`http://localhost:8100/api/pets/curator/${user?.id}`, {
  credentials: 'include',
});
```

**Результат:**
- ✅ Мессенджер будет показывать животных на попечении в отдельном разделе
- ✅ Пользователи смогут делиться животными на попечении в чатах

---

## 📋 СРЕДНИЙ ПРИОРИТЕТ

(Будут добавлены позже)

---

## ✅ ЗАВЕРШЕНО

(Будут добавлены позже)
