# API Documentation - ЗооПлатформа

**Версия:** 2.0  
**Дата:** 17 января 2025  
**Статус:** Development

> **Полная документация всех API endpoints всех микросервисов проекта**

---

## 📚 Содержание

1. [Микросервисы и порты](#-микросервисы-и-порты)
2. [Авторизация и безопасность](#-авторизация-и-безопасность)
3. [Main API](#-main-api-localhost8000) - Основной сайт
4. [Admin API](#-admin-api-localhost9000) - Админ-панель
5. [PetBase API](#-petbase-api-localhost8100) - Реестр животных
6. [Shelter API](#-shelter-api-localhost8200) - Кабинет приюта
7. [Owner API](#-owner-api-localhost8400) - Кабинет владельца
8. [Volunteer API](#-volunteer-api-localhost8500) - Кабинет зоопомощника
9. [Clinic API](#-clinic-api-localhost8600) - Кабинет ветклиники
10. [Внешние API](#-внешние-api)
11. [Форматы ответов](#-формат-ответов)
12. [Коды ошибок](#-коды-ошибок)

---

## 🌐 Микросервисы и порты

| Сервис | URL (Dev) | URL (Production) | Описание |
|--------|-----------|------------------|----------|
| **Main** | http://localhost:3000 | https://zooplatform.ru | Основной сайт (соцсеть) |
| **Main API** | http://localhost:8000 | https://api.zooplatform.ru | Backend основного сайта |
| **Admin** | http://localhost:4000 | https://admin.zooplatform.ru | Админ-панель |
| **Admin API** | http://localhost:9000 | https://admin-api.zooplatform.ru | Backend админ-панели |
| **PetID** | http://localhost:4100 | https://petid.zooplatform.ru | Реестр животных |
| **PetID API** | http://localhost:8100 | https://petid-api.zooplatform.ru | Backend реестра животных |
| **Shelter** | http://localhost:5100 | https://shelter.zooplatform.ru | Кабинет приюта |
| **Shelter API** | http://localhost:8200 | https://shelter-api.zooplatform.ru | Backend приюта |
| **Owner** | http://localhost:6100 | https://owner.zooplatform.ru | Кабинет владельца |
| **Owner API** | http://localhost:8400 | https://owner-api.zooplatform.ru | Backend владельца |
| **Volunteer** | http://localhost:6200 | https://volunteer.zooplatform.ru | Кабинет зоопомощника |
| **Volunteer API** | http://localhost:8500 | https://volunteer-api.zooplatform.ru | Backend зоопомощника |
| **Clinic** | http://localhost:6300 | https://clinic.zooplatform.ru | Кабинет ветклиники |
| **Clinic API** | http://localhost:8600 | https://clinic-api.zooplatform.ru | Backend ветклиники |
| **Mobile** | http://localhost:8081 | - | React Native (Expo) |

---

## 🔐 Авторизация и безопасность

### Типы аутентификации

**Main API и Admin API:** JWT токены в cookie `auth_token`  
**PetID API:** JWT токены в заголовке `Authorization: Bearer <token>` или X-User-ID (только для разработки)

**SSO (Single Sign-On):** Вход на любом сервисе → токен доступен везде.

### Защищённые и публичные endpoints

#### Публичные (без аутентификации)
- `GET /api/health` - проверка здоровья сервиса
- `GET /api/species` - список видов животных
- `GET /api/breeds` - список пород
- `GET /api/cards` - информационные карточки о породах

#### Защищённые (требуют аутентификации)
- Все endpoints `/api/pets/*` - управление питомцами
- Все endpoints `/api/admin/*` - административные функции
- Все endpoints `/api/posts/*` - создание/редактирование постов

### Проверка владельца

При операциях UPDATE и DELETE система автоматически проверяет:
- Пользователь может редактировать только своих питомцев
- Пользователь может удалять только свои посты
- User ID берётся из JWT токена, а не из запроса (защита от подделки)

### CORS

Разрешённые origins настраиваются через переменную окружения `ALLOWED_ORIGINS`:

**Development:**
- `http://localhost:3000` - основной сайт
- `http://localhost:4100` - ЗооБаза frontend

**Production:**
- `https://zooplatform.ru`
- `https://petid.zooplatform.ru`
- Другие домены проекта

### Примеры запросов

#### С JWT токеном (рекомендуется для продакшена)
```bash
curl -H "Authorization: Bearer eyJhbGc..." http://localhost:8100/api/pets
```

#### С X-User-ID (только для разработки)
```bash
curl -H "X-User-ID: 1" http://localhost:8100/api/pets
```

#### Без аутентификации (публичные endpoints)
```bash
curl http://localhost:8100/api/species
```

### Коды ошибок безопасности

- `401 Unauthorized` - не предоставлен токен или токен невалидный
- `403 Forbidden` - попытка доступа к чужому ресурсу или неразрешённый origin
- `429 Too Many Requests` - превышен rate limit

---

## 📱 Main API (localhost:8000)

### Авторизация

```
POST   /api/auth/register          Регистрация нового пользователя
POST   /api/auth/login             Вход в систему
POST   /api/auth/logout            Выход из системы
GET    /api/auth/me                Получить текущего пользователя (SSO endpoint)
GET    /api/auth/verify            Проверить токен
```

**Примеры:**

```bash
# Регистрация
curl -X POST \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123",
    "name": "Иван Иванов"
  }' \
  http://localhost:8000/api/auth/register

# Вход
curl -X POST \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123"
  }' \
  http://localhost:8000/api/auth/login

# Проверка авторизации (SSO)
curl -H "Cookie: auth_token=<jwt>" \
  http://localhost:8000/api/auth/me
```

**Ответ /api/auth/me (используется для SSO):**
```json
{
  "success": true,
  "data": {
    "user": {
      "id": 1,
      "email": "user@example.com",
      "name": "Иван",
      "last_name": "Иванов",
      "role": "user",
      "avatar": "/uploads/users/1/avatar.jpg"
    },
    "token": "eyJhbGciOiJIUzI1NiIs..."
  }
}
```

### Пользователи

```
GET    /api/users                  Получить всех пользователей (защищено)
GET    /api/users/:id              Получить пользователя по ID (публично)
PUT    /api/profile                Обновить свой профиль (защищено)
POST   /api/profile/avatar         Загрузить аватар (защищено)
DELETE /api/profile/avatar/delete  Удалить аватар (защищено)
POST   /api/profile/cover          Загрузить обложку профиля (защищено)
DELETE /api/profile/cover/delete   Удалить обложку (защищено)
```

**Примеры:**

```bash
# Получить пользователя (публично)
curl http://localhost:8000/api/users/1

# Обновить профиль
curl -X PUT \
  -H "Cookie: auth_token=<jwt>" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Иван",
    "last_name": "Иванов",
    "bio": "Люблю животных",
    "location": "Москва",
    "phone": "+7 (999) 123-45-67"
  }' \
  http://localhost:8000/api/profile

# Загрузить аватар
curl -X POST \
  -H "Cookie: auth_token=<jwt>" \
  -F "avatar=@/path/to/photo.jpg" \
  http://localhost:8000/api/profile/avatar
```

**Ответ GET /api/users/:id:**
```json
{
  "success": true,
  "data": {
    "id": 1,
    "name": "Иван",
    "last_name": "Иванов",
    "email": "user@example.com",
    "avatar": "/uploads/users/1/avatar.jpg",
    "cover_photo": "/uploads/users/1/cover.jpg",
    "bio": "Люблю животных",
    "location": "Москва",
    "phone": "+7 (999) 123-45-67",
    "created_at": "2024-01-01T10:00:00Z"
  }
}
```

### Посты

```
GET    /api/posts                  Получить все посты (лента, публично)
GET    /api/posts/:id              Получить пост по ID (защищено)
POST   /api/posts                  Создать пост (защищено)
PUT    /api/posts/:id              Обновить свой пост (защищено)
DELETE /api/posts/:id              Удалить свой пост (защищено)
GET    /api/posts/drafts           Получить черновики (защищено)
GET    /api/posts/user/:id         Получить посты пользователя (публично)
GET    /api/posts/pet/:id          Получить посты питомца (публично)
GET    /api/posts/organization/:id Получить посты организации (публично)
```

**Примеры:**

```bash
# Получить ленту постов
curl http://localhost:8000/api/posts

# Создать пост
curl -X POST \
  -H "Cookie: auth_token=<jwt>" \
  -H "Content-Type: application/json" \
  -d '{
    "content": "Мой новый пост!",
    "status": "published",
    "pet_ids": [1, 2],
    "media": ["/uploads/posts/photo1.jpg"]
  }' \
  http://localhost:8000/api/posts

# Получить посты пользователя
curl http://localhost:8000/api/posts/user/1

# Получить посты питомца
curl http://localhost:8000/api/posts/pet/5
```

### Комментарии

```
GET    /api/comments/post/:id      Получить комментарии к посту (защищено)
POST   /api/comments/post/:id      Добавить комментарий (защищено)
DELETE /api/comments/:id           Удалить свой комментарий (защищено)
```

### Лайки

```
POST   /api/posts/:id/like         Поставить/убрать лайк (защищено)
GET    /api/posts/:id/likes        Получить список лайкнувших (защищено)
```

### Голосования (Polls)

```
POST   /api/polls/:id/vote         Проголосовать (защищено)
GET    /api/polls/:id/results      Получить результаты (защищено)
```

### Питомцы

```
GET    /api/pets                   Получить питомцев текущего пользователя (защищено)
GET    /api/pets/:id               Получить питомца по ID (защищено)
POST   /api/pets                   Добавить питомца (защищено)
PUT    /api/pets/:id               Обновить питомца (защищено)
DELETE /api/pets/:id               Удалить питомца (защищено)
GET    /api/pets/user/:id          Получить питомцев пользователя (публично)
```

**Примеры:**

```bash
# Получить моих питомцев
curl -H "Cookie: auth_token=<jwt>" \
  http://localhost:8000/api/pets

# Получить питомцев пользователя (публично)
curl http://localhost:8000/api/pets/user/1
```

### Медиа (аватары, обложки, фото)

```
POST   /api/media/upload           Загрузить медиа файл (защищено)
GET    /api/media/user/:id         Получить галерею пользователя (защищено)
GET    /api/media/file/:id         Получить медиа файл (публично)
DELETE /api/media/delete/:id       Удалить медиа (защищено)
GET    /api/media/stats            Статистика медиа (защищено)
```

**Chunked Upload (для больших файлов):**

```
POST   /api/media/chunked/initiate Инициировать загрузку (защищено)
POST   /api/media/chunked/upload   Загрузить chunk (защищено)
POST   /api/media/chunked/complete Завершить загрузку (защищено)
```

**Примеры:**

```bash
# Загрузить фото
curl -X POST \
  -H "Cookie: auth_token=<jwt>" \
  -F "file=@/path/to/photo.jpg" \
  -F "user_id=1" \
  http://localhost:8000/api/media/upload

# Получить галерею пользователя
curl -H "Cookie: auth_token=<jwt>" \
  http://localhost:8000/api/media/user/1
```

### Друзья

```
GET    /api/friends                Получить список друзей (защищено)
GET    /api/friends/requests       Получить входящие запросы (защищено)
POST   /api/friends/send           Отправить запрос в друзья (защищено)
POST   /api/friends/accept         Принять запрос (защищено)
POST   /api/friends/reject         Отклонить запрос (защищено)
DELETE /api/friends/remove         Удалить из друзей (защищено)
GET    /api/friends/status         Получить статус дружбы (защищено)
```

**Примеры:**

```bash
# Отправить запрос в друзья
curl -X POST \
  -H "Cookie: auth_token=<jwt>" \
  -H "Content-Type: application/json" \
  -d '{"friend_id": 5}' \
  http://localhost:8000/api/friends/send

# Получить входящие запросы
curl -H "Cookie: auth_token=<jwt>" \
  http://localhost:8000/api/friends/requests

# Принять запрос
curl -X POST \
  -H "Cookie: auth_token=<jwt>" \
  -H "Content-Type: application/json" \
  -d '{"friend_id": 5}' \
  http://localhost:8000/api/friends/accept
```

### Уведомления

```
GET    /api/notifications          Получить уведомления (защищено)
GET    /api/notifications/unread   Получить количество непрочитанных (защищено)
PUT    /api/notifications/:id      Отметить как прочитанное (защищено)
POST   /api/notifications/read-all Отметить все как прочитанные (защищено)
```

**Примеры:**

```bash
# Получить уведомления
curl -H "Cookie: auth_token=<jwt>" \
  http://localhost:8000/api/notifications

# Получить количество непрочитанных
curl -H "Cookie: auth_token=<jwt>" \
  http://localhost:8000/api/notifications/unread

# Отметить все как прочитанные
curl -X POST \
  -H "Cookie: auth_token=<jwt>" \
  http://localhost:8000/api/notifications/read-all
```

**Ответ GET /api/notifications:**
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "user_id": 1,
      "type": "friend_request",
      "title": "Новый запрос в друзья",
      "message": "Иван Иванов хочет добавить вас в друзья",
      "link": "/profile/5",
      "is_read": false,
      "created_at": "2025-01-17T10:00:00Z"
    }
  ]
}
```

### Организации

```
GET    /api/organizations/all      Получить все организации (публично)
GET    /api/organizations/my       Получить мои организации (защищено)
POST   /api/organizations          Создать организацию (защищено)
GET    /api/organizations/:id      Получить организацию (защищено)
PUT    /api/organizations/:id      Обновить организацию (защищено)
GET    /api/organizations/user/:id Получить организации пользователя (защищено)
GET    /api/organizations/members/:id Получить участников организации (защищено)
POST   /api/organizations/members/add Добавить участника (защищено)
PUT    /api/organizations/members/update Обновить участника (защищено)
DELETE /api/organizations/members/remove Удалить участника (защищено)
```

**Примеры:**

```bash
# Получить все организации
curl http://localhost:8000/api/organizations/all

# Создать организацию
curl -X POST \
  -H "Cookie: auth_token=<jwt>" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Ветклиника Айболит",
    "type": "clinic",
    "description": "Современная ветеринарная клиника",
    "address_city": "Москва",
    "phone": "+7 (495) 123-45-67",
    "email": "info@aibolit.ru"
  }' \
  http://localhost:8000/api/organizations

# Добавить участника
curl -X POST \
  -H "Cookie: auth_token=<jwt>" \
  -H "Content-Type: application/json" \
  -d '{
    "organization_id": 5,
    "user_id": 10,
    "role": "admin",
    "position": "Главный ветеринар"
  }' \
  http://localhost:8000/api/organizations/members/add
```

### Мессенджер (личные чаты)

```
GET    /api/chats                  Список диалогов (защищено)
GET    /api/chats/:userId          История сообщений с пользователем (защищено)
POST   /api/messages/send          Отправить текстовое сообщение (защищено)
POST   /api/messages/send-media    Отправить медиа сообщение (защищено)
GET    /api/messages/unread        Количество непрочитанных сообщений (защищено)
```

**Примеры:**

```bash
# Получить список диалогов
curl -H "Cookie: auth_token=<jwt>" \
  http://localhost:8000/api/chats

# Получить историю с пользователем
curl -H "Cookie: auth_token=<jwt>" \
  http://localhost:8000/api/chats/5

# Отправить сообщение
curl -X POST \
  -H "Cookie: auth_token=<jwt>" \
  -H "Content-Type: application/json" \
  -d '{
    "receiver_id": 5,
    "content": "Привет!"
  }' \
  http://localhost:8000/api/messages/send
```

### Избранное (Favorites)

```
GET    /api/favorites              Получить избранных питомцев (защищено)
POST   /api/favorites              Добавить в избранное (защищено)
DELETE /api/favorites/:id          Удалить из избранного (защищено)
```

### Роли и права

```
GET    /api/roles/available        Получить все роли (защищено)
GET    /api/roles/user/:id         Получить роли пользователя (защищено)
POST   /api/roles/grant            Назначить роль (защищено, требует прав)
POST   /api/roles/revoke           Отозвать роль (защищено, требует прав)
```

### Верификация пользователей

```
POST   /api/verification/verify    Верифицировать пользователя (защищено, требует прав)
POST   /api/verification/unverify  Снять верификацию (защищено, требует прав)
GET    /api/verification/status/:id Получить статус верификации (публично)
GET    /api/users/verified         Получить список верифицированных (публично)
```

### Жалобы (Reports)

```
POST   /api/reports                Создать жалобу (защищено)
```

**Пример:**

```bash
# Пожаловаться на пост
curl -X POST \
  -H "Cookie: auth_token=<jwt>" \
  -H "Content-Type: application/json" \
  -d '{
    "target_type": "post",
    "target_id": 123,
    "reason": "spam",
    "description": "Рекламный спам"
  }' \
  http://localhost:8000/api/reports
```

### Активность пользователей

```
POST   /api/activity/update        Обновить активность (защищено)
GET    /api/activity/online        Количество онлайн пользователей (публично)
GET    /api/activity/stats         Статистика активности (публично)
```

### Логи пользователей

```
GET    /api/users/logs/:id         Получить логи пользователя (защищено, требует прав)
GET    /api/users/storage/:id      Статистика хранилища пользователя (защищено)
```

### Административные логи

```
GET    /api/admin/logs             Получить логи администраторов (защищено, требует прав)
GET    /api/admin/logs/stats       Статистика логов (защищено, требует прав)
```

### Объявления о питомцах

```
GET    /api/announcements          Получить объявления (защищено)
GET    /api/announcements/:id      Получить объявление (защищено)
POST   /api/announcements          Создать объявление (защищено)
PUT    /api/announcements/:id      Обновить объявление (защищено)
DELETE /api/announcements/:id      Удалить объявление (защищено)
GET    /api/announcements/posts/:id Посты объявления (защищено)
POST   /api/announcements/donations/:id Пожертвование (защищено)
```

---

## 👥 Admin API (localhost:9000)

**Назначение:** Административная панель для модерации и управления платформой  
**Аутентификация:** JWT через SSO (Main API), требуется роль `superadmin`  
**База данных:** Общая `database/data.db`

### Авторизация

```
POST   /api/admin/auth/login       Вход в админ-панель
POST   /api/admin/auth/logout      Выход
GET    /api/admin/auth/me          Получить текущего админа
```

### Пользователи

```
GET    /api/admin/users            Список всех пользователей
GET    /api/admin/users/:id        Информация о пользователе
PUT    /api/admin/users/:id        Обновить пользователя
DELETE /api/admin/users/:id        Удалить пользователя (бан)
```

### Посты

```
GET    /api/admin/posts            Список всех постов
GET    /api/admin/posts/:id        Информация о посте
DELETE /api/admin/posts/:id        Удалить пост
```

### Организации

```
GET    /api/admin/organizations    Список организаций
PUT    /api/admin/organizations/:id Верифицировать/обновить организацию
GET    /api/admin/organizations/stats Статистика организаций
```

### Модерация

```
GET    /api/admin/moderation/reports Список жалоб
PUT    /api/admin/moderation/reports/:id Рассмотреть жалобу
GET    /api/admin/moderation/stats  Статистика модерации
```

### Статистика

```
GET    /api/admin/stats/overview   Общая статистика платформы
```

### Логи

```
GET    /api/admin/logs             Системные логи
```

### Health Check

```
GET    /api/admin/health           Health check админ-панели
GET    /api/admin/health/services  Health check всех сервисов
```

**Примеры:**

```bash
# Вход в админ-панель
curl -X POST \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "admin_password"
  }' \
  http://localhost:9000/api/admin/auth/login

# Получить список пользователей
curl -H "Cookie: auth_token=<jwt>" \
  http://localhost:9000/api/admin/users

# Рассмотреть жалобу
curl -X PUT \
  -H "Cookie: auth_token=<jwt>" \
  -H "Content-Type: application/json" \
  -d '{
    "status": "resolved",
    "action": "post_deleted",
    "notes": "Пост удален за нарушение правил"
  }' \
  http://localhost:9000/api/admin/moderation/reports/5
```

---

## 🐾 PetID API (localhost:8100)

**Аутентификация:** JWT токены или X-User-ID (dev)  
**CORS:** Настраивается через ALLOWED_ORIGINS  
**Документация:** См. `petbase/backend/README_AUTH.md`

### 🔒 Безопасность

- ✅ JWT аутентификация реализована
- ✅ Проверка владельца при update/delete
- ✅ User ID из токена, а не из запроса
- ✅ CORS с проверкой origins
- ⏳ Rate limiting (в разработке)
- ⏳ Валидация входных данных (в разработке)

### Питомцы (CRUD) - 🔒 Требуют аутентификации

```
POST   /api/pets                       Создать карточку питомца
GET    /api/pets                       Получить всех питомцев пользователя
GET    /api/pets/:id                   Получить полную карточку
GET    /api/pets/:id/summary           Получить краткую информацию (для постов)
PUT    /api/pets/:id                   Обновить карточку (только своего!)
DELETE /api/pets/:id                   Удалить карточку (только своего!)
```

**Примеры:**

```bash
# Создать питомца (user_id берётся из токена)
curl -X POST \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"name":"Барсик","species":"Кошка","breed":"Британская"}' \
  http://localhost:8100/api/pets

# Получить питомца
curl -H "Authorization: Bearer <token>" \
  http://localhost:8100/api/pets/1

# Обновить питомца (проверяется владелец)
curl -X PUT \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"name":"Барсик Обновлённый"}' \
  http://localhost:8100/api/pets/1

# Удалить питомца (проверяется владелец)
curl -X DELETE \
  -H "Authorization: Bearer <token>" \
  http://localhost:8100/api/pets/1
```

### Списки питомцев - 🔒 Требуют аутентификации

```
GET    /api/pets/user/:userId          Питомцы пользователя
GET    /api/pets/status/:status        Питомцы по статусу (home, looking_for_home, lost)
```

**Статусы питомцев:**
- `home` - дома с владельцем
- `looking_for_home` - ищет дом
- `lost` - потерялся
- `found` - найден
- `deceased` - умер

### Справочники (ЗооБаза) - 🌐 Публичные

```
GET    /api/species                    Список видов животных
GET    /api/species/:id                Информация о виде
GET    /api/breeds                     Список пород
GET    /api/breeds/:id                 Информация о породе
GET    /api/breeds/species/:id         Породы конкретного вида
GET    /api/cards                      Информационные карточки о породах
GET    /api/cards/:id                  Карточка породы
GET    /api/cards/breed/:id            Карточки конкретной породы
```

**Примеры:**

```bash
# Получить все виды (без аутентификации)
curl http://localhost:8100/api/species

# Получить породы собак
curl http://localhost:8100/api/breeds/species/1

# Получить информацию о породе
curl http://localhost:8100/api/breeds/5
```

### События (история питомца) - ✅ Реализовано в v0.11.2

```
GET    /api/petid/:id/events       История событий питомца (публично)
POST   /api/pet-events             Создать событие (без auth для клиник)
GET    /api/petid/:id/medical      Медицинская история (публично)
```

**Типы событий:**
- `registration` - регистрация в клинике
- `verification` - подтверждение PetID клиникой
- `ownership_change` - смена владельца
- `sterilization` - стерилизация
- `vaccination` - вакцинация
- `medical_visit` - визит к ветеринару
- `lost` - потерялся
- `found` - нашёлся
- `death` - смерть

**Примеры:**

```bash
# Получить историю событий питомца
curl http://localhost:8100/api/petid/123/events

# Создать событие (из клиники)
curl -X POST \
  -H "Content-Type: application/json" \
  -d '{
    "pet_id": 123,
    "event_type": "verification",
    "event_date": "2025-01-17",
    "description": "Подтверждение PetID клиникой",
    "clinic_id": 5,
    "notes": "Добавлен chip number, медицинская информация"
  }' \
  http://localhost:8100/api/pet-events
```

**Ответ GET /api/petid/:id/events:**
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "pet_id": 123,
      "event_type": "registration",
      "event_date": "2025-01-17",
      "description": "Регистрация в клинике",
      "clinic_id": 5,
      "clinic_name": "Ветклиника Айболит",
      "notes": "Первичная регистрация",
      "created_at": "2025-01-17T10:00:00Z"
    }
  ]
}
```

### Чипы и метки - 🔒 Планируется v1.1.0

```
GET    /api/chips/:number              Найти питомца по номеру чипа
POST   /api/chips                      Зарегистрировать чип
PUT    /api/chips/:id                  Обновить информацию о чипе
```

### Медицинская информация - 🔒 Планируется v1.1.0

```
GET    /api/pets/:id/medical           Медицинская карта
POST   /api/pets/:id/medical           Добавить медицинскую запись
GET    /api/pets/:id/vaccinations      Список вакцинаций
POST   /api/pets/:id/vaccinations      Добавить вакцинацию
```

### Родословная - 🔒 Планируется v1.2.0

```
GET    /api/pets/:id/parents           Родители питомца
POST   /api/pets/:id/parents           Указать родителей
GET    /api/pets/:id/children          Потомки питомца
```

---

## 🏥 Shelter API (localhost:8200)

### Приюты

```
GET    /api/my-shelters                Список приютов пользователя
POST   /api/shelters                   Создать приют
GET    /api/organization               Информация о текущем приюте (требует X-Shelter-ID)
GET    /api/profile                    Профиль текущего пользователя
```

### Животные в приюте

```
GET    /api/my-animals                 Животные текущего приюта (требует X-Shelter-ID)
GET    /api/adoptions                  Заявки на пристройство (требует X-Shelter-ID)
GET    /api/volunteers                 Волонтёры приюта (требует X-Shelter-ID)
```

**Multi-tenancy:** Все endpoints с животными требуют заголовок `X-Shelter-ID` для изоляции данных между приютами.

---

## 👤 Owner API (localhost:8400)

**Назначение:** Кабинет владельца питомца для управления своими питомцами  
**Аутентификация:** JWT через SSO (Main API)  
**База данных:** Общая `database/data.db`

### Питомцы владельца

```
GET    /api/pets                   Список питомцев пользователя
GET    /api/pets/:id               Получить питомца по ID
POST   /api/pets                   Создать питомца
PUT    /api/pets/:id               Обновить питомца
POST   /api/pets/:id/photo         Загрузить фото питомца
GET    /api/breeds                 Получить список пород
GET    /api/profile                Профиль текущего пользователя
GET    /api/health                 Health check
```

**Примеры:**

```bash
# Получить моих питомцев
curl -H "Cookie: auth_token=<jwt>" \
  http://localhost:8400/api/pets

# Создать питомца
curl -X POST \
  -H "Cookie: auth_token=<jwt>" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Барсик",
    "species": "cat",
    "breed": "Британская короткошерстная",
    "birth_date": "2020-05-15",
    "gender": "male",
    "color": "Серый"
  }' \
  http://localhost:8400/api/pets

# Загрузить фото питомца
curl -X POST \
  -H "Cookie: auth_token=<jwt>" \
  -F "photo=@/path/to/photo.jpg" \
  http://localhost:8400/api/pets/123/photo
```

**Ответ GET /api/pets:**
```json
{
  "success": true,
  "data": [
    {
      "id": 123,
      "user_id": 1,
      "name": "Барсик",
      "species": "cat",
      "breed": "Британская короткошерстная",
      "birth_date": "2020-05-15",
      "gender": "male",
      "color": "Серый",
      "photo": "/uploads/pets/123.jpg",
      "created_at": "2025-01-17T10:00:00Z"
    }
  ]
}
```

---

## 🤝 Volunteer API (localhost:8500)

**Назначение:** Кабинет зоопомощника для работы с подопечными питомцами  
**Аутентификация:** JWT через SSO (Main API)  
**База данных:** Общая `database/data.db`

### Подопечные зоопомощника

```
GET    /api/my-pets                Список подопечных питомцев (где curator_id = user_id)
GET    /api/my-tasks               Задачи зоопомощника
GET    /api/profile                Профиль текущего пользователя
GET    /api/health                 Health check
```

**Примеры:**

```bash
# Получить подопечных питомцев
curl -H "Cookie: auth_token=<jwt>" \
  http://localhost:8500/api/my-pets

# Получить задачи
curl -H "Cookie: auth_token=<jwt>" \
  http://localhost:8500/api/my-tasks
```

**Ответ GET /api/my-pets:**
```json
{
  "success": true,
  "data": [
    {
      "id": 456,
      "name": "Шарик",
      "species": "dog",
      "breed": "Дворняга",
      "curator_id": 1,
      "status": "looking_for_home",
      "photo": "/uploads/pets/456.jpg",
      "created_at": "2025-01-10T10:00:00Z"
    }
  ]
}
```

---

## 🏥 Clinic API (localhost:8600)

**Назначение:** Кабинет ветеринарной клиники для регистрации животных и ведения медицинских карт  
**Аутентификация:** JWT через SSO (Main API)  
**Multi-tenancy:** Заголовок `X-Clinic-ID` для изоляции данных между клиниками

### Авторизация и выбор клиники

```
GET    /api/health                     Health check
GET    /api/profile                    Профиль текущего пользователя
GET    /api/my-clinics                 Список клиник пользователя (без X-Clinic-ID)
POST   /api/clinics                    Создать клинику (без X-Clinic-ID)
```

**Примеры:**

```bash
# Health check
curl http://localhost:8600/api/health

# Получить список клиник пользователя
curl -H "Cookie: auth_token=<jwt>" \
     http://localhost:8600/api/my-clinics

# Создать клинику
curl -X POST \
     -H "Cookie: auth_token=<jwt>" \
     -H "Content-Type: application/json" \
     -d '{
       "name": "Ветклиника Айболит",
       "type": "clinic",
       "description": "Современная ветеринарная клиника",
       "address_city": "Москва",
       "phone": "+7 (495) 123-45-67",
       "email": "info@aibolit.ru",
       "website": "https://aibolit.ru"
     }' \
     http://localhost:8600/api/clinics
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "id": 5,
    "name": "Ветклиника Айболит"
  }
}
```

### Организация (требует X-Clinic-ID)

```
GET    /api/organization               Информация о текущей клинике
PUT    /api/organization               Обновить информацию о клинике
```

**Примеры:**

```bash
# Получить информацию о клинике
curl -H "Cookie: auth_token=<jwt>" \
     -H "X-Clinic-ID: 5" \
     http://localhost:8600/api/organization

# Обновить информацию
curl -X PUT \
     -H "Cookie: auth_token=<jwt>" \
     -H "X-Clinic-ID: 5" \
     -H "Content-Type: application/json" \
     -d '{
       "name": "Ветклиника Айболит Плюс",
       "description": "Обновленное описание",
       "phone": "+7 (495) 999-88-77"
     }' \
     http://localhost:8600/api/organization
```

### Команда клиники (требует X-Clinic-ID)

```
GET    /api/members                    Список участников клиники
POST   /api/members                    Добавить участника
PUT    /api/members                    Обновить роль участника
DELETE /api/members                    Удалить участника
GET    /api/users/search               Поиск пользователей для добавления
```

**Роли в клинике:**
- `owner` - владелец (полный доступ)
- `admin` - администратор (управление командой)
- `veterinarian` - ветеринар (работа с пациентами)
- `assistant` - ассистент (просмотр)

**Примеры:**

```bash
# Получить список участников
curl -H "Cookie: auth_token=<jwt>" \
     -H "X-Clinic-ID: 5" \
     http://localhost:8600/api/members

# Добавить участника
curl -X POST \
     -H "Cookie: auth_token=<jwt>" \
     -H "X-Clinic-ID: 5" \
     -H "Content-Type: application/json" \
     -d '{
       "user_id": 10,
       "role": "veterinarian",
       "position": "Главный ветеринар"
     }' \
     http://localhost:8600/api/members

# Поиск пользователей
curl -H "Cookie: auth_token=<jwt>" \
     -H "X-Clinic-ID: 5" \
     "http://localhost:8600/api/users/search?q=Иван"
```

### Регистрация животных (требует X-Clinic-ID)

**Новое в v0.11.2!** Полный функционал регистрации животных с двумя сценариями.

#### Поиск существующих карточек

```
GET    /api/pets/search                Поиск питомцев (через PetBase API)
```

**Query параметры:**
- `owner_phone` - телефон владельца (основной способ)
- `chip_number` - номер микрочипа
- `name` - кличка питомца
- `status` - статус карточки (pending_verification, verified)

**Пример:**

```bash
# Поиск по телефону владельца
curl "http://localhost:8100/api/pets/search?owner_phone=%2B79991234567&status=pending_verification"

# Поиск по chip number
curl "http://localhost:8100/api/pets/search?chip_number=643094100123456"
```

**Ответ:**
```json
{
  "success": true,
  "data": [
    {
      "id": 123,
      "name": "Барсик",
      "species": "cat",
      "breed": "Британская короткошерстная",
      "gender": "male",
      "birth_date": "2020-05-15",
      "chip_number": null,
      "status": "pending_verification",
      "photo": "/uploads/pets/123.jpg",
      "owner_name": "Иван Иванов",
      "owner_phone": "+7 (999) 123-45-67",
      "owner_email": "ivan@example.com",
      "created_at": "2025-01-10T10:00:00Z"
    }
  ]
}
```

#### Дополнение существующей карточки

```
PUT    /api/pets/:id                   Дополнить карточку (через PetBase API)
POST   /api/pet-events                 Создать событие (через PetBase API)
```

**Пример:**

```bash
# Дополнить карточку
curl -X PUT \
     -H "Cookie: auth_token=<jwt>" \
     -H "X-Clinic-ID: 5" \
     -H "Content-Type: application/json" \
     -d '{
       "chip_number": "643094100123456",
       "is_sterilized": true,
       "sterilization_date": "2021-06-20",
       "last_vaccination_date": "2024-12-01",
       "blood_type": "A",
       "photo": "/uploads/pets/123_updated.jpg",
       "status": "verified"
     }' \
     http://localhost:8100/api/pets/123

# Создать событие "Подтверждение PetID клиникой"
curl -X POST \
     -H "Cookie: auth_token=<jwt>" \
     -H "X-Clinic-ID: 5" \
     -H "Content-Type: application/json" \
     -d '{
       "pet_id": 123,
       "event_type": "verification",
       "event_date": "2025-01-17",
       "description": "Подтверждение PetID клиникой",
       "clinic_id": 5,
       "notes": "Добавлен chip number, медицинская информация"
     }' \
     http://localhost:8100/api/pet-events
```

#### Создание новой карточки

```
POST   /api/users                      Создать владельца (если новый, через Main API)
POST   /api/pets                       Создать карточку питомца (через PetBase API)
POST   /api/pet-events                 Создать событие (через PetBase API)
POST   /api/media/upload               Загрузить фото (через Main API)
```

**Пример полного цикла:**

```bash
# 1. Поиск владельца
curl "http://localhost:8000/api/users/search?phone=%2B79991234567"

# 2. Если не найден - создать владельца
curl -X POST \
     -H "Cookie: auth_token=<jwt>" \
     -H "Content-Type: application/json" \
     -d '{
       "name": "Иван Иванов",
       "phone": "+7 (999) 123-45-67",
       "email": "ivan@example.com",
       "password": "temp_password_12345"
     }' \
     http://localhost:8000/api/users

# 3. Загрузить фото питомца
curl -X POST \
     -H "Cookie: auth_token=<jwt>" \
     -F "file=@/path/to/photo.jpg" \
     -F "user_id=10" \
     http://localhost:8000/api/media/upload

# 4. Создать карточку питомца
curl -X POST \
     -H "Cookie: auth_token=<jwt>" \
     -H "X-Clinic-ID: 5" \
     -H "Content-Type: application/json" \
     -d '{
       "user_id": 10,
       "name": "Барсик",
       "species": "cat",
       "breed": "Британская короткошерстная",
       "birth_date": "2020-05-15",
       "gender": "male",
       "color": "Серый",
       "chip_number": "643094100987654",
       "is_sterilized": true,
       "sterilization_date": "2021-06-20",
       "last_vaccination_date": "2024-12-01",
       "blood_type": "A",
       "photo": "/uploads/pets/new_photo.jpg",
       "status": "verified"
     }' \
     http://localhost:8100/api/pets

# 5. Создать событие "Регистрация в клинике"
curl -X POST \
     -H "Cookie: auth_token=<jwt>" \
     -H "X-Clinic-ID: 5" \
     -H "Content-Type: application/json" \
     -d '{
       "pet_id": 124,
       "event_type": "registration",
       "event_date": "2025-01-17",
       "description": "Регистрация в клинике",
       "clinic_id": 5,
       "notes": "Первичная регистрация"
     }' \
     http://localhost:8100/api/pet-events
```

**Генерация chip number:**
- Префикс: `643094` (Россия)
- Формат: 15 цифр (префикс + 9 случайных цифр)
- Пример: `643094100987654`

### Пациенты клиники (требует X-Clinic-ID)

```
GET    /api/my-patients                Список пациентов клиники
GET    /api/appointments               Записи на приём (планируется)
```

**Примеры:**

```bash
# Получить пациентов клиники
curl -H "Cookie: auth_token=<jwt>" \
     -H "X-Clinic-ID: 5" \
     http://localhost:8600/api/my-patients
```

**Ответ:**
```json
{
  "success": true,
  "data": [
    {
      "id": 123,
      "user_id": 10,
      "name": "Барсик",
      "species": "cat",
      "breed": "Британская короткошерстная",
      "birth_date": "2020-05-15",
      "gender": "male",
      "color": "Серый",
      "chip_number": "643094100987654",
      "photo": "/uploads/pets/123.jpg",
      "status": "verified",
      "created_at": "2025-01-17T10:00:00Z",
      "updated_at": "2025-01-17T14:30:00Z"
    }
  ]
}
```

### Уведомления владельцам

При регистрации или подтверждении карточки автоматически отправляется уведомление владельцу:

```
POST   /api/notifications/send         Отправить уведомление (через Main API)
```

**Типы уведомлений:**
- `pet_registered` - питомец зарегистрирован
- `pet_verified` - карточка подтверждена клиникой

**Пример:**

```bash
curl -X POST \
     -H "Cookie: auth_token=<jwt>" \
     -H "Content-Type: application/json" \
     -d '{
       "user_id": 10,
       "type": "pet_registered",
       "title": "Питомец зарегистрирован",
       "message": "Ваш питомец Барсик успешно зарегистрирован в системе PetID",
       "link": "/pets/123"
     }' \
     http://localhost:8000/api/notifications/send
```

### Интеграция с другими сервисами

**Clinic API использует:**

1. **Main API (localhost:8000)**
   - `/api/auth/me` - проверка авторизации (SSO)
   - `/api/users/search` - поиск владельцев
   - `/api/users` - создание владельцев
   - `/api/media/upload` - загрузка фото
   - `/api/notifications/send` - уведомления

2. **PetBase API (localhost:8100)**
   - `/api/pets/search` - поиск питомцев
   - `/api/pets` - создание карточек
   - `/api/pets/:id` - обновление карточек
   - `/api/pet-events` - создание событий

### Коды ошибок

- `400 Bad Request` - невалидные данные
- `401 Unauthorized` - не авторизован
- `403 Forbidden` - нет прав (не член клиники)
- `404 Not Found` - ресурс не найден
- `409 Conflict` - конфликт (например, пользователь уже участник)
- `500 Internal Server Error` - ошибка сервера

---

## 🏠 Shelter API (localhost:8200)

**Назначение:** Кабинет приюта для управления животными и волонтёрами  
**Аутентификация:** JWT через SSO (Main API)  
**Multi-tenancy:** Заголовок `X-Shelter-ID` для изоляции данных между приютами  
**База данных:** Общая `database/data.db`

### Авторизация и выбор приюта

```
GET    /api/health                 Health check
GET    /api/profile                Профиль текущего пользователя
GET    /api/my-shelters            Список приютов пользователя (без X-Shelter-ID)
POST   /api/shelters               Создать приют (без X-Shelter-ID)
```

**Примеры:**

```bash
# Health check
curl http://localhost:8200/api/health

# Получить список приютов пользователя
curl -H "Cookie: auth_token=<jwt>" \
  http://localhost:8200/api/my-shelters

# Создать приют
curl -X POST \
  -H "Cookie: auth_token=<jwt>" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Приют Добрые руки",
    "type": "shelter",
    "description": "Приют для бездомных животных",
    "address_city": "Москва",
    "phone": "+7 (495) 123-45-67",
    "email": "info@shelter.ru"
  }' \
  http://localhost:8200/api/shelters
```

### Организация (требует X-Shelter-ID)

```
GET    /api/organization           Информация о текущем приюте
PUT    /api/organization           Обновить информацию о приюте
```

**Примеры:**

```bash
# Получить информацию о приюте
curl -H "Cookie: auth_token=<jwt>" \
  -H "X-Shelter-ID: 3" \
  http://localhost:8200/api/organization
```

### Животные в приюте (требует X-Shelter-ID)

```
GET    /api/animals                Список животных приюта
POST   /api/animals                Добавить животное
PUT    /api/animals/:id            Обновить информацию о животном
DELETE /api/animals/:id            Удалить животное
```

**Примеры:**

```bash
# Получить животных приюта
curl -H "Cookie: auth_token=<jwt>" \
  -H "X-Shelter-ID: 3" \
  http://localhost:8200/api/animals
```

### Статистика (требует X-Shelter-ID)

```
GET    /api/stats                  Статистика приюта
```

**Ответ GET /api/stats:**
```json
{
  "success": true,
  "data": {
    "total_animals": 45,
    "looking_for_home": 30,
    "adopted_this_month": 5,
    "volunteers": 12
  }
}
```

---

## 📊 Статические файлы

### Uploads (медиа пользователей)

```
GET    /uploads/avatars/:filename      Аватары пользователей
GET    /uploads/covers/:filename       Обложки профилей
GET    /uploads/posts/:filename        Медиа в постах
GET    /uploads/pets/:filename         Фото питомцев
```

**Development:** Локальная папка `./uploads/`  
**Production:** S3 / Yandex Object Storage + CDN

---

## 🔗 Внешние API

### DaData (поиск организаций)

```
Endpoint: https://suggestions.dadata.ru/suggestions/api/4_1/rs/suggest/party
Используется: Main API → /api/organizations/search
```

### Yandex Maps

```
Endpoint: https://api-maps.yandex.ru/2.1/
Используется: Frontend для отображения карт организаций
```

### Yandex Object Storage (планируется)

```
Endpoint: https://storage.yandexcloud.net
Bucket: zooplatform-uploads
Используется: Хранение медиа файлов в Production
```

---

## 🔄 WebSocket (планируется v1.0.0)

### Реал-тайм уведомления

```
WS     ws://localhost:8000/ws          WebSocket соединение
```

**События:**
- `new_message` - новое сообщение
- `new_comment` - новый комментарий
- `new_like` - новый лайк
- `friend_request` - запрос в друзья
- `pet_status_change` - изменение статуса питомца

---

## 📱 Mobile API

Mobile приложение использует те же API endpoints, что и веб-версия.

**Base URL (Dev):** http://localhost:8000  
**Base URL (Prod):** https://api.zooplatform.ru

---

## 🚨 Коды ошибок

### HTTP Status Codes

| Код | Название | Описание | Когда возвращается |
|-----|----------|----------|-------------------|
| 200 | OK | Успешный запрос | GET, PUT, PATCH, DELETE успешны |
| 201 | Created | Ресурс создан | POST успешен |
| 204 | No Content | Успешно, без тела ответа | DELETE успешен (альтернатива 200) |
| 400 | Bad Request | Невалидные данные | Ошибка валидации, неверный JSON |
| 401 | Unauthorized | Не авторизован | Нет токена, токен невалидный |
| 403 | Forbidden | Нет прав доступа | Попытка доступа к чужому ресурсу |
| 404 | Not Found | Ресурс не найден | Запрошенный ID не существует |
| 409 | Conflict | Конфликт данных | Email уже существует, дубликат |
| 429 | Too Many Requests | Превышен лимит запросов | Rate limiting |
| 500 | Internal Server Error | Ошибка сервера | Непредвиденная ошибка |

### Формат ошибок

**Простая ошибка:**
```json
{
  "success": false,
  "error": "User not found"
}
```

**Ошибка валидации:**
```json
{
  "success": false,
  "error": "Validation failed",
  "details": {
    "email": "Invalid email format",
    "password": "Password must be at least 8 characters"
  }
}
```

### Типичные ошибки

**401 Unauthorized:**
- Токен не предоставлен
- Токен истёк
- Токен невалидный
- **Действие:** Перенаправить на страницу входа

**403 Forbidden:**
- Попытка редактировать чужой ресурс
- Недостаточно прав для операции
- Не является участником организации
- **Действие:** Показать сообщение "Доступ запрещён"

**404 Not Found:**
- Пользователь не найден
- Пост не найден
- Питомец не найден
- **Действие:** Показать сообщение "Ресурс не найден"

**409 Conflict:**
- Email уже зарегистрирован
- Пользователь уже участник организации
- Дубликат chip number
- **Действие:** Показать конкретную ошибку пользователю

---

## 📋 Типы событий питомцев (Pet Events)

**Используется в:** PetBase API `/api/pet-events`, Clinic API

| Тип | Код | Описание | Кто создаёт |
|-----|-----|----------|-------------|
| Регистрация | `registration` | Первичная регистрация в клинике | Clinic |
| Подтверждение | `verification` | Подтверждение PetID клиникой | Clinic |
| Смена владельца | `ownership_change` | Передача питомца новому владельцу | Owner, Shelter |
| Стерилизация | `sterilization` | Операция стерилизации | Clinic |
| Вакцинация | `vaccination` | Прививка | Clinic |
| Визит к ветеринару | `medical_visit` | Плановый или внеплановый визит | Clinic |
| Потерялся | `lost` | Питомец потерялся | Owner |
| Нашёлся | `found` | Питомец найден | Owner, Volunteer |
| Смерть | `death` | Смерть питомца | Clinic, Owner |

**Структура события:**
```json
{
  "pet_id": 123,
  "event_type": "verification",
  "event_date": "2025-01-17",
  "description": "Подтверждение PetID клиникой",
  "clinic_id": 5,
  "notes": "Добавлен chip number, медицинская информация"
}
```

---

## 🔐 Роли и права доступа

### Роли пользователей

| Роль | Код | Доступ | Описание |
|------|-----|--------|----------|
| Пользователь | `user` | Main | Обычный пользователь платформы |
| Владелец | `owner` | Main, Owner | Владелец питомца |
| Волонтёр | `volunteer` | Main, Volunteer | Зоопомощник |
| Участник приюта | `shelter_member` | Main, Shelter | Сотрудник приюта |
| Участник клиники | `clinic_member` | Main, Clinic | Сотрудник ветклиники |
| Модератор | `moderator` | Main, Admin | Модератор контента |
| Суперадмин | `superadmin` | Всё | Полный доступ ко всем сервисам |

### Роли в организациях

**Для клиник и приютов:**

| Роль | Код | Права |
|------|-----|-------|
| Владелец | `owner` | Полный доступ, управление командой |
| Администратор | `admin` | Управление командой, настройки |
| Ветеринар | `veterinarian` | Работа с пациентами, медкарты (только клиники) |
| Ассистент | `assistant` | Просмотр информации |
| Участник | `member` | Базовый доступ |

**Проверка прав:**
- Владелец может всё
- Администратор может управлять участниками (кроме владельца)
- Нельзя удалить владельца
- Нельзя удалить самого себя

---

## 📝 Формат ответов

### Успешный ответ:

```json
{
  "success": true,
  "data": {
    // данные
  }
}
```

### Ошибка:

```json
{
  "success": false,
  "error": "Описание ошибки"
}
```

### Пагинация:

```json
{
  "success": true,
  "data": [...],
  "pagination": {
    "page": 1,
    "per_page": 20,
    "total": 150,
    "total_pages": 8
  }
}
```

---

## 🚀 Rate Limiting

Для защиты от DDoS и злоупотреблений:

| Endpoint | Лимит | Статус |
|----------|-------|--------|
| `/api/auth/login` | 5 запросов / минуту | ✅ Реализовано |
| `/api/auth/register` | 3 запроса / час | ✅ Реализовано |
| `/api/posts` (POST) | 10 постов / час | ⏳ Планируется |
| `/api/messages` (POST) | 100 сообщений / час | ⏳ Планируется |
| `/api/pets/*` | 100 запросов / минуту | ⏳ Планируется |
| Остальные | 100 запросов / минуту | ⏳ Планируется |

**Заголовки ответа при rate limiting:**
```
X-RateLimit-Limit: 100
X-RateLimit-Remaining: 95
X-RateLimit-Reset: 1640000000
```

---

## 🔐 Безопасность

### Реализовано

- ✅ JWT аутентификация
- ✅ Проверка владельца ресурсов
- ✅ CORS с проверкой origins
- ✅ Prepared statements (защита от SQL injection)
- ✅ HTTPS в продакшене

### В разработке

- ⏳ Rate limiting для всех endpoints
- ⏳ Валидация входных данных
- ⏳ Логирование всех операций
- ⏳ Мониторинг подозрительной активности

### Рекомендации для клиентов

1. **Хранение токенов:**
   - Web: HttpOnly cookies (защита от XSS)
   - Mobile: Secure Storage / Keychain

2. **Обработка ошибок:**
   - 401 → Перенаправить на логин
   - 403 → Показать сообщение "Доступ запрещён"
   - 429 → Показать "Слишком много запросов, попробуйте позже"

3. **Refresh токены:**
   - Access token: 15 минут
   - Refresh token: 7 дней
   - Автоматическое обновление при 401

---

## 📚 Документация API

**Swagger/OpenAPI:** (планируется)  
**Postman Collection:** (планируется)

**Дополнительная документация:**
- `docs/SECURITY.md` - полная документация по безопасности
- `docs/PETBASE_INTEGRATION.md` - архитектура интеграции ЗооБазы
- `petbase/backend/README_AUTH.md` - инструкция по аутентификации
- `petbase/TESTING_SECURITY.md` - тесты безопасности
- `petbase/INTEGRATION_SUMMARY.md` - сводка по интеграции

---

## 🔮 Планируется в будущем

### v0.12.0 - Медицинские карты (Sprint 11.3)
- Полная медицинская история питомца
- Записи о визитах к ветеринару
- График вакцинаций
- Напоминания о прививках

### v0.13.0 - Онлайн-запись в клинику (Sprint 11.4)
- `/api/clinic/appointments` - запись на приём
- `/api/clinic/schedule` - расписание клиники
- Уведомления о записях

### v1.0.0 - Стабильный релиз
- Полный функционал всех микросервисов
- Мобильное приложение (iOS/Android)
- WebSocket для real-time уведомлений
- Push-уведомления

### v1.1.0 - Родословная
- `/api/petid/pets/:id/parents` - родители питомца
- `/api/petid/pets/:id/pedigree` - полная родословная
- Генеалогическое дерево

### v1.2.0 - ОСВВ и приюты
- `/api/shelter/osvv/*` - программа ОСВВ
- `/api/shelter/:id/visits` - онлайн-запись на посещение приюта
- Календарь мероприятий приюта

### v1.3.0 - Волонтёры и сборы
- `/api/fundraisers/*` - сборы средств
- `/api/volunteer/fosters` - передержки
- Система донатов

### v2.0.0 - Расширенная функциональность
- GraphQL API
- WebSocket для real-time
- Elasticsearch для поиска
- Микросервисы на Kubernetes

---

## 📚 Дополнительная документация

**Архитектура:**
- `docs/STRUCTURE.md` - структура проекта
- `docs/SSO_ARCHITECTURE.md` - архитектура SSO
- `docs/SSO_FLOW.md` - поток авторизации
- `docs/PETBASE_INTEGRATION.md` - интеграция PetBase

**Безопасность:**
- `docs/SECURITY.md` - политики безопасности
- `petbase/TESTING_SECURITY.md` - тесты безопасности

**Разработка:**
- `docs/CHANGELOG.md` - история изменений
- `docs/ROADMAP.md` - план развития
- `docs/MVP.md` - MVP функционал
- `AGENTS.md` - руководство для разработки

**Правила разработки:**
- `.kiro/steering/project-structure.md` - структура проекта
- `.kiro/steering/sso-integration.md` - правила SSO
- `.kiro/steering/api-design.md` - дизайн API
- `.kiro/steering/error-handling.md` - обработка ошибок
- `.kiro/steering/database-migrations.md` - миграции БД
- `.kiro/steering/shared-components.md` - общие компоненты

---

## 🛠️ Инструменты для работы с API

### Postman Collection (планируется)
Полная коллекция всех endpoints с примерами запросов.

### Swagger/OpenAPI (планируется)
Интерактивная документация API с возможностью тестирования.

### SDK (планируется)
- JavaScript/TypeScript SDK
- Python SDK
- Go SDK

---

## 📞 Поддержка

**Вопросы по API:**
- Email: dev@zooplatform.ru
- Telegram: @zooplatform_dev

**Баги и предложения:**
- GitHub Issues: https://github.com/zooplatform/issues

---

**Документ создан:** 29 декабря 2024  
**Последнее обновление:** 18 января 2025 (v0.11.2)  
**Автор:** ЗооПлатформа Team  
**Версия:** 2.0 (полная документация всех микросервисов, Sprint 11.2)
