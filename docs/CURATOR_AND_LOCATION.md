# Система кураторов и локаций питомцев

## Обзор

Система ЗооБаза поддерживает три типа ответственности за питомцев:
1. **Владелец** - постоянный хозяин питомца
2. **Куратор** - волонтёр, который следит за бездомным животным
3. **Никого** - бездомное животное без владельца и куратора

**ВАЖНО:** У животного может быть **ЛИБО** владелец, **ЛИБО** куратор, **ЛИБО** никого. Не может быть одновременно и владелец, и куратор.

## Типы локаций

### home (Дома у владельца)
- Питомец живёт дома у своего владельца
- Обычно используется когда есть `user_id` (владелец)
- Статус: `home` или `adopted`

### street (На улице)
- Бездомное животное
- Может иметь куратора, который следит за ним
- Статус: обычно `looking_for_home` или `lost`

### foster (На передержке)
- Животное временно живёт у волонтёра
- Указывается адрес передержки (`foster_address`)
- Может иметь куратора
- Статус: обычно `looking_for_home`

### shelter (В приюте)
- Животное находится в приюте
- Указывается название приюта (`shelter_name`)
- Может иметь куратора
- Статус: обычно `looking_for_home`

## Поля базы данных

### Куратор
- `curator_id` (INTEGER, NULL) - ID пользователя-куратора в системе
- `curator_name` (TEXT) - ФИО куратора (если не зарегистрирован в системе)
- `curator_phone` (TEXT) - Телефон куратора для связи

### Локация
- `location` (TEXT) - Текущая локация: `home`, `street`, `foster`, `shelter`
- `foster_address` (TEXT) - Адрес передержки (если location = 'foster')
- `shelter_name` (TEXT) - Название приюта (если location = 'shelter')

## Сценарии использования

### 1. Домашний питомец с владельцем
```json
{
  "user_id": 123,
  "name": "Барсик",
  "location": "home",
  "status": "home",
  "owner_name": "Иванов Иван",
  "owner_phone": "+7 999 123-45-67"
}
```

### 2. Бездомное животное с куратором
```json
{
  "user_id": 0,
  "name": "Рыжик",
  "location": "street",
  "status": "looking_for_home",
  "curator_id": 456,
  "curator_name": "Петрова Мария",
  "curator_phone": "+7 999 987-65-43"
}
```

### 3. Животное на передержке
```json
{
  "user_id": 0,
  "name": "Шарик",
  "location": "foster",
  "status": "looking_for_home",
  "foster_address": "г. Москва, ул. Ленина, д. 10",
  "curator_id": 789,
  "curator_name": "Сидорова Анна",
  "curator_phone": "+7 999 111-22-33"
}
```

### 4. Животное в приюте
```json
{
  "user_id": 0,
  "name": "Мурка",
  "location": "shelter",
  "status": "looking_for_home",
  "shelter_name": "Приют 'Добрые руки'",
  "curator_name": "Волонтёр Ольга",
  "curator_phone": "+7 999 444-55-66"
}
```

### 5. Бездомное животное без присмотра
```json
{
  "user_id": 0,
  "name": "Безымянный",
  "location": "street",
  "status": "looking_for_home"
}
```
Примечание: Нет ни владельца, ни куратора - животное живёт на улице без присмотра.

## API Endpoints

Все существующие endpoints для питомцев поддерживают новые поля:

- `GET /api/pets` - Получить всех питомцев (включая curator и location)
- `GET /api/pets/:id` - Получить питомца (включая curator и location)
- `POST /api/pets` - Создать питомца (можно указать curator и location)
- `PUT /api/pets/:id` - Обновить питомца (можно изменить curator и location)

## UI

### Вкладка "Куратор и локация"

На странице питомца добавлена новая вкладка "🤝 Куратор и локация", которая показывает:

1. **Текущая локация** - где находится питомец:
   - 🏠 Дома у владельца (зелёный)
   - 🌆 На улице (оранжевый)
   - 🏡 На передержке (фиолетовый)
   - 🏥 В приюте (синий)

2. **Адрес/название** - в зависимости от локации:
   - Адрес передержки (для foster)
   - Название приюта (для shelter)

3. **Информация о кураторе**:
   - ID куратора в системе
   - ФИО куратора
   - Телефон куратора (кликабельный)

4. **Кто отвечает за питомца** - визуальное отображение:
   - 👤 Есть владелец (зелёный блок)
   - 🤝 Есть куратор (синий блок)
   - 🐾 Бездомное животное без присмотра (серый блок)

5. **Пояснение** - краткая справка о системе владельцев и кураторов

## Логика работы

### Правила
1. У животного может быть **ЛИБО** владелец (`user_id`), **ЛИБО** куратор (`curator_id`/`curator_name`), **ЛИБО** никого
2. Если есть `user_id` (владелец), то `location` обычно `home`, куратора быть не должно
3. Если есть `curator`, но нет `user_id`, то `location` может быть `street`, `foster`, `shelter`
4. Если нет ни `user_id`, ни `curator`, то животное бездомное без присмотра

### Программа ОСВВ
Животные, прошедшие программу ОСВВ (Отлов-Стерилизация-Вакцинация-Возврат):
- Имеют ушную бирку (`ear_tag_number`)
- **Обычно имеют куратора** (волонтёра, который следит за ними)
- `location` = `street` (возвращены на улицу)
- `is_sterilized` = true
- `is_vaccinated` = true
- `user_id` = 0 или NULL (нет владельца)

## Миграция

Миграция `015_add_curator_and_location.sql` добавляет все необходимые поля с безопасными DEFAULT значениями:
- Существующие питомцы получают `location = 'home'`
- Все новые поля опциональные (могут быть пустыми)
- Данные не теряются

## Следующие шаги

1. Добавить фильтры по локации на главной странице
2. Добавить статистику по локациям
3. Создать отдельную страницу для кураторов
4. Добавить уведомления кураторам о событиях с их питомцами
5. Интеграция с картами для отображения локаций

---

**Последнее обновление:** 29 декабря 2024
