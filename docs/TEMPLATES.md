# Шаблоны компонентов

Этот файл содержит шаблоны для создания новых страниц и компонентов в проекте.

---

## Шаблон страницы (Page Template)

Используйте этот шаблон для создания новых страниц в `frontend/app/(main)/`.

### Структура файла

```tsx
'use client';

import { useState } from 'react';

export default function PageName() {
  return (
    <div>
      {/* Заголовок страницы */}
      <div className="mb-6">
        <h1 className="text-2xl font-bold text-gray-900 mb-2">Название страницы</h1>
        <p className="text-gray-600">Описание страницы</p>
      </div>

      {/* Основной контент */}
      <div className="bg-white rounded-xl shadow-sm p-6 border border-gray-100">
        <h2 className="text-lg font-semibold text-gray-900 mb-4">Заголовок секции</h2>
        
        {/* Контент секции */}
        <div className="space-y-4">
          {/* Ваш контент здесь */}
        </div>
      </div>

      {/* Дополнительная информация (опционально) */}
      <div className="mt-6 bg-blue-50 border border-blue-200 rounded-lg p-4">
        <h3 className="font-semibold text-blue-900 mb-2">💡 Подсказка:</h3>
        <ul className="text-sm text-blue-800 space-y-1">
          <li>• Пункт 1</li>
          <li>• Пункт 2</li>
          <li>• Пункт 3</li>
        </ul>
      </div>
    </div>
  );
}
```

### Важные правила

1. **НЕ используйте** `max-w-*` классы на корневом div - это создаст отступ справа
2. **Используйте** `'use client'` если нужен state или интерактивность
3. **Структура отступов**:
   - `mb-6` для заголовка страницы
   - `mt-6` для дополнительных блоков
   - `space-y-4` для вертикальных отступов между элементами

### Цветовая схема

- **Синий (основной)**: `#1B76FF` - кнопки, ссылки, акценты
- **Красный (уведомления)**: `#FC2B2B` - бейджи, важные элементы
- **Фон сайта**: `linear-gradient(to bottom right, #f9fafb, #f3f4f6)`
- **Карточки**: `bg-white` с `border border-gray-100` и `shadow-sm`

---

## Шаблон информационного блока

```tsx
<div className="p-4 bg-gray-50 rounded-lg">
  <h3 className="font-semibold text-gray-900 mb-3 flex items-center gap-2">
    🏢 Заголовок блока
  </h3>
  <div className="space-y-2 text-sm">
    <div>
      <span className="text-gray-600">Метка:</span>{' '}
      <span className="text-gray-900">Значение</span>
    </div>
  </div>
</div>
```

---

## Шаблон карточки с тенью

```tsx
<div className="bg-white rounded-xl shadow-sm p-6 border border-gray-100">
  <h2 className="text-lg font-semibold text-gray-900 mb-4">Заголовок</h2>
  {/* Контент */}
</div>
```

---

## Шаблон кнопки

```tsx
<button 
  className="px-4 py-2 rounded-lg text-white font-medium transition-colors"
  style={{ backgroundColor: '#1B76FF' }}
>
  Текст кнопки
</button>
```

---

## Шаблон поиска с автодополнением

```tsx
<div className="relative">
  <SearchIcon className="absolute left-3 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" />
  <input
    type="text"
    placeholder="Поиск..."
    className="w-full pl-10 pr-4 py-3 text-sm border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:border-transparent"
    style={{ '--tw-ring-color': '#1B76FF' } as React.CSSProperties}
  />
</div>
```

---

## Структура папок для новой страницы

```
frontend/app/(main)/
  └── page-name/
      └── page.tsx
```

**Пример**: Для страницы "Организации" по адресу `/org`:
```
frontend/app/(main)/
  └── org/
      └── page.tsx
```

---

## Layout структура

Все страницы в `(main)` автоматически получают:
- Header (липкий, сверху)
- Sidebar (слева, 180px, скрыт на мобильных)
- Main content (flex-1, центр)

**Ширина контейнера**: `max-w-[1150px]` (стиль VK)

---

## Адаптивность

- **Desktop (lg+)**: Sidebar виден, полная ширина
- **Tablet (md-lg)**: Sidebar скрыт, контент на всю ширину
- **Mobile (<md)**: Компактный вид, мобильное меню

---

## Примеры использования

### Страница со списком
- `/org` - Организации (поиск + детальная информация)

### Страница с лентой
- `/` - Главная (лента + правая панель с фильтрами)

### Статическая страница
- `/about` - О платформе (текст + изображения)

---

## Чек-лист перед созданием новой страницы

- [ ] Создана папка в `frontend/app/(main)/`
- [ ] Файл называется `page.tsx`
- [ ] Добавлен `'use client'` если нужна интерактивность
- [ ] НЕ используется `max-w-*` на корневом div
- [ ] Используются цвета из палитры (#1B76FF, #FC2B2B)
- [ ] Добавлена ссылка в `Sidebar.tsx` если нужно
- [ ] Проверена адаптивность на разных экранах
