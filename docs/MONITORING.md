# Система мониторинга и логирования ошибок

## Обзор

Система мониторинга позволяет отслеживать здоровье всех микросервисов, логировать ошибки и анализировать производительность в реальном времени.

## Компоненты системы

### 1. База данных (SQLite)

**Таблицы:**
- `error_logs` - логи всех ошибок (4xx, 5xx статусы)
- `request_logs` - логи всех запросов (опционально)
- `performance_metrics` - метрики производительности
- `system_alerts` - системные алерты

**Представления:**
- `error_stats_24h` - статистика ошибок за 24 часа по сервисам
- `request_stats_hourly` - почасовая статистика запросов

### 2. Backend API (Admin Backend - порт 9000)

**Endpoints:**

```
GET /api/monitoring/errors
Возвращает последние 50 ошибок
Response: { success: true, data: ErrorLog[] }

GET /api/monitoring/metrics
Возвращает системные метрики
Response: { success: true, data: SystemMetrics }

GET /api/monitoring/error-stats
Возвращает статистику ошибок по сервисам за 24ч
Response: { success: true, data: { [service: string]: number } }

GET /api/health
Проверяет статус всех сервисов
Response: { services: ServiceStatus[], overall: string }
```

### 3. Frontend Dashboard (Admin Frontend - http://localhost:4000/monitoring)

**Функции:**
- ✅ Отображение активных пользователей
- ✅ Счетчики ошибок (за час, за 24ч)
- ✅ Статус всех сервисов (online/slow/offline)
- ✅ Таблица последних ошибок с деталями
- ✅ Статистика ошибок по сервисам
- ✅ Авто-обновление каждые 10 секунд

## Как это работает

### Автоматическое логирование ошибок

Каждый backend сервис может использовать middleware для автоматического логирования:

```go
// В main.go любого сервиса
import "admin/handlers"

// Оборачиваем все роуты в middleware
http.Handle("/api/", handlers.LogErrorMiddleware(db, "Main Backend")(
    http.HandlerFunc(yourHandler),
))
```

**Что логируется:**
- Service name (Main Backend, Clinic Backend, etc.)
- Endpoint (URL path)
- HTTP method (GET, POST, etc.)
- Error message (HTTP status text)
- User ID (если авторизован)
- IP address
- User Agent
- Timestamp

### Проверка здоровья сервисов

Health check автоматически проверяет все сервисы каждые 5 секунд:

```go
// Проверяет доступность endpoint
GET http://localhost:8000/api/health -> 200 OK (online)
GET http://localhost:8100/api/health -> timeout (offline)
GET http://localhost:8200/api/health -> 200 OK but slow (slow)
```

**Статусы:**
- `online` - сервис отвечает < 1 секунды
- `slow` - сервис отвечает > 1 секунды
- `offline` - сервис не отвечает или ошибка

## Использование

### Для разработчиков

**1. Просмотр ошибок в реальном времени:**
```bash
# Открыть Admin панель
http://localhost:4000/monitoring

# Или через API
curl http://localhost:9000/api/monitoring/errors
```

**2. Мониторинг статуса сервисов:**
```bash
# Скрипт для мониторинга соединений
./scripts/watch-requests.sh

# Health check всех сервисов
curl http://localhost:9000/api/health
```

**3. Анализ ошибок:**
```sql
-- Топ ошибок за последние 24 часа
SELECT service, COUNT(*) as error_count
FROM error_logs
WHERE created_at > datetime('now', '-24 hours')
GROUP BY service
ORDER BY error_count DESC;

-- Ошибки конкретного пользователя
SELECT * FROM error_logs
WHERE user_id = 1
ORDER BY created_at DESC
LIMIT 10;
```

### Для администраторов

**Dashboard показывает:**
- 👥 Активные пользователи (онлайн сейчас)
- ⚠️ Ошибки за последний час
- 📊 Ошибки за последние 24 часа
- 💾 Размер базы данных
- 🟢 Статус каждого сервиса
- 📋 Последние 50 ошибок с деталями

**Алерты:**
- Если сервис offline - красная карточка
- Если сервис slow - желтая карточка
- Если > 10 ошибок за час - показывается в статистике

## Примеры использования

### Пример 1: Пользователь встретил ошибку 404

**Что происходит:**
1. User делает запрос: `GET /api/posts/999`
2. Backend возвращает 404 Not Found
3. Middleware автоматически логирует:
   ```json
   {
     "service": "Main Backend",
     "endpoint": "/api/posts/999",
     "method": "GET",
     "error_message": "Not Found",
     "user_id": 1,
     "ip_address": "127.0.0.1",
     "created_at": "2024-01-30 10:00:00"
   }
   ```
4. Ошибка появляется в dashboard через 10 секунд

### Пример 2: Сервис упал

**Что происходит:**
1. Health check пытается подключиться к сервису
2. Получает timeout или connection refused
3. Статус меняется на `offline`
4. В dashboard сервис показывается красным с ❌
5. Администратор видит проблему и перезапускает сервис

### Пример 3: Медленный ответ

**Что происходит:**
1. Health check получает ответ за 1500ms
2. Статус меняется на `slow`
3. В dashboard сервис показывается желтым с ⚠️
4. Администратор может проверить нагрузку и оптимизировать

## Скрипты мониторинга

### 1. watch-requests.sh
Показывает активные соединения к каждому сервису:
```bash
./scripts/watch-requests.sh
```

### 2. monitor-requests.sh
Показывает каждое новое соединение в реальном времени:
```bash
./scripts/monitor-requests.sh
```

### 3. log-requests.sh
Логирует запросы в файлы:
```bash
./scripts/log-requests.sh
```

## Настройка алертов (будущее)

В будущем можно добавить:
- Email уведомления при критических ошибках
- Telegram бот для алертов
- Slack интеграция
- Автоматический перезапуск упавших сервисов

## Производительность

**Влияние на производительность:**
- Логирование ошибок: ~1-2ms на запрос (асинхронно)
- Health check: не влияет на основные запросы
- Dashboard: обновляется каждые 10 секунд

**Оптимизация:**
- Ошибки логируются асинхронно (goroutine)
- Старые логи можно удалять (> 30 дней)
- Индексы на всех важных полях

## Безопасность

**Доступ к мониторингу:**
- ✅ Только для superadmin
- ✅ Требуется авторизация
- ✅ Не показывает чувствительные данные (пароли, токены)

**Что НЕ логируется:**
- Пароли
- JWT токены
- Платежные данные
- Личная информация (кроме user_id)

## Troubleshooting

### Проблема: Ошибки не логируются

**Решение:**
1. Проверьте что middleware подключен
2. Проверьте что таблица `error_logs` существует
3. Проверьте права на запись в БД

### Проблема: Dashboard не обновляется

**Решение:**
1. Проверьте что Admin Backend запущен (порт 9000)
2. Проверьте CORS настройки
3. Откройте консоль браузера для ошибок

### Проблема: Все сервисы показываются offline

**Решение:**
1. Проверьте что сервисы запущены: `./run`
2. Проверьте что health endpoints доступны
3. Проверьте firewall/network настройки

## Roadmap

**Планируется добавить:**
- [ ] Графики производительности (response time)
- [ ] Алерты в Telegram
- [ ] Экспорт логов в CSV/JSON
- [ ] Фильтры по дате/сервису/пользователю
- [ ] Автоматическое создание тикетов для критических ошибок
- [ ] Интеграция с Sentry/Datadog
- [ ] Метрики использования памяти/CPU
- [ ] Distributed tracing

---

**Документация обновлена:** 30.01.2024
