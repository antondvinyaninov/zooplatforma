# Архитектура микросервисов - Полное описание

**Дата:** 17 января 2025  
**Версия:** 2.0

---

## 📋 Содержание

1. [Auth Service](#1-auth-service-порт-7000) - Авторизация и управление пользователями
2. [Main Service](#2-main-service-порт-8000) - Социальная сеть
3. [PetBase Service](#3-petbase-service-порт-8100) - База данных животных
4. [Admin Service](#4-admin-service-порт-9000) - Административная панель
5. [Clinic Service](#5-clinic-service-порт-8600) - Ветеринарные клиники
6. [Shelter Service](#6-shelter-service-порт-8200) - Приюты
7. [Owner Service](#7-owner-service-порт-8400) - Владельцы питомцев
8. [Volunteer Service](#8-volunteer-service-порт-8500) - Зоопомощники
9. [Связи между сервисами](#связи-между-сервисами)

---

## 1. Auth Service (порт 7000)

### Назначение
Единая точка авторизации для всей платформы (SSO - Single Sign-On)

### База данных
`auth/database/auth.db`

### Таблицы
```sql
users                    -- Пользователи
sessions                 -- Активные сессии
refresh_tokens           -- Refresh токены
password_resets          -- Сброс пароля
email_verifications      -- Подтверждение email
```

### API Endpoints

#### Авторизация
```
POST   /api/auth/register          Регистрация нового пользователя
POST   /api/auth/login             Вход в систему
POST   /api/auth/logout            Выход из системы
POST   /api/auth/refresh           Обновить токен
GET    /api/auth/me                Получить текущего пользователя
GET    /api/auth/verify            Проверить токен (для других сервисов)
```

#### Управление паролем
```
POST   /api/auth/forgot-password   Запросить сброс пароля
POST   /api/auth/reset-password    Сбросить пароль
POST   /api/auth/change-password   Изменить пароль
```

#### Email верификация
```
POST   /api/auth/send-verification Отправить письмо подтверждения
POST   /api/auth/verify-email      Подтвердить email
```

### Используется сервисами
- Main, Admin, PetBase, Clinic, Shelter, Owner, Volunteer (все!)

---

## 2. Main Service (порт 8000)

### Назначение
Социальная сеть для владельцев питомцев

### База данных
`main/database/main.db`

### Таблицы
```sql
posts                    -- Посты
comments                 -- Комментарии
likes                    -- Лайки
polls                    -- Опросы
poll_options             -- Варианты опросов
poll_votes               -- Голоса в опросах
friends                  -- Друзья
friend_requests          -- Запросы в друзья
notifications            -- Уведомления
messages                 -- Личные сообщения
organizations            -- Организации
organization_members     -- Участники организаций
favorites                -- Избранное
reports                  -- Жалобы
user_activity            -- Активность пользователей
user_logs                -- Логи действий пользователей
```


### API Endpoints

#### Посты
```
GET    /api/posts                  Лента постов (публично)
GET    /api/posts/:id              Получить пост
POST   /api/posts                  Создать пост
PUT    /api/posts/:id              Обновить пост
DELETE /api/posts/:id              Удалить пост
GET    /api/posts/drafts           Черновики
GET    /api/posts/user/:id         Посты пользователя
GET    /api/posts/organization/:id Посты организации
```

#### Лайки
```
POST   /api/posts/:id/like         Лайк/анлайк
GET    /api/posts/:id/like         Статус лайка
GET    /api/posts/:id/likers       Список лайкнувших
```

#### Комментарии
```
GET    /api/comments               Комментарии к посту
POST   /api/comments               Добавить комментарий
DELETE /api/comments/:id           Удалить комментарий
```

#### Опросы
```
POST   /api/polls/:id/vote         Проголосовать
GET    /api/polls/:id/results      Результаты опроса
```

#### Друзья
```
GET    /api/friends                Список друзей
GET    /api/friends/requests       Входящие запросы
POST   /api/friends/send           Отправить запрос
POST   /api/friends/accept         Принять запрос
POST   /api/friends/reject         Отклонить запрос
DELETE /api/friends/remove         Удалить из друзей
GET    /api/friends/status         Статус дружбы
```

#### Уведомления
```
GET    /api/notifications          Список уведомлений
GET    /api/notifications/unread   Количество непрочитанных
PUT    /api/notifications/:id      Отметить прочитанным
POST   /api/notifications/read-all Отметить все прочитанными
```

#### Мессенджер
```
GET    /api/chats                  Список диалогов
GET    /api/chats/:userId          История с пользователем
POST   /api/messages/send          Отправить сообщение
POST   /api/messages/send-media    Отправить медиа
GET    /api/messages/unread        Непрочитанные сообщения
```

#### Организации
```
GET    /api/organizations/all      Все организации
GET    /api/organizations/my       Мои организации
POST   /api/organizations          Создать организацию
GET    /api/organizations/:id      Получить организацию
PUT    /api/organizations/:id      Обновить организацию
GET    /api/organizations/:id/members  Участники
POST   /api/organizations/:id/members  Добавить участника
PUT    /api/organizations/:id/members/:userId  Обновить участника
DELETE /api/organizations/:id/members/:userId  Удалить участника
```

#### Медиа
```
POST   /api/media/upload           Загрузить файл
GET    /api/media/user/:id         Галерея пользователя
DELETE /api/media/:id              Удалить файл
POST   /api/profile/avatar         Загрузить аватар
DELETE /api/profile/avatar         Удалить аватар
POST   /api/profile/cover          Загрузить обложку
DELETE /api/profile/cover          Удалить обложку
```

#### Избранное
```
GET    /api/favorites              Избранные питомцы
POST   /api/favorites              Добавить в избранное
DELETE /api/favorites/:id          Удалить из избранного
```

#### Жалобы
```
POST   /api/reports                Создать жалобу
```

#### Профиль
```
GET    /api/profile                Мой профиль
PUT    /api/profile                Обновить профиль
GET    /api/users/:id              Профиль пользователя
```

### Зависимости от других сервисов
- **Auth Service** - проверка авторизации
- **PetBase Service** - данные о питомцах для постов

---

## 3. PetBase Service (порт 8100)

### Назначение
Центральная база данных всех животных платформы

### База данных
`petbase/database/petbase.db`

### Таблицы
```sql
species                  -- Виды животных (собаки, кошки, птицы...)
breeds                   -- Породы
pet_cards                -- Информационные карточки о породах
pets                     -- ВСЕ ПИТОМЦЫ (главная таблица!)
pet_medical_records      -- Медицинские записи
pet_vaccinations         -- Вакцинации
pet_events               -- История событий
pet_chips                -- Чипы и метки
pet_pedigree             -- Родословная
pet_photos               -- Фотографии питомцев
```

### API Endpoints

#### Справочники (публичные)
```
GET    /api/species                Виды животных
GET    /api/species/:id            Информация о виде
POST   /api/species                Создать вид (superadmin)
PUT    /api/species/:id            Обновить вид (superadmin)
DELETE /api/species/:id            Удалить вид (superadmin)

GET    /api/breeds                 Породы
GET    /api/breeds/:id             Информация о породе
GET    /api/breeds/species/:id     Породы конкретного вида
POST   /api/breeds                 Создать породу (superadmin)
PUT    /api/breeds/:id             Обновить породу (superadmin)
DELETE /api/breeds/:id             Удалить породу (superadmin)

GET    /api/cards                  Информационные карточки
GET    /api/cards/:id              Карточка породы
GET    /api/cards/breed/:id        Карточки конкретной породы
POST   /api/cards                  Создать карточку (superadmin)
PUT    /api/cards/:id              Обновить карточку (superadmin)
DELETE /api/cards/:id              Удалить карточку (superadmin)
```

#### Питомцы (главная таблица)
```
GET    /api/pets                   Все питомцы (с фильтрами)
GET    /api/pets/:id               Конкретный питомец
POST   /api/pets                   Создать питомца
PUT    /api/pets/:id               Обновить питомца
DELETE /api/pets/:id               Удалить питомца
GET    /api/pets/user/:userId      Питомцы пользователя
GET    /api/pets/organization/:id  Питомцы организации
GET    /api/pets/status/:status    Питомцы по статусу
GET    /api/pets/:id/summary       Краткая информация
```

#### Медицинские записи
```
GET    /api/pets/:id/medical       Медкарта питомца
POST   /api/pets/:id/medical       Добавить запись
PUT    /api/medical/:id            Обновить запись
DELETE /api/medical/:id            Удалить запись
```

#### Вакцинации
```
GET    /api/pets/:id/vaccinations  Вакцинации питомца
POST   /api/pets/:id/vaccinations  Добавить вакцинацию
PUT    /api/vaccinations/:id       Обновить вакцинацию
DELETE /api/vaccinations/:id       Удалить вакцинацию
```

#### Чипы и метки
```
GET    /api/chips/:number          Найти по номеру чипа
POST   /api/chips                  Зарегистрировать чип
PUT    /api/chips/:id              Обновить чип
GET    /api/pets/:id/chip          Чип питомца
```

#### История событий
```
GET    /api/pets/:id/events        История питомца
POST   /api/pet-events             Добавить событие
GET    /api/petid/:id/medical      Медицинская история
```

#### Родословная
```
GET    /api/pets/:id/parents       Родители
POST   /api/pets/:id/parents       Указать родителей
GET    /api/pets/:id/children      Потомки
GET    /api/pets/:id/pedigree      Полная родословная
```

#### Фотографии
```
POST   /api/pets/:id/photos        Загрузить фото
GET    /api/pets/:id/photos        Фотографии питомца
DELETE /api/photos/:id             Удалить фото
```

### Используется сервисами
- Main - для отображения питомцев в постах
- Clinic - для медицинских записей
- Shelter - для животных в приюте
- Owner - для управления своими питомцами
- Volunteer - для подопечных животных

---

## 4. Admin Service (порт 9000)

### Назначение
Административная панель для модерации и управления платформой

### База данных
`admin/database/admin.db`

### Таблицы
```sql
admin_logs               -- Логи действий администраторов
moderation_queue         -- Очередь модерации
banned_users             -- Забаненные пользователи
banned_ips               -- Забаненные IP
system_settings          -- Настройки системы
```

### API Endpoints

#### Пользователи
```
GET    /api/admin/users            Список пользователей
GET    /api/admin/users/:id        Информация о пользователе
PUT    /api/admin/users/:id        Обновить пользователя
DELETE /api/admin/users/:id        Забанить пользователя
POST   /api/admin/users/:id/unban  Разбанить пользователя
GET    /api/admin/users/banned     Забаненные пользователи
```

#### Посты
```
GET    /api/admin/posts            Список постов
GET    /api/admin/posts/:id        Информация о посте
DELETE /api/admin/posts/:id        Удалить пост
POST   /api/admin/posts/:id/hide   Скрыть пост
POST   /api/admin/posts/:id/show   Показать пост
```

#### Организации
```
GET    /api/admin/organizations    Список организаций
PUT    /api/admin/organizations/:id Верифицировать организацию
DELETE /api/admin/organizations/:id Удалить организацию
GET    /api/admin/organizations/stats Статистика организаций
```

#### Модерация
```
GET    /api/admin/moderation/reports Список жалоб
GET    /api/admin/moderation/reports/:id Информация о жалобе
PUT    /api/admin/moderation/reports/:id Рассмотреть жалобу
GET    /api/admin/moderation/queue  Очередь модерации
GET    /api/admin/moderation/stats  Статистика модерации
```

#### Статистика
```
GET    /api/admin/stats/overview   Общая статистика
GET    /api/admin/stats/users      Статистика пользователей
GET    /api/admin/stats/posts      Статистика постов
GET    /api/admin/stats/pets       Статистика питомцев
GET    /api/admin/stats/organizations Статистика организаций
```

#### Логи
```
GET    /api/admin/logs             Системные логи
GET    /api/admin/logs/admin       Логи администраторов
GET    /api/admin/logs/user/:id    Логи пользователя
```

#### Настройки
```
GET    /api/admin/settings         Настройки системы
PUT    /api/admin/settings         Обновить настройки
```

### Зависимости от других сервисов
- **Auth Service** - проверка прав superadmin
- **Main Service** - модерация постов, пользователей
- **PetBase Service** - статистика питомцев

---

## 5. Clinic Service (порт 8600)

### Назначение
Кабинет ветеринарной клиники

### База данных
`clinic/database/clinic.db`

### Таблицы
```sql
clinic_appointments      -- Записи на прием
clinic_patients          -- Пациенты клиники (ссылки на pets)
clinic_schedules         -- Расписание клиники
clinic_services          -- Услуги клиники
clinic_invoices          -- Счета
clinic_staff             -- Сотрудники клиники
```

### API Endpoints

#### Клиники
```
GET    /api/my-clinics             Мои клиники
POST   /api/clinics                Создать клинику
GET    /api/organization           Текущая клиника (требует X-Clinic-ID)
PUT    /api/organization           Обновить клинику
```

#### Команда
```
GET    /api/members                Участники клиники
POST   /api/members                Добавить участника
PUT    /api/members                Обновить участника
DELETE /api/members                Удалить участника
```

#### Пациенты
```
GET    /api/patients               Пациенты клиники
GET    /api/patients/:id           Информация о пациенте
POST   /api/patients               Добавить пациента
PUT    /api/patients/:id           Обновить пациента
```

#### Записи на прием
```
GET    /api/appointments           Записи клиники
GET    /api/appointments/:id       Информация о записи
POST   /api/appointments           Создать запись
PUT    /api/appointments/:id       Обновить запись
DELETE /api/appointments/:id       Отменить запись
GET    /api/appointments/today     Записи на сегодня
GET    /api/appointments/upcoming  Предстоящие записи
```

#### Расписание
```
GET    /api/schedules              Расписание клиники
POST   /api/schedules              Создать расписание
PUT    /api/schedules/:id          Обновить расписание
DELETE /api/schedules/:id          Удалить расписание
```

#### Услуги
```
GET    /api/services               Услуги клиники
POST   /api/services               Добавить услугу
PUT    /api/services/:id           Обновить услугу
DELETE /api/services/:id           Удалить услугу
```

#### Счета
```
GET    /api/invoices               Счета клиники
GET    /api/invoices/:id           Информация о счете
POST   /api/invoices               Создать счет
PUT    /api/invoices/:id           Обновить счет
```

### Зависимости от других сервисов
- **Auth Service** - авторизация
- **PetBase Service** - данные питомцев, медицинские записи
- **Main Service** - данные организации

---

## 6. Shelter Service (порт 8200)

### Назначение
Кабинет приюта для животных

### База данных
`shelter/database/shelter.db`

### Таблицы
```sql
shelter_adoptions        -- Заявки на пристройство
shelter_volunteers       -- Волонтеры приюта
shelter_donations        -- Пожертвования
shelter_expenses         -- Расходы приюта
shelter_animals          -- Животные в приюте (ссылки на pets)
```

### API Endpoints

#### Приюты
```
GET    /api/my-shelters            Мои приюты
POST   /api/shelters               Создать приют
GET    /api/organization           Текущий приют (требует X-Shelter-ID)
PUT    /api/organization           Обновить приют
```

#### Животные
```
GET    /api/my-animals             Животные приюта
GET    /api/animals/:id            Информация о животном
POST   /api/animals                Добавить животное
PUT    /api/animals/:id            Обновить животное
DELETE /api/animals/:id            Удалить животное
```

#### Заявки на пристройство
```
GET    /api/adoptions              Заявки на пристройство
GET    /api/adoptions/:id          Информация о заявке
POST   /api/adoptions              Создать заявку
PUT    /api/adoptions/:id          Обновить заявку
POST   /api/adoptions/:id/approve  Одобрить заявку
POST   /api/adoptions/:id/reject   Отклонить заявку
```

#### Волонтеры
```
GET    /api/volunteers             Волонтеры приюта
POST   /api/volunteers             Добавить волонтера
DELETE /api/volunteers/:id         Удалить волонтера
```

#### Пожертвования
```
GET    /api/donations              Пожертвования
POST   /api/donations              Добавить пожертвование
GET    /api/donations/stats        Статистика пожертвований
```

#### Расходы
```
GET    /api/expenses               Расходы приюта
POST   /api/expenses               Добавить расход
PUT    /api/expenses/:id           Обновить расход
DELETE /api/expenses/:id           Удалить расход
GET    /api/expenses/stats         Статистика расходов
```

### Зависимости от других сервисов
- **Auth Service** - авторизация
- **PetBase Service** - данные животных
- **Main Service** - данные организации

---

## 7. Owner Service (порт 8400)

### Назначение
Кабинет владельца питомца

### База данных
Не имеет своей БД, использует PetBase

### API Endpoints

#### Питомцы
```
GET    /api/pets                   Мои питомцы
GET    /api/pets/:id               Информация о питомце
POST   /api/pets                   Добавить питомца
PUT    /api/pets/:id               Обновить питомца
DELETE /api/pets/:id               Удалить питомца
POST   /api/pets/:id/photo         Загрузить фото
```

#### Медицинские записи
```
GET    /api/pets/:id/medical       Медкарта питомца
GET    /api/pets/:id/vaccinations  Вакцинации
```

#### Профиль
```
GET    /api/profile                Мой профиль
```

### Зависимости от других сервисов
- **Auth Service** - авторизация
- **PetBase Service** - все операции с питомцами

---

## 8. Volunteer Service (порт 8500)

### Назначение
Кабинет зоопомощника

### База данных
`volunteer/database/volunteer.db`

### Таблицы
```sql
volunteer_tasks          -- Задачи зоопомощника
volunteer_reports        -- Отчеты о работе
volunteer_hours          -- Учет часов работы
```

### API Endpoints

#### Подопечные
```
GET    /api/my-pets                Подопечные питомцы (curator_id = user_id)
GET    /api/pets/:id               Информация о питомце
PUT    /api/pets/:id               Обновить информацию
```

#### Задачи
```
GET    /api/my-tasks               Мои задачи
GET    /api/tasks/:id              Информация о задаче
POST   /api/tasks                  Создать задачу
PUT    /api/tasks/:id              Обновить задачу
POST   /api/tasks/:id/complete     Завершить задачу
```

#### Отчеты
```
GET    /api/reports                Мои отчеты
POST   /api/reports                Создать отчет
GET    /api/reports/:id            Информация об отчете
```

#### Учет времени
```
GET    /api/hours                  Учет часов
POST   /api/hours                  Добавить часы
GET    /api/hours/stats            Статистика часов
```

#### Профиль
```
GET    /api/profile                Мой профиль
```

### Зависимости от других сервисов
- **Auth Service** - авторизация
- **PetBase Service** - данные подопечных питомцев

---

## Связи между сервисами

### Схема взаимодействия

```
                    ┌─────────────────┐
                    │  Auth Service   │
                    │    (порт 7000)  │
                    └────────┬────────┘
                             │
                    Все проверяют токены
                             │
        ┌────────────────────┼────────────────────┐
        ↓                    ↓                    ↓
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│ Main Service  │    │PetBase Service│    │ Admin Service │
│  (порт 8000)  │    │  (порт 8100)  │    │  (порт 9000)  │
└───────┬───────┘    └───────┬───────┘    └───────┬───────┘
        │                    │                    │
        │                    │                    │
        └────────────────────┼────────────────────┘
                             │
                    Все используют PetBase
                    для данных о животных
                             │
        ┌────────────────────┼────────────────────┐
        ↓                    ↓                    ↓
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│Clinic Service │    │Shelter Service│    │ Owner Service │
│  (порт 8600)  │    │  (порт 8200)  │    │  (порт 8400)  │
└───────────────┘    └───────────────┘    └───────────────┘
        │                    │                    │
        └────────────────────┼────────────────────┘
                             │
                    ┌───────────────┐
                    │Volunteer Svc  │
                    │  (порт 8500)  │
                    └───────────────┘
```

### Правила взаимодействия

1. **Авторизация**: Все сервисы → Auth Service
2. **Данные животных**: Все сервисы → PetBase Service
3. **Социальные функции**: Все сервисы → Main Service (посты, друзья)
4. **Модерация**: Admin Service → Main Service, PetBase Service

---

## Порты сервисов

| Сервис | Backend | Frontend | Назначение |
|--------|---------|----------|------------|
| Auth | 7000 | - | Авторизация (только API) |
| Main | 8000 | 3000 | Социальная сеть |
| PetBase | 8100 | 4100 | База данных животных |
| Admin | 9000 | 4000 | Админ панель |
| Shelter | 8200 | 5100 | Приюты |
| Owner | 8400 | 6100 | Владельцы |
| Volunteer | 8500 | 6200 | Зоопомощники |
| Clinic | 8600 | 6300 | Ветклиники |

---

**Дата обновления:** 17 января 2025
