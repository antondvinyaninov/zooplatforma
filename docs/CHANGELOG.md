# Changelog

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/),
и этот проект придерживается [Semantic Versioning](https://semver.org/lang/ru/).

<!-- 
ПРАВИЛО: В секции "Технологии" указывать ТОЛЬКО новые или измененные технологии.
НЕ перечислять весь стек проекта в каждой версии!

✅ Правильно:
### Технологии
- Добавлено: DaData API для поиска организаций
- Обновлено: React 18 → 19

❌ Неправильно:
### Технологии
- Go 1.25.5
- Node.js 25.2.0
- Next.js 16.0.8
- (весь стек проекта)

Полный стек технологий находится в README.md
Подробные правила: ../.kiro/steering/changelog-rules.md
-->

## [Unreleased]

### Added
- **ЭТАП 7 - Спринт 7.1: База данных и API для истории событий питомцев**
  - **Миграция 032: Таблица `pet_events`**
    - Полная история жизни питомца от рождения до смерти
    - 10 типов событий: registration, ownership_change, sterilization, vaccination, treatment, lost, found, death, shelter_intake, adoption
    - Привязка к создателю события (пользователь, клиника, организация)
    - Медицинские данные: вакцины, препараты, дозировки, следующая дата
    - Смена владельца: предыдущий/новый владелец, причина передачи
    - Потеря/находка: место, обстоятельства, контакты
    - Смерть: причина (natural, euthanasia, accident, disease), подтверждение клиники
    - Приют: shelter_id, номер договора пристройства
    - Верификация событий: is_verified, verified_by, verified_at
    - 9 индексов для быстрого поиска (по питомцу, типу, дате, создателю)
    - Файл: `database/migrations/032_create_pet_events.sql`
  - **Go модель PetEvent**
    - Полная структура события с всеми полями
    - Константы для типов событий и причин смерти
    - Структура CreatePetEventRequest для создания событий
    - Дополнительные поля для отображения (имена создателей, владельцев)
    - Файл: `petbase/backend/models/pet_event.go`
  - **API endpoints для событий питомцев**
    - `GET /api/petid/:id/events` - получить историю событий питомца
      - Фильтр по типу события (query parameter `type`)
      - Сортировка по дате (DESC)
      - JOIN с users и organizations для получения имён
      - Публичный endpoint (без авторизации)
    - `POST /api/petid/:id/events` - создать новое событие
      - Требует авторизации
      - Валидация типа события
      - Автоматическая установка даты (если не указана)
      - Поддержка всех типов событий с соответствующими полями
    - `GET /api/petid/:id/medical` - получить медицинскую историю
      - Только медицинские события (vaccination, treatment, sterilization)
      - Информация о вакцинах, препаратах, дозировках
      - Дата следующей процедуры
      - Публичный endpoint
    - Файл: `petbase/backend/handlers/pet_events.go`
  - **Регистрация роутов в PetBase**
    - Функция `handlePetIDRoutes` для маршрутизации событий
    - Интеграция с middleware авторизации для POST запросов
    - Файл: `petbase/backend/main.go`

- **Документация по автотестам API**
  - Полное руководство по запуску и написанию тестов
  - Описание текущего покрытия (7 тестов Main API)
  - Шаблоны для добавления новых тестов
  - Best practices для тестирования
  - Инструкции по отладке
  - Файл: `tests/api/README.md`
  - Разделы:
    - Быстрый старт
    - Структура тестов
    - Покрытие тестами (Main, Admin, PetID)
    - Запуск тестов (все, конкретный, параллельный)
    - Добавление новых тестов (шаблон + вспомогательные функции)
    - Статистика (7/65+ тестов)
    - Отладка и Best Practices

## [0.10.6] - 2025-01-14 🛡️

### Added
- **Действия с постом для автора и участников организации в меню**
  - Кнопка "Редактировать" с иконкой карандаша (синяя)
  - Кнопка "Удалить" с иконкой корзины (красная)
  - Умная проверка прав на редактирование:
    - Для постов от пользователя: проверка `user.id === post.author_id`
    - Для постов от организации: проверка членства через API
    - Разрешено для ролей: owner, admin, moderator
    - Запрос к `/api/organizations/{id}/members` для проверки роли
  - Состояние `canEditPost` управляет отображением кнопок
  - Автоматическая проверка при монтировании компонента
  - Разделитель между действиями автора и "Пожаловаться"
  - Функционал:
    - Удаление: подтверждение через `confirm()`, DELETE запрос к API
    - Редактирование: заглушка с alert (TODO: реализовать позже)
  - Callback `onDelete` для обновления списка постов после удаления
  - Файл: `main/frontend/app/components/posts/PostCard.tsx`
  - API: `DELETE /api/posts/{id}`, `GET /api/organizations/{id}/members`

- **Система модерации и жалоб (Спринт 6.3)**
  - База данных:
    - Таблица `reports` для хранения жалоб на контент
    - Таблица `moderation_log` для логирования действий модераторов
    - Таблица `user_bans` для управления блокировками пользователей
    - 10 типов причин жалоб: spam, harassment, violence, hate_speech, misinformation, inappropriate, copyright, animal_abuse, fraud, other
    - 5 типов действий модератора: no_action, warning, content_removed, user_banned, user_suspended
    - Индексы для быстрого поиска по статусу, типу контента, модератору
    - Файл: `database/migrations/030_create_reports_table.sql`
  - Backend API для пользователей (Main):
    - `POST /api/reports` - создание жалобы на контент
    - Валидация: проверка дубликатов жалоб от одного пользователя
    - Автоматическое логирование создания жалобы в `user_logs`
    - Файл: `main/backend/handlers/reports.go`
    - Роут зарегистрирован в `main/backend/main.go`
  - Backend API для модераторов (Admin):
    - `GET /api/admin/moderation/reports?status=pending|resolved` - список жалоб
    - `POST /api/admin/moderation/reports/{id}` - рассмотрение жалобы
    - `GET /api/admin/moderation/stats` - статистика модерации
    - Файл: `admin/backend/handlers/moderation.go`
    - Роуты зарегистрированы в `admin/backend/main.go`
  - Frontend компонент для пользователей:
    - Компонент `ReportButton` - кнопка "Пожаловаться" с иконкой флага
    - Модальное окно с выбором причины жалобы (10 вариантов)
    - Поле для дополнительного описания (до 500 символов)
    - Анимация успешной отправки жалобы
    - Защита от повторных жалоб на один объект
    - Файл: `main/frontend/app/components/posts/ReportButton.tsx`
    - Интеграция в `PostCard.tsx` - кнопка в панели действий поста
  - Frontend страница модерации (Admin):
    - Страница `/moderation` для работы модераторов
    - Карточки статистики:
      - Ожидают рассмотрения (жёлтая)
      - Рассмотрено (зелёная)
      - Всего жалоб (красная)
    - Фильтр по статусу: pending / resolved
    - Список жалоб с информацией:
      - Причина жалобы (цветной badge)
      - Тип и ID объекта
      - Описание проблемы
      - Автор жалобы и дата
      - Решение модератора (для рассмотренных)
    - Модальное окно рассмотрения жалобы:
      - Выбор действия (5 вариантов)
      - Комментарий модератора
      - Сохранение решения
    - Файл: `admin/frontend/app/(dashboard)/moderation/page.tsx`
    - Вкладка "Модерация" добавлена в навигацию админ-панели
    - Файл: `admin/frontend/app/(dashboard)/layout.tsx`
  - Типы контента для жалоб: post, comment, user, organization, pet
  - Цветовая индикация причин жалоб
  - Автоматическое обновление списка после рассмотрения

- **Страница мониторинга работоспособности сервисов в админ-панели**
  - Вкладка "Мониторинг" (`/health`) для проверки всех микросервисов
  - Проверка 14 сервисов (7 backend + 7 frontend):
    - Main (localhost:3000/8000)
    - Admin (localhost:4000/9000)
    - PetBase (localhost:4100/8100)
    - Shelter (localhost:5100/8200)
    - Owner (localhost:6100/8400)
    - Volunteer (localhost:6200/8500)
    - Clinic (localhost:6300/8600)
  - Статусы сервисов:
    - 🟢 online - работает нормально (задержка <1000ms)
    - 🟡 slow - работает медленно (задержка >1000ms)
    - 🔴 offline - недоступен
  - Общий статус системы: healthy / degraded / down
  - Карточки статистики с градиентами (всего, работают, медленно, недоступны)
  - Отображение задержки (latency) для каждого сервиса
  - Автообновление каждые 10 секунд (можно отключить)
  - Кнопка ручного обновления
  - Файлы:
    - Backend: `admin/backend/handlers/health.go`
    - Frontend: `admin/frontend/app/(dashboard)/health/page.tsx`
    - Layout: `admin/frontend/app/(dashboard)/layout.tsx`
  - API endpoint: `GET /api/admin/health/services`
  - Timeout проверки: 5 секунд на сервис
- **Документация по оптимизации производительности**
  - Руководство `docs/OPTIMIZATION_GUIDE.md`
  - Описание N+1 проблемы и способов решения
  - Чеклисты оптимизации для Backend, Frontend, Database
  - Целевые метрики производительности
  - Примеры оптимизаций из проекта
  - Инструменты для мониторинга и профилирования
- **Миграция 029: Индексы для оптимизации производительности**
  - Добавлено 30+ критичных индексов для ускорения запросов
  - Индексы для таблиц: posts, comments, likes, post_pets, organizations, organization_members, notifications, user_roles, users, favorites, admin_logs, user_logs
  - Составные индексы для сложных запросов (multi-column indexes)
  - Partial индексы с WHERE clause для специфичных фильтров
  - Команда ANALYZE для обновления статистики оптимизатора
  - Ожидаемое ускорение запросов: 5-10x для ленты, 3-5x для комментариев
  - Файл: `database/migrations/029_add_performance_indexes.sql`
  - Всего индексов в БД: 128
- **Скрипт проверки использования индексов**
  - Утилита `scripts/check-indexes.sh` для анализа запросов
  - Использует EXPLAIN QUERY PLAN для проверки 10 критичных запросов
  - Показывает какие запросы используют индексы (✅) и какие делают полное сканирование (❌)
  - Результаты проверки: 9 из 10 запросов используют индексы
  - Помогает выявить узкие места в производительности
- **Система модерации и жалоб (Спринт 6.3 завершён!)**
  - Миграция 030: Таблицы для системы модерации
  - Таблица `reports` - жалобы на контент и пользователей
    - Типы контента: post, comment, user, organization, pet
    - Причины: spam, harassment, violence, hate_speech, misinformation, inappropriate, copyright, animal_abuse, fraud, other
    - Статусы: pending, reviewing, resolved, rejected
    - Действия модератора: no_action, warning, content_removed, user_banned, user_suspended
  - Таблица `moderation_log` - логирование всех действий модераторов
  - Таблица `user_bans` - блокировки пользователей (warning, temporary, permanent)
  - Backend API для модерации:
    - `GET /api/admin/moderation/reports?status=pending` - очередь жалоб
    - `POST /api/admin/moderation/reports/{id}` - рассмотреть жалобу
    - `GET /api/admin/moderation/stats` - статистика модерации
  - Индексы для быстрого поиска жалоб по статусу, типу, модератору
  - Файлы:
    - `database/migrations/030_create_reports_table.sql`
    - `admin/backend/handlers/moderation.go`
    - `admin/backend/main.go` (регистрация роутов)
- **Управление ролями пользователей в админ-панели**
  - Компонент `RolesManager` на странице настроек пользователя (`/users/{id}`)
  - Отображение всех назначенных ролей пользователя с цветовой индикацией:
    - user - серый (#6B7280)
    - volunteer - зелёный (#10B981)
    - shelter_admin - синий (#3B82F6)
    - clinic_admin - фиолетовый (#8B5CF6)
    - moderator - оранжевый (#F97316)
    - superadmin - красный (#EF4444)
  - Модальное окно добавления роли:
    - Выбор роли из доступных (исключая уже назначенные)
    - Поле для примечания о причине назначения
  - Кнопка отзыва роли (недоступна для роли 'user')
  - Отображение даты назначения роли и примечаний
  - Файл: `admin/frontend/app/(dashboard)/users/[id]/page.tsx`
  - API методы:
    - `GET /api/users/{id}/roles` - получение ролей пользователя
    - `GET /api/roles` - список всех доступных ролей
    - `POST /api/roles/grant` - назначение роли
    - `POST /api/roles/revoke` - отзыв роли
  - Интеграция с существующей системой ролей из `main/backend/handlers/roles.go`

### Changed
- **Улучшен UX кнопки "Пожаловаться" на постах**
  - Старое поведение: отдельная кнопка с иконкой флага в панели действий (рядом с лайками и комментариями)
  - Новое поведение: пункт "Пожаловаться" в выпадающем меню с тремя точками
  - Преимущества:
    - Более чистый интерфейс - меньше кнопок в панели действий
    - Логичное расположение - действия с постом в одном месте
    - Соответствует паттернам других соцсетей (Instagram, Facebook)
  - Изменения:
    - `ReportButton` теперь рендерит пункт меню вместо отдельной кнопки
    - Добавлен callback `onClose` для закрытия родительского меню
    - В `PostCard` добавлено выпадающее меню при клике на три точки
    - Меню содержит пункт "Пожаловаться" с иконкой флага
  - Файлы:
    - `main/frontend/app/components/posts/ReportButton.tsx`
    - `main/frontend/app/components/posts/PostCard.tsx`
- **Оптимизирована загрузка постов - устранена N+1 проблема**
  - Проблема: при загрузке 20 постов делалось 40+ запросов к БД (N+1 anti-pattern)
  - Старый подход: `loadPollsForPosts` и `loadPetsForPost` делали запрос для каждого поста в цикле
  - Новый подход: batch loading - один запрос для всех опросов, один для всех питомцев
  - Результат: вместо 40+ запросов теперь всего 3 запроса (посты + опросы + питомцы)
  - Ускорение загрузки ленты в **10-15 раз**
  - Файлы:
    - `main/backend/handlers/posts.go` - использование batch функций
    - `main/backend/handlers/posts_optimized.go` - новые функции `loadPollsForPostsBatch` и `loadPetsForPostsBatch`
  - Техника: использование `IN (?)` запросов с плейсхолдерами, группировка данных в map
- **Оптимизирована загрузка вкладки "Настройки" в админ-панели**
  - Роли пользователя теперь загружаются независимо от статистики хранилища
  - Убрана блокирующая загрузка - каждая секция загружается параллельно
  - Улучшен UX - пользователь видит роли сразу, не дожидаясь загрузки всей статистики
  - Файл: `admin/frontend/app/(dashboard)/users/[id]/page.tsx`
- **Удалена отдельная страница управления ролями `/roles`**
  - Управление ролями теперь доступно только в настройках конкретного пользователя
  - Более логичный UX - роли назначаются в контексте пользователя
  - Удалён файл: `admin/frontend/app/(dashboard)/roles/page.tsx`

### Fixed
- **Исправлена CORS политика для страницы модерации в админ-панели**
  - Проблема: запросы к `/api/admin/moderation/reports` и `/api/admin/moderation/stats` возвращали ошибку "Failed to fetch"
  - Причина: CORS middleware не устанавливал заголовок `Access-Control-Allow-Origin` для запросов без origin (например, из браузера)
  - Решение: добавлена проверка - если origin не указан, устанавливается `Access-Control-Allow-Origin: *`
  - Теперь страница `/moderation` корректно загружает данные о жалобах и статистику
  - Файл: `admin/backend/main.go`
  - Изменения в функции `enableCORS`:
    - Добавлена переменная `originAllowed` для отслеживания разрешённых origins
    - Если origin пустой (запрос без origin), разрешаем доступ
    - Сохранён `credentials: 'include'` для работы с cookies
- **Добавлено детальное логирование в страницу модерации**
  - Добавлены `console.log` для отладки статусов ответов и данных
  - Помогает быстро выявлять проблемы с API endpoints
  - Файл: `admin/frontend/app/(dashboard)/moderation/page.tsx`
- **Исправлена работа модального окна жалобы**
  - Проблема: модальное окно не открывалось из-за рендеринга внутри dropdown меню
  - Решение: модальное окно теперь рендерится на уровне PostCard, вне структуры меню
  - Проблема: ошибка "setIsOpen is not defined" при закрытии модального окна
  - Решение: заменены все вызовы `setIsOpen(false)` на `onClose()`
  - Изменения:
    - `ReportButton` теперь принимает `isOpen` и `onClose` как props
    - Состояние модального окна управляется в `PostCard`
    - Убрана кнопка из `ReportButton` - компонент рендерит только модальное окно
    - Кнопка "Пожаловаться" в меню открывает модальное окно через состояние
  - Файлы:
    - `main/frontend/app/components/posts/ReportButton.tsx`
    - `main/frontend/app/components/posts/PostCard.tsx`
- **Исправлена ошибка компиляции в main frontend**
  - Проблема: отсутствовал пакет `lucide-react` для иконок в компоненте ReportButton
  - Решение: установлен пакет `lucide-react` в `main/frontend`
  - Команда: `npm install lucide-react`
  - Файл: `main/frontend/package.json`
  - Теперь компонент ReportButton корректно компилируется и отображает иконки
- **Исправлен URL API в странице модерации админ-панели**
  - Проблема: страница `/moderation` обращалась к несуществующему порту `4001`
  - Решение: исправлены URL на правильный порт admin backend `9000`
  - Изменено: `http://localhost:4001` → `http://localhost:9000`
  - Файл: `admin/frontend/app/(dashboard)/moderation/page.tsx`
- **Исправлена ошибка компиляции в admin frontend**
  - Проблема: отсутствовал пакет `lucide-react` для иконок в странице модерации
  - Решение: установлен пакет `lucide-react` в `admin/frontend`
  - Команда: `npm install lucide-react`
  - Файл: `admin/frontend/package.json`
  - Теперь страница `/moderation` корректно компилируется и отображает иконки
- **Исправлена ошибка компиляции в posts_optimized.go**
  - Проблема: использовались несуществующие поля `IsMultiple`, `IsAnonymous`, `EndDate`
  - Решение: заменены на правильные названия полей из модели Poll: `MultipleChoice`, `AnonymousVoting`, `ExpiresAt`
  - Файл: `main/backend/handlers/posts_optimized.go`
- **Исправлена CORS политика для админ-панели**
  - Проблема: запросы с админ-панели (`http://localhost:4000`) блокировались CORS
  - Добавлен порт 4000 в список разрешённых origins
  - Теперь оба фронтенда (main на :3000 и admin на :4000) могут обращаться к backend API
  - Файл: `main/backend/main.go`
- **Исправлена долгая загрузка главной страницы**
  - Проблема: на главной странице `http://localhost:3000/` бесконечно крутились метки загрузки
  - Endpoint `GET /api/posts` был защищён `AuthMiddleware`, что блокировало загрузку ленты постов для неавторизованных пользователей
  - Решение:
    - Убрана авторизация с endpoint `GET /api/posts` - теперь лента постов доступна всем
    - Добавлена проверка авторизации внутри `PostsHandler` для метода POST (создание постов требует авторизации)
    - Изменён порядок регистрации роутов (публичный `/api/posts` регистрируется раньше защищённого `/api/posts/`)
  - Файлы: `main/backend/main.go`, `main/backend/handlers/posts.go`
  - Теперь главная страница с лентой постов доступна всем пользователям, включая неавторизованных
- **Исправлено отображение питомцев на странице профиля пользователя**
  - Проблема: питомцы не отображались на странице `/users/{id}` из-за защиты endpoint авторизацией
  - Endpoint `GET /api/pets/user/{id}` был защищён `AuthMiddleware`, что блокировало просмотр питомцев других пользователей
  - Решение: убрана авторизация с endpoint `/api/pets/user/{id}`, теперь он публичный
  - Изменён порядок регистрации роутов для корректной работы (публичный роут `/api/pets/user/` должен быть зарегистрирован раньше защищённого `/api/pets/`)
  - Файл: `main/backend/main.go`
  - Теперь любой пользователь может просматривать питомцев других пользователей на их страницах профиля
- **Исправлен endpoint загрузки питомцев в admin панели**
  - Проблема: ошибка "Failed to fetch" при загрузке питомцев на странице пользователя в admin панели
  - Использовался неправильный URL `http://localhost:8100/api/pets?user_id=${userId}` вместо `http://localhost:8000/api/pets/user/${userId}`
  - Решение: исправлен URL и добавлен `credentials: 'include'` для передачи cookies
  - Файл: `admin/frontend/app/(dashboard)/users/[id]/page.tsx`
- **Исправлено отображение постов пользователя в admin панели**
  - Проблема: посты не отображались на вкладке "Посты" страницы пользователя в admin панели
  - Использовался неправильный URL `http://localhost:8000/api/posts?user_id=${userId}` вместо `http://localhost:8000/api/posts/user/${userId}`
  - Endpoint был защищён `AuthMiddleware`, что блокировало публичный просмотр постов
  - Решение:
    - Исправлен URL в компоненте `PostsTab` на правильный `/api/posts/user/${userId}`
    - Убрана авторизация с публичных endpoints: `/api/posts/user/`, `/api/posts/pet/`, `/api/posts/organization/`
    - Изменён порядок регистрации роутов (публичные роуты регистрируются раньше защищённого `/api/posts/`)
  - Файлы: `admin/frontend/app/(dashboard)/users/[id]/page.tsx`, `main/backend/main.go`
  - Теперь посты пользователей, питомцев и организаций доступны для публичного просмотра
- **Исправлено отображение верификации пользователя на main сайте**
  - Проблема: после верификации пользователя в admin панели, галочка верификации не отображалась на main сайте
  - Endpoint `GET /api/users/{id}` был полностью защищён `AuthMiddleware`, что блокировало загрузку актуальных данных профиля
  - Дополнительная проблема: при просмотре своего профиля использовались закешированные данные из `AuthContext` вместо свежих данных с сервера
  - Решение:
    - Сделан `GET /api/users/{id}` публичным для просмотра профилей любых пользователей
    - Добавлена проверка авторизации внутри `UserHandler` для методов PUT и DELETE
    - PUT и DELETE доступны только для редактирования/удаления собственного профиля
    - Изменён порядок регистрации роутов (публичный `/api/users/` регистрируется раньше защищённого `/api/users`)
    - Убрана логика использования закешированных данных из `AuthContext` - теперь всегда загружаются свежие данные с сервера
  - Файлы: `main/backend/main.go`, `main/backend/handlers/users.go`, `main/frontend/app/(main)/[userId]/page.tsx`
  - Теперь профили пользователей доступны для публичного просмотра с актуальными данными верификации

### Changed
- **Оптимизирована загрузка ленты постов**
  - Добавлена пагинация с параметром `limit` (по умолчанию 20 постов, максимум 100)
  - Загрузка питомцев для постов теперь ограничена (только если прикреплено не более 5 питомцев)
  - Загрузка опросов стала опциональной через параметр `include_polls=true` (по умолчанию отключена)
  - Это решает проблему N+1 запросов к БД и ускоряет загрузку главной страницы
  - Файл: `main/backend/handlers/posts.go`
  - Примеры использования:
    - `GET /api/posts` - 20 постов без опросов
    - `GET /api/posts?limit=10` - 10 постов
    - `GET /api/posts?limit=50&include_polls=true` - 50 постов с опросами

### Added
- **Система логирования действий пользователей и детальная страница пользователя с вкладками**
  - **Таблица БД `user_logs`:**
    - Поля: id, user_id, action_type, action_details, ip_address, user_agent, created_at
    - Индексы по user_id, created_at, action_type для быстрого поиска
    - Типы действий: register, login, logout, profile_update, post_create, post_delete, comment_create, like, friend_request, message_send, pet_create, pet_update, organization_create, organization_join, error
    - Файл миграции: `database/migrations/031_create_user_logs.sql`
  - **API endpoints для логов и статистики пользователей:**
    - `GET /api/users/logs/{id}` - получение логов действий пользователя (последние 100 записей)
    - `GET /api/users/storage/{id}` - статистика использования хранилища:
      - posts_count - количество постов
      - pets_count - количество питомцев
      - media_count - количество медиа файлов
      - media_size_bytes/media_size_mb - размер медиа файлов
      - friends_count - количество друзей
      - organizations_count - количество организаций
      - comments_count - количество комментариев
    - Файл: `main/backend/handlers/user_logs.go`
  - **Функции handler:**
    - `CreateUserLog` - создание записи в логе пользователя
    - `GetUserLogsHandler` - получение логов с пагинацией
    - `GetUserStorageStatsHandler` - получение статистики использования хранилища
  - **Автоматическое логирование действий пользователей:**
    - **Регистрация и авторизация:**
      - `register` - при регистрации нового пользователя
      - `login` - при входе в систему
      - `logout` - при выходе из системы
      - Файл: `main/backend/handlers/auth.go`
    - **Профиль:**
      - `profile_update` - при обновлении данных профиля
      - Файл: `main/backend/handlers/profile.go`
    - **Посты:**
      - `post_create` - при создании поста (от пользователя или организации)
      - Файл: `main/backend/handlers/posts.go`
    - **Комментарии:**
      - `comment_create` - при создании комментария к посту
      - Файл: `main/backend/handlers/comments.go`
    - **Лайки:**
      - `like_add` - при добавлении лайка на пост
      - `like_remove` - при удалении лайка с поста
      - Файл: `main/backend/handlers/likes.go`
    - **Друзья:**
      - `friend_request_send` - при отправке запроса в друзья
      - `friend_request_accept` - при принятии запроса в друзья
      - Файл: `main/backend/handlers/friends.go`
    - Все логи сохраняют IP адрес и User-Agent для отслеживания подозрительной активности
    - Логирование происходит после успешного выполнения действия
  - **Детальная страница пользователя `/users/[id]` с 4 вкладками:**
    - **Вкладка "Логирование":**
      - История всех действий пользователя с момента регистрации
      - Отображение типа действия, деталей, IP адреса, даты и времени
      - Помогает отслеживать ошибки и подозрительную активность
      - Последние 50 записей
    - **Вкладка "Питомцы":**
      - Список всех питомцев пользователя
      - Отображение количества питомцев
      - Карточки с именем, видом и породой
      - Загрузка через API ЗооБазы (порт 8100)
    - **Вкладка "Посты":**
      - Список всех постов пользователя
      - Отображение количества постов
      - Содержание поста и дата создания
    - **Вкладка "Настройки":**
      - Кнопка верификации пользователя
      - Статистика использования хранилища:
        - Количество постов, питомцев, медиа файлов
        - Размер медиа файлов в MB
        - Количество друзей, организаций, комментариев
      - Помогает контролировать использование ресурсов
  - Файл: `admin/frontend/app/(dashboard)/users/[id]/page.tsx`
  - Роуты добавлены в `main/backend/main.go`
- **Система отслеживания активности пользователей и виджет "Пользователи онлайн"**
  - **Таблица БД `user_activity`:**
    - Поля: user_id (PRIMARY KEY), last_seen, ip_address, user_agent
    - Индекс по last_seen для быстрого поиска онлайн пользователей
    - Пользователь считается онлайн, если был активен в последние 5 минут
    - Файл миграции: `database/migrations/030_create_user_activity.sql`
  - **API endpoints для активности:**
    - `POST /api/activity/update` - обновление времени последней активности пользователя
    - `GET /api/activity/online` - получение количества пользователей онлайн
    - `GET /api/activity/stats` - детальная статистика активности:
      - online_now - онлайн сейчас (последние 5 минут)
      - active_last_hour - активны за последний час
      - active_last_24h - активны за последние 24 часа
      - total_users - всего пользователей
    - Файл: `main/backend/handlers/user_activity.go`
  - **Функции handler:**
    - `UpdateUserActivityHandler` - обновляет/создаёт запись активности с IP и User-Agent
    - `GetOnlineUsersCountHandler` - возвращает количество онлайн пользователей
    - `GetUserActivityStatsHandler` - возвращает полную статистику активности
    - `UpdateUserActivity` - вспомогательная функция для обновления активности
  - **Автоматическое отслеживание активности:**
    - Добавлено в `AuthMiddleware` автоматическое обновление активности при каждом авторизованном запросе
    - Обновление происходит асинхронно (через goroutine) для минимального влияния на производительность
    - Сохраняется IP адрес и User-Agent для аналитики
    - Файл: `main/backend/middleware/auth.go`
  - **Виджет "Пользователи онлайн" на дашборде админ-панели:**
    - Отображает количество пользователей онлайн с анимированным индикатором (пульсирующая зелёная точка)
    - Показывает "Активны сейчас (5 мин)" как подсказку
    - Зелёный цвет для выделения онлайн статуса
    - Дополнительная информация в других виджетах:
      - "За последний час" - в виджете "Всего пользователей"
      - "За 24 часа" - в виджете "Верифицированные"
    - Автоматическое обновление статистики при загрузке страницы
    - Файл: `admin/frontend/app/(dashboard)/dashboard/page.tsx`
  - Роуты добавлены в `main/backend/main.go`
- **Страница редактирования питомца `/pets/[id]/edit` с улучшенным дизайном**
  - **Новый дизайн в стиле редактирования профиля пользователя:**
    - Разделение на секции с боковым меню навигации
    - Индикаторы заполненности разделов (зелёные точки)
    - Подсказки и описания для каждого раздела
  - **Четыре раздела:**
    - "Основная информация" - фото, кличка, вид, порода, пол, дата рождения, окрас, размер
    - "Медицинская информация" - прививки, стерилизация, номер чипа с подробными описаниями
    - "Фотогалерея" - загрузка и управление дополнительными фото питомца
    - "Дополнительно" - статус питомца (дома, ищет дом, потерялся, найден, нужна помощь)
  - **Функционал фотогалереи:**
    - Загрузка нескольких фото питомца (сохраняются как base64 в JSON)
    - Сетка фото 2-3 колонки (адаптивная)
    - Удаление фото при наведении (кнопка с крестиком)
    - Превью загруженных фото
    - Валидация типа и размера файлов (макс 10MB)
    - Использует поле `photos` в БД (JSON массив base64 строк)
    - Автоматическое сохранение фото при отправке формы
    - Загрузка существующих фото из БД при открытии страницы
  - **Функционал загрузки основного фото:**
    - Превью фото питомца
    - Кнопки "Изменить фото" и "Удалить"
    - Валидация типа и размера файла (макс 10MB)
  - **Адаптивный дизайн:**
    - 2 колонки на десктопе (форма + боковое меню)
    - 1 колонка на мобильных устройствах
  - Валидация обязательных полей (кличка и вид)
  - Автозаполнение формы текущими данными питомца
  - Файл: `main/frontend/app/(main)/pets/[id]/edit/page.tsx`
  - Использует API ЗооБазы: `GET http://localhost:8100/api/pets/{id}`, `PUT http://localhost:8100/api/pets/{id}`
- **Кнопка "Редактировать" на странице питомца**
  - Отображается только для владельца питомца
  - Проверка прав доступа через сравнение `currentUserId === pet.user_id`
  - Переход на страницу редактирования `/pets/{id}/edit`
  - Файл: `main/frontend/app/(main)/pets/[id]/page.tsx`
- **Фотогалерея на странице просмотра питомца**
  - Автоматическое отображение дополнительных фото из поля `photos`
  - Сетка фото 2-3 колонки (адаптивная)
  - Парсинг JSON массива фото из БД
  - Hover эффект при наведении на фото
  - Отображается только если есть фото в галерее
  - Поддержка base64 изображений и URL путей
  - Файл: `main/frontend/app/(main)/pets/[id]/page.tsx`

### Technical
- **Текущая реализация фотогалереи питомцев:**
  - Фото сохраняются как base64 строки в JSON массиве в поле `photos` таблицы `pets`
  - Это временное решение для быстрого MVP
  - **TODO для будущих версий:**
    - Реализовать загрузку фото на сервер (в `uploads/pets/`)
    - Сохранять пути к файлам вместо base64
    - Добавить оптимизацию изображений (resize, compression)
    - Реализовать удаление файлов с сервера при удалении фото
  - Преимущества текущего подхода: простота, не требует файлового хранилища
  - Недостатки: большой размер JSON, медленная загрузка при многих фото

### Changed
- **Улучшен дизайн страницы просмотра питомца**
  - Убрана большая заглушка с фото питомца вверху страницы
  - Теперь сразу показывается фотогалерея с реальными фото
  - Основное фото питомца отображается в cover-секции и в аватаре
  - Более компактный и логичный layout
  - Файл: `main/frontend/app/(main)/pets/[id]/page.tsx`
- **Полный редизайн страницы редактирования питомца**
  - Старый дизайн: простая форма на одной странице
  - Новый дизайн: разделение на секции с боковым меню, как в редактировании профиля
  - Улучшен UX с подсказками и индикаторами заполненности
  - Добавлена возможность загрузки и удаления фото

### Fixed
- **Исправлена ошибка компиляции в main.go для роутов активности пользователей**
  - Проблема: использовалась несуществующая переменная `db` вместо `database.DB`
  - Ошибка компиляции: `undefined: db` на строках 161-163
  - Решение: заменено `db` на `database.DB` во всех роутах активности
  - Теперь backend компилируется и запускается корректно
  - Виджет "Пользователи онлайн" работает и показывает реальное количество активных пользователей
  - Файл: `main/backend/main.go`
- **Исправлено выделение активной вкладки в админ-панели**
  - Проблема: при переключении между вкладками активная вкладка не сразу выделялась
  - Требовалось два клика для полного выделения вкладки
  - Причина: использовался `window.location.pathname` вместо хука `usePathname()` из Next.js
  - `window.location.pathname` не обновляется автоматически при client-side навигации
  - Решение: заменён на `usePathname()` хук, который реактивно отслеживает изменения URL
  - Теперь активная вкладка корректно выделяется сразу при переходе
  - Файл: `admin/frontend/app/(dashboard)/layout.tsx`
- **Исправлено отображение логотипа в админ-панели**
  - Проблема: логотип не отображался на странице дашборда и других страницах админ-панели
  - Причина: файл `logo.svg` отсутствовал в папке `admin/frontend/public/`
  - Layout админ-панели ссылался на `/logo.svg`, но файл не существовал
  - Решение: скопирован `favicon.svg` в `admin/frontend/public/logo.svg`
  - Теперь логотип корректно отображается в header админ-панели
  - Файл: `admin/frontend/public/logo.svg`
- **Исправлено логирование действий администраторов**
  - Проблема: логи не создавались при действиях администраторов (верификация, назначение ролей)
  - Функция `CreateAdminLog` была определена, но нигде не вызывалась
  - Решение: добавлены вызовы `CreateAdminLog` во все критичные действия:
    - `VerifyUserHandler` - логирование верификации пользователя (action: verify_user)
    - `UnverifyUserHandler` - логирование снятия верификации (action: unverify_user)
    - `GrantRoleHandler` - логирование назначения роли (action: grant_role)
    - `RevokeRoleHandler` - логирование отзыва роли (action: revoke_role)
  - Логи содержат:
    - ID и email администратора
    - Тип действия и тип цели
    - ID и имя целевого пользователя
    - Детали действия (например, название роли)
    - IP адрес и User-Agent
    - Дата и время
  - Теперь все действия администраторов отображаются на странице `/logs` в админ-панели
  - Файлы: `main/backend/handlers/verification.go`, `main/backend/handlers/roles.go`
- **Исправлена возможность открывать карточки питомцев в каталоге**
  - Проблема: карточки питомцев в каталоге `/catalog` были некликабельными
  - Пользователи не могли перейти к просмотру питомца, хотя был `cursor-pointer`
  - Решение: добавлен обработчик `onClick` для перехода на `/pets/${pet.id}`
  - Теперь клик по любой части карточки открывает страницу питомца
  - Кнопки "Избранное", "Поделиться" и "Написать" используют `e.stopPropagation()` для предотвращения перехода
  - Файл: `main/frontend/app/(main)/catalog/page.tsx`
- **Исправлено отображение base64 фото в фотогалерее питомца**
  - Проблема: загруженные фото не отображались на странице просмотра питомца
  - Причина: код проверял только `photo.startsWith('http')`, но base64 изображения начинаются с `data:image/`
  - К base64 строкам ошибочно добавлялся префикс `http://localhost:8000`, что ломало изображение
  - Решение: добавлена проверка `photo.startsWith('data:')` для корректной обработки base64 изображений
  - Теперь поддерживаются три формата:
    - `data:image/...` - base64 изображения (текущая реализация)
    - `http://...` или `https://...` - полные URL
    - `/uploads/...` - относительные пути (для будущей реализации с файловым хранилищем)
  - Файл: `main/frontend/app/(main)/pets/[id]/page.tsx`
- **Исправлен роут перехода к питомцу в PetCard**
  - Проблема: при клике на карточку питомца в метках поста переход был на `/petid/{id}` вместо `/pets/{id}`
  - Это приводило к 404 ошибке
  - Решение: изменён роут с `/petid/${pet.id}` на `/pets/${pet.id}`
  - Файл: `main/frontend/app/components/posts/PetCard.tsx`
- **Добавлена возможность открывать питомцев на странице профиля**
  - Проблема: карточки питомцев в виджете "Питомцы" на странице профиля были некликабельными
  - Пользователи не могли перейти к просмотру питомца
  - Решение: 
    - Заменён `<div>` на `<button>` с обработчиком клика
    - Добавлен переход на `/pets/${pet.id}` при клике
    - Добавлены hover эффекты (ring, затемнение фото, показ имени)
    - Исправлено отображение фото через `getMediaUrl()`
  - Файл: `main/frontend/app/(main)/[userId]/page.tsx`
- **Исправлена проверка владельца питомца для кнопки "Редактировать"**
  - Проблема: кнопка "Редактировать" не отображалась на странице питомца даже для владельца
  - Причина: использовалось несуществующее поле `pet.owner_id` вместо `pet.user_id`
  - В таблице `pets` поле владельца называется `user_id`, а не `owner_id`
  - Решение: изменена проверка с `currentUserId === pet.owner_id` на `currentUserId === pet.user_id`
  - Файл: `main/frontend/app/(main)/pets/[id]/page.tsx`
- **Исправлен API endpoint на странице редактирования питомца**
  - Проблема: страница редактирования не могла загрузить данные питомца из API ЗооБазы
  - Ошибка "Unauthorized: authentication required" из-за отсутствия заголовка аутентификации
  - Решение:
    - Добавлена загрузка текущего пользователя через `/api/auth/me`
    - Добавлен заголовок `X-User-ID` во все запросы к API ЗооБазы (порт 8100)
    - Загрузка питомца происходит только после получения ID пользователя
    - Добавлена проверка `currentUserId` перед сохранением
  - Файл: `main/frontend/app/(main)/pets/[id]/edit/page.tsx`
  - API: `GET http://localhost:8100/api/pets/{id}`, `PUT http://localhost:8100/api/pets/{id}`
- **Исправлена обработка NULL значений в поле notes таблицы user_roles**
  - Проблема: SQL ошибка "converting NULL to string is unsupported" при загрузке ролей пользователя
  - Backend пытался сканировать NULL значение в обычный string
  - Решение: использован `sql.NullString` для безопасной обработки NULL значений
  - Файл: `main/backend/handlers/roles.go`

### Added
- **Система логирования действий администраторов**
  - **Таблица БД `admin_logs`:**
    - Поля: id, admin_id, admin_email, action_type, target_type, target_id, target_name, details, ip_address, user_agent, created_at
    - Типы действий: verify_user, unverify_user, grant_role, revoke_role, delete_post, delete_user, update_organization, delete_organization
    - Типы целей: user, post, role, organization
    - Индексы для быстрого поиска по admin_id, action_type, target_type, target_id, created_at
    - Файл миграции: `database/migrations/029_create_admin_logs.sql`
  - **Модель AdminLog:**
    - Структура лога с полями и константами
    - Константы для типов действий и целей
    - AdminLogResponse для API
    - Файл: `main/backend/models/admin_log.go`
  - **API endpoints для логов:**
    - `GET /api/admin/logs` - получить логи с фильтрацией
    - `GET /api/admin/logs/stats` - статистика по логам
    - Параметры фильтрации: action_type, target_type, limit
    - Файл: `main/backend/handlers/admin_logs.go`
  - **Функция `CreateAdminLog`:**
    - Создание записи в логе
    - Сохранение IP адреса и User-Agent
    - Используется во всех критичных действиях администраторов
  - **Функция `GetAdminLogStats`:**
    - Общее количество логов
    - Логи за последние 24 часа
    - Статистика по типам действий
    - Топ-5 администраторов по активности
  - Роуты добавлены в `main/backend/main.go`
- **Страница логирования в Admin панели `/logs` (полноценная реализация)**
  - **Статистика:**
    - Всего логов
    - Логи за последние 24 часа
    - Распределение по типам действий (топ-3)
    - Топ-3 администраторов по активности
  - **Фильтры:**
    - Поиск по администратору, цели или деталям
    - Фильтр по типу действия (верификация, роли, удаление и т.д.)
    - Фильтр по типу цели (пользователи, посты, роли, организации)
  - **Таблица логов:**
    - Дата и время действия
    - Email администратора
    - Тип действия с цветовой индикацией:
      - Удаление - красный
      - Верификация - зелёный
      - Роли - синий
      - Остальное - серый
    - Цель действия (имя и тип)
    - IP адрес
  - Лимит: 200 последних логов
  - Файл: `admin/frontend/app/(dashboard)/logs/page.tsx`
  - Использует API: `GET /api/admin/logs`, `GET /api/admin/logs/stats`
- **Страница управления постами в Admin панели `/posts`**
  - **Статистика:**
    - Всего постов
    - Всего лайков
    - Всего комментариев
  - **Поиск:**
    - Поиск по содержанию поста
    - Поиск по автору
  - **Список постов:**
    - Автор и дата публикации
    - Полное содержание поста
    - Счётчики лайков и комментариев
    - ID поста
  - **Действия:**
    - Кнопка "Просмотр" - открывает пост на главном сайте
    - Кнопка "Удалить" - удаление поста с подтверждением
  - Файл: `admin/frontend/app/(dashboard)/posts/page.tsx`
  - Использует API: `GET /api/posts`, `DELETE /api/posts/{id}`
- **Страница управления организациями в Admin панели `/organizations`**
  - **Статистика:**
    - Всего организаций
    - Приюты
    - Ветклиники
    - Фонды
  - **Фильтры:**
    - Поиск по названию или городу
    - Фильтр по типу: Все / Приюты / Клиники / Фонды
  - **Карточки организаций:**
    - Название и тип с цветовой индикацией
    - Город, регион
    - Телефон и email
    - Описание (первые 3 строки)
    - Дата создания
  - **Действия:**
    - Кнопка "Просмотр" - открывает страницу организации на главном сайте
    - Кнопка "Редактировать" - открывает страницу редактирования
  - Сетка: 1 колонка (mobile), 2 колонки (tablet), 3 колонки (desktop)
  - Файл: `admin/frontend/app/(dashboard)/organizations/page.tsx`
  - Использует API: `GET /api/organizations/all`
- **Страница детального просмотра организации в Admin панели `/organizations/[id]`**
  - **Информация об организации:**
    - Название, тип, ID
    - Адрес (город, регион, полный адрес)
    - Контакты (телефон, email, сайт)
    - Дата создания
    - Полное описание
  - **Статистика:**
    - Количество участников команды
    - Количество питомцев
    - Количество постов
  - **Список участников команды:**
    - Аватар, имя, ID пользователя
    - Роль с цветовой индикацией (owner, admin, moderator, member)
    - Должность
    - Дата присоединения
    - Сетка: 1 колонка (mobile), 2 колонки (tablet), 3 колонки (desktop)
  - **Действия:**
    - Кнопка "Назад к списку организаций"
    - Кнопка "Открыть на сайте" - открывает страницу организации на главном сайте
    - Кнопка "Редактировать" - открывает страницу редактирования
  - Файл: `admin/frontend/app/(dashboard)/organizations/[id]/page.tsx`
  - Использует API: `GET /api/organizations/{id}`, `GET /api/organizations/{id}/members`, `GET /api/pets?organization_id={id}`, `GET /api/posts?organization_id={id}`
- **Страница детального просмотра пользователя в Admin панели `/users/[id]`**
  - **Информация о пользователе:**
    - Аватар, имя, фамилия, ID
    - Email, местоположение, телефон
    - Биография
    - Дата регистрации
    - Статус верификации с датой
  - **Статистика активности:**
    - Количество постов
    - Количество полученных лайков
    - Количество комментариев
    - Количество друзей
  - **История активности:**
    - Последние 10 постов пользователя
    - Для каждого поста: содержание, дата, лайки, комментарии
  - **Действия:**
    - Кнопка "Назад к списку пользователей"
    - Кнопка "Открыть профиль" - открывает профиль на главном сайте
    - Кнопка "Верифицировать" / "Снять верификацию"
  - Файл: `admin/frontend/app/(dashboard)/users/[id]/page.tsx`
  - Использует API: `GET /api/users/{id}`, `GET /api/posts?user_id={id}`, `GET /api/friends?user_id={id}`
- **Система ролей пользователей**
  - **Таблица БД `user_roles`:**
    - Поля: id, user_id, role, granted_by, granted_at, expires_at, is_active, notes
    - Роли: user, volunteer, shelter_admin, clinic_admin, moderator, superadmin
    - Уникальность: один пользователь не может иметь дублирующиеся активные роли
    - Индексы для быстрого поиска по user_id, role, is_active
    - Автоматическое назначение роли 'user' всем существующим пользователям
    - Файл миграции: `database/migrations/027_create_user_roles.sql`
  - **Модель UserRole:**
    - Структура роли с полями и связями
    - Константы для всех типов ролей
    - Функция `IsValidRole` для валидации
    - Карта прав доступа `RolePermissions` для каждой роли
    - Функция `HasPermission` для проверки прав
    - Файл: `main/backend/models/role.go`
  - **API endpoints для управления ролями:**
    - `GET /api/roles/available` - список всех доступных ролей с описаниями
    - `GET /api/roles/user/{id}` - получить роли конкретного пользователя
    - `POST /api/roles/grant` - назначить роль пользователю
    - `POST /api/roles/revoke` - отозвать роль у пользователя
    - Файл: `main/backend/handlers/roles.go`
  - **Функция `GrantRoleHandler`:**
    - Проверка прав (только superadmin и moderator)
    - Валидация user_id и role
    - Проверка существования пользователя
    - Проверка на дублирование роли
    - Поддержка expires_at для временных ролей
    - Поддержка notes для комментариев
  - **Функция `RevokeRoleHandler`:**
    - Проверка прав (только superadmin и moderator)
    - Деактивация роли (is_active = 0)
    - Защита: нельзя отозвать роль 'user'
  - **Функция `GetUserRolesHandler`:**
    - Загрузка всех активных ролей пользователя
    - Информация о том, кто назначил роль
    - Сортировка по дате назначения
  - **Middleware для проверки ролей:**
    - `RoleMiddleware` - проверка конкретной роли
    - `AnyRoleMiddleware` - проверка наличия хотя бы одной из ролей
    - `EnrichWithRoles` - добавление ролей в контекст запроса
    - `HasPermission` - проверка конкретного права доступа
    - Файл: `main/backend/middleware/role_middleware.go`
  - **Страница управления ролями в Admin панели `/roles`:**
    - Список всех пользователей с поиском
    - Отображение активных ролей выбранного пользователя
    - Модальное окно назначения роли:
      - Выбор роли из доступных
      - Описание роли
      - Поле для примечаний
    - Кнопка отзыва роли для каждой роли (кроме 'user')
    - Цветовая индикация ролей:
      - user - серый
      - volunteer - зелёный
      - shelter_admin - синий
      - clinic_admin - фиолетовый
      - moderator - жёлтый
      - superadmin - красный
    - Файл: `admin/frontend/app/(dashboard)/roles/page.tsx`
  - **Dashboard Layout для Admin панели:**
    - Единый layout с навигацией
    - Вкладка "Роли" с иконкой щита
    - Проверка авторизации
    - Обработка logout
    - Файл: `admin/frontend/app/(dashboard)/layout.tsx`
  - Роуты добавлены в `main/backend/main.go`
- **Система верификации пользователей**
  - **Таблица БД - добавлены поля в `users`:**
    - `verified` (BOOLEAN) - статус верификации
    - `verified_at` (DATETIME) - дата верификации
    - `verified_by` (INTEGER) - ID модератора, который верифицировал
    - Индекс для быстрого поиска верифицированных пользователей
    - Файл миграции: `database/migrations/028_add_user_verification.sql`
  - **Обновлена модель User:**
    - Добавлены поля: Verified, VerifiedAt, VerifiedBy
    - Обновлен UserResponse для API
    - Файл: `main/backend/models/user.go`
  - **API endpoints для верификации:**
    - `POST /api/verification/verify` - верифицировать пользователя (только moderator/superadmin)
    - `POST /api/verification/unverify` - снять верификацию
    - `GET /api/verification/status/{id}` - получить статус верификации
    - `GET /api/users/verified?verified=true` - список верифицированных пользователей
    - Файл: `main/backend/handlers/verification.go`
  - **Функция `VerifyUserHandler`:**
    - Проверка прав модератора через `hasModeratorRights`
    - Валидация user_id
    - Проверка, не верифицирован ли уже
    - Установка verified=1, verified_at, verified_by
    - Логирование действия
  - **Функция `UnverifyUserHandler`:**
    - Снятие верификации (обнуление полей)
    - Проверка прав модератора
  - **Функция `GetVerifiedUsersHandler`:**
    - Фильтрация по параметру ?verified=true
    - Возврат списка с полной информацией
  - **Страница управления пользователями в Admin панели `/users`:**
    - Список всех пользователей с аватарами
    - **Статистика:**
      - Всего пользователей
      - Верифицированные (зелёный badge)
      - Не верифицированные (серый badge)
    - **Фильтры:**
      - Поиск по имени/email
      - Все / Верифицированные / Не верифицированные
    - **Таблица пользователей:**
      - Аватар и имя с ID
      - Email
      - Дата регистрации
      - Статус верификации (badge)
      - Кнопки действий: 
        - "Профиль" - открывает профиль пользователя на главном сайте в новой вкладке
        - "Верифицировать" / "Снять верификацию"
    - Файл: `admin/frontend/app/(dashboard)/users/page.tsx`
  - **Бейдж "✓ Проверенный" на профиле пользователя:**
    - Синяя галочка (CheckBadgeIcon) рядом с именем
    - Отображается только для верифицированных пользователей
    - Tooltip "Проверенный пользователь"
    - Файл: `main/frontend/app/(main)/[userId]/page.tsx`
  - Роуты добавлены в `main/backend/main.go`

### Changed
- **Улучшена навигация на странице списка организаций `/organizations`**
  - Клик по карточке организации теперь открывает детальную страницу `/organizations/[id]`
  - Добавлен cursor-pointer для визуальной индикации кликабельности
  - Файл: `admin/frontend/app/(dashboard)/organizations/page.tsx`
- **Исправлена навигация в Dashboard Layout**
  - Проблема: вкладки "Посты" и "Логирование" вели на `/dashboard` вместо своих страниц
  - Решение: обновлены роуты для всех вкладок
  - Добавлено определение активной вкладки для `/posts` и `/logs`
  - Теперь все вкладки работают корректно
  - Файл: `admin/frontend/app/(dashboard)/layout.tsx`
- **Улучшена навигация на странице списка пользователей `/users`**
  - Клик по строке пользователя теперь открывает детальную страницу `/users/[id]`
  - Добавлен cursor-pointer для визуальной индикации кликабельности
  - Кнопка "Профиль" заменена на клик по всей строке для удобства
  - Файл: `admin/frontend/app/(dashboard)/users/page.tsx`

### Fixed
- **Исправлена навигация в Admin панели**
  - Проблема: пользователь не хотел редирект на `/users`, хотел видеть дашборд со статистикой
  - Решение: создана страница дашборда `/dashboard` с карточками статистики
  - Главная страница админки теперь редиректит на `/dashboard`
  - Файл: `admin/frontend/app/page.tsx` (изменён редирект с `/users` на `/dashboard`)
  - Файл: `admin/frontend/app/(dashboard)/dashboard/page.tsx` (новая страница дашборда)
- **Исправлена навигация в Dashboard Layout**
  - Добавлена вкладка "Дашборд" в начало списка вкладок
  - Обновлены роуты: dashboard → `/dashboard`, users → `/users`
  - Исправлено определение активной вкладки по URL pathname
  - По умолчанию активна вкладка "dashboard"
  - Файл: `admin/frontend/app/(dashboard)/layout.tsx`
- **Исправлена ошибка "Cannot read properties of undefined (reading 'charAt')" в AdminLayout**
  - Проблема: `adminUser` был undefined при первом рендере, вызывая ошибку при попытке вызвать `.charAt(0)`
  - Решение: добавлена optional chaining `adminUser?.email?.charAt(0)` с fallback значениями
  - Теперь используется 'A' как fallback для аватара, 'Admin' для email, 'admin' для роли
  - Файл: `admin/frontend/app/components/admin/AdminLayout.tsx`
- **Исправлена ошибка загрузки ролей пользователя на странице управления ролями**
  - Проблема: API handlers возвращали просто массив, а не обёрнутый в `{success: true, data: [...]}`
  - Решение: обновлены `GetUserRolesHandler` и `GetAvailableRolesHandler` для возврата консистентного формата
  - Теперь все API endpoints возвращают единообразный формат ответа
  - Файл: `main/backend/handlers/roles.go`
- **Исправлена ошибка "users.map is not a function" на странице управления ролями**
  - Проблема: API возвращает `{success: true, data: [...]}`, а frontend ожидал просто массив
  - Решение: добавлена обработка структуры ответа `result.data || result` с fallback
  - Добавлена проверка `Array.isArray()` для всех загружаемых данных (users, roles, userRoles)
  - Файл: `admin/frontend/app/(dashboard)/roles/page.tsx`
- **Исправлена ошибка "membersData is not defined" на странице детального просмотра организации**
  - Проблема: переменная `membersData` использовалась вне области видимости
  - Решение: объявлена переменная в начале функции и используется для подсчёта статистики
  - Файл: `admin/frontend/app/(dashboard)/organizations/[id]/page.tsx`
- **Исправлена ошибка "postsData is not defined" на странице детального просмотра пользователя**
  - Проблема: переменная `postsData` использовалась вне области видимости
  - Решение: объявлена переменная в начале функции и используется для подсчёта статистики
  - Файл: `admin/frontend/app/(dashboard)/users/[id]/page.tsx`
- **Исправлена ошибка "users.filter is not a function" на страницах админки**
  - Проблема: API `/api/users` возвращает `{success: true, data: [...]}`, а frontend ожидал просто массив
  - Решение: добавлена обработка структуры ответа `result.data || result` с fallback
  - Добавлена проверка `Array.isArray()` с fallback на пустой массив
  - Теперь пользователи корректно отображаются на страницах дашборда и управления пользователями
  - Файлы: `admin/frontend/app/(dashboard)/dashboard/page.tsx`, `admin/frontend/app/(dashboard)/users/page.tsx`
  - Файл: `admin/frontend/app/page.tsx`
- **Исправлено определение активной вкладки в Dashboard Layout**
  - Добавлено определение активной вкладки по URL pathname
  - Теперь правильно подсвечивается текущая вкладка
  - Файл: `admin/frontend/app/(dashboard)/layout.tsx`

### Added
- **Отправка фото и видео в мессенджере**
  - **Backend API endpoint `POST /api/messages/send-media`:**
    - Multipart form upload для медиа файлов
    - Поддержка множественной загрузки (до 5 файлов)
    - Автоматическое определение типа файла (image/video)
    - Сохранение файлов в `uploads/messages/`
    - Создание записей в таблице `message_attachments`
    - Возврат сообщения с attachments
    - Файл: `main/backend/handlers/messenger.go`
  - **Функция `SendMediaMessageHandler`:**
    - Парсинг multipart form (макс 50MB)
    - Валидация receiver_id
    - Создание/поиск чата
    - Сохранение сообщения с текстом (опционально)
    - Обработка каждого файла через `saveUploadedFile`
    - Создание attachment записей в БД
    - Обновление last_message в чате
  - **Функция `saveUploadedFile`:**
    - Генерация уникального имени файла (UUID)
    - Создание директории `uploads/messages/`
    - Сохранение файла на диск
    - Возврат относительного пути для URL
  - **Функция `getMessageAttachments`:**
    - Загрузка всех attachments для сообщения
    - JOIN с таблицей `message_attachments`
    - Сортировка по дате создания
  - **Обновлена функция `getMessageByID`:**
    - Теперь загружает attachments для каждого сообщения
    - Использует `getMessageAttachments`
  - **Обновлена функция `GetChatMessagesHandler`:**
    - Загружает attachments для всех сообщений в чате
  - **UI для отправки медиа:**
    - Кнопка "Прикрепить" с иконкой скрепки
    - Input для выбора файлов (image/*, video/*)
    - Множественный выбор файлов (до 5 штук)
    - Preview выбранных изображений (миниатюры 20x20)
    - Кнопка удаления файла из preview (красный крестик)
    - Отправка через FormData при наличии файлов
    - Файл: `main/frontend/app/(main)/messenger/page.tsx`
  - **Отображение медиа в сообщениях:**
    - Изображения: кликабельные, открываются в новой вкладке
    - Видео: встроенный плеер с controls
    - Rounded углы для медиа
    - Отображение текста + медиа в одном сообщении
    - Адаптивная ширина (max-w-full)
  - **State управление:**
    - `selectedFiles: File[]` - выбранные файлы
    - `previewUrls: string[]` - URL для preview
    - `fileInputRef` - ref для скрытого input
  - **Функции:**
    - `handleFileSelect` - обработка выбора файлов, валидация типов
    - `removeFile` - удаление файла из списка
  - Роут: `POST /api/messages/send-media` в `main/backend/main.go`
  - Таблица БД: `message_attachments` (уже существовала в модели)

### Added
- **Страница уведомлений `/notifications`**
  - Полноценная страница с красивым UI и градиентным header
  - Header с иконкой колокольчика и счетчиком непрочитанных
  - Кнопка "Прочитать все" (отображается если есть непрочитанные)
  - **Фильтры уведомлений:**
    - Все (показывает общее количество)
    - Непрочитанные (показывает количество непрочитанных)
    - Лайки (❤️ эмодзи)
    - Комментарии (💬 эмодзи)
    - Друзья (👥 эмодзи)
  - **Пагинация:**
    - 20 уведомлений на страницу
    - Кнопки "Назад" / "Вперёд"
    - Нумерация страниц с активной страницей
    - Счетчик "Показано X-Y из Z"
  - **Отображение уведомлений:**
    - Иконки по типу (сердце, комментарий, друзья)
    - Форматирование времени (только что, X мин/ч/дн назад, дата)
    - Синий фон для непрочитанных
    - Синяя точка для отметки как прочитанное
  - **Интерактивность:**
    - Клик по уведомлению → переход на соответствующую страницу
    - Клик по синей точке → отметить как прочитанное
    - Автоматическая отметка при клике на уведомление
  - **Пустое состояние:**
    - Иконка колокольчика
    - Текст "Нет уведомлений" или "Нет уведомлений в этой категории"
  - Адаптивный дизайн
  - Файл: `main/frontend/app/(main)/notifications/page.tsx`
  - Использует API из `notificationsApi`: `getAll()`, `markAsRead()`, `markAllAsRead()`
- **Кнопка "Показать все уведомления" в NotificationsDropdown**
  - Добавлен footer в dropdown с кнопкой
  - Клик → переход на `/notifications`
  - Отображается только если есть уведомления
  - Dropdown теперь показывает только первые 5 уведомлений
  - Файл: `main/frontend/app/components/layout/NotificationsDropdown.tsx`

### Added
- **Интеграция PetID в постах (Спринт 5.1)**
  - **Компонент PetCard для отображения питомцев в постах**
    - Красивая карточка с градиентным фоном (синий-голубой)
    - Фото питомца (или первая буква имени)
    - Полная информация: имя, порода, возраст, пол, окрас, размер
    - Статус питомца с цветовыми индикаторами:
      * Ищет дом - оранжевый
      * Потерялся - красный
      * Найден - зелёный
      * Нужна помощь - фиолетовый
      * Дома - синий
    - Badge "SOS" для срочных случаев (urgent)
    - Местоположение (город/регион) с иконкой
    - Badge организации (если питомец из приюта/клиники)
    - Превью истории питомца (первые 2 строки)
    - Hover эффекты: увеличение фото, изменение цвета границы
    - Анимация стрелки при наведении
    - Файл: `main/frontend/app/components/posts/PetCard.tsx`
  - **Клик по карточке → переход в PetID**
    - При клике на карточку питомца открывается страница `/petid/{id}`
    - Полная интеграция с PetID микросервисом
  - **Синхронизация данных с PetID**
    - Backend загружает полную информацию о питомцах из таблицы `pets`
    - JOIN с таблицей `organizations` для получения названия организации
    - Обновлена функция `loadPetsForPost` в `main/backend/handlers/posts.go`
    - Загружаются поля: id, name, species, breed, gender, birth_date, color, size, photo, status, city, region, urgent, story, organization_id, organization_name, organization_type
    - Обновлена модель `Pet` в `main/backend/models/pet.go` (добавлены все необходимые поля)
  - **Обновлен PostCard**
    - Заменена старая простая карточка питомца на новый компонент PetCard
    - Обновлен интерфейс Pet с полным набором полей
    - Импортирован компонент PetCard
    - Файл: `main/frontend/app/components/posts/PostCard.tsx`

### Changed
- **Обновлена документация MVP.md - добавлен детальный план спринтов**
  - Разделены оставшиеся 132 задачи на 8 этапов (ЭТАП 5-12)
  - Каждый этап разбит на спринты по 1-4 дня
  - Добавлены цели и результаты для каждого этапа
  - Общий срок до запуска MVP: 15 недель (~3.5 месяца)
  - Этапы:
    * ЭТАП 5 - Доработка Main (2 недели, 6 задач)
    * ЭТАП 6 - Система ролей и модерация (1.5 недели, 14 задач)
    * ЭТАП 7 - PetID: История событий (1.5 недели, 16 задач)
    * ЭТАП 8 - Owner: Кабинет владельца (2 недели, 27 задач)
    * ЭТАП 9 - Volunteer: Кабинет зоопомощника (2 недели, 30 задач)
    * ЭТАП 10 - Shelter: Кабинет приюта (1.5 недели, 21 задача)
    * ЭТАП 11 - Clinic: Кабинет ветклиники (2 недели, 38 задач)
    * ЭТАП 12 - Тестирование и запуск (1 неделя)
  - Файл: `docs/MVP.md`
- **Обновлена документация MVP.md**
  - Отмечены как выполненные задачи "Репосты/Поделиться" (раздел 1.1. Лента)
  - Функционал был реализован в v0.10.5, но не был отмечен в MVP
  - Обновлен прогресс: 199 → 202 задачи (60% → 60.5%)
  - Файл: `docs/MVP.md`

### Added
- **Приоритет постов друзей в ленте**
  - Посты от друзей теперь показываются в начале ленты
  - Сортировка: сначала посты друзей (по дате), затем остальные посты (по дате)
  - SQL запрос проверяет статус дружбы через таблицу `friendships`
  - Работает для авторизованных пользователей (для неавторизованных - обычная сортировка по дате)
  - Файл: `main/backend/handlers/posts.go` (функция `getAllPosts`)
  - Логика: `ORDER BY is_friend DESC, p.created_at DESC`

## [0.10.5] 👥❤️ - 2025-01-13

### Added
- **Система лайков постов (Frontend)**
  - Двойной клик на контент поста для быстрого лайка
  - Анимация сердечка при лайке (большое красное сердце с эффектом ping)
  - Кнопка лайка с визуальной индикацией:
    - Пустое сердце (outline) - не лайкнуто
    - Заполненное красное сердце (filled) - лайкнуто
  - Счётчик лайков с возможностью просмотра списка лайкнувших
  - Автоматическое обновление статуса лайка при загрузке поста
  - Анимация только при лайке (не при анлайке)
  - Файл: `main/frontend/app/components/posts/PostCard.tsx`
  - Использует API: `POST /api/posts/{id}/like` (toggle лайка)
  - Использует API: `GET /api/posts/{id}/like` (статус лайка)
  - Backend уже реализован в `main/backend/handlers/likes.go`
- **Система "Поделиться" постами**
  - Кнопка "Поделиться" с выпадающим меню
  - Варианты шаринга:
    - Копировать ссылку (с уведомлением "Ссылка скопирована!")
    - ВКонтакте (открывает vk.com/share.php)
    - Telegram (открывает t.me/share/url)
    - WhatsApp (открывает wa.me)
  - Формат ссылки: `{origin}/?metka={post_id}`
  - Текст для шаринга: первые 100 символов контента поста
  - Меню закрывается автоматически после выбора действия
  - Файл: `main/frontend/app/components/posts/PostCard.tsx`
- **Система друзей (Frontend)**
  - **Компонент FriendButton на странице профиля**
    - Кнопка "Добавить в друзья" для чужих профилей
    - Кнопка "Принять" / "Отклонить" для входящих запросов
    - Кнопка "Удалить из друзей" для существующих друзей
    - Статус "Запрос отправлен" для исходящих запросов
    - Автоматическое обновление статуса после действия
    - Не отображается на своём профиле
    - Файл: `main/frontend/app/components/profile/FriendButton.tsx`
    - Использует API: `POST /api/friends/send`, `POST /api/friends/accept`, `POST /api/friends/reject`, `DELETE /api/friends/remove`
    - Интеграция с `friendsApi.ts`
  - **Страница списка друзей `/friends`**
    - Отображение всех друзей пользователя с аватарами
    - Поиск по имени друга (live search)
    - Счётчик друзей с учётом фильтрации
    - Кнопка "Запросы в друзья" для перехода к входящим запросам
    - Сетка друзей: 1 колонка (mobile), 2 колонки (tablet), 3 колонки (desktop)
    - Клик по карточке друга → переход на профиль
    - Пустое состояние: "У вас пока нет друзей"
    - Файл: `main/frontend/app/(main)/friends/page.tsx`
    - Компонент: `main/frontend/app/components/profile/FriendsList.tsx`
  - **Страница входящих запросов `/friends/requests`**
    - Список всех входящих запросов в друзья
    - Информация о каждом запросе:
      - Аватар, имя, местоположение
      - Дата отправки запроса
    - Действия для каждого запроса:
      - Кнопка "Принять" (синяя)
      - Кнопка "Отклонить" (серая)
    - Клик по аватару/имени → переход на профиль отправителя
    - Кнопка "Назад" для возврата к списку друзей
    - Пустое состояние: "Нет входящих запросов в друзья"
    - Файл: `main/frontend/app/(main)/friends/requests/page.tsx`
  - **Dropdown запросов в друзья в Header**
    - Иконка UserGroupIcon с красным badge счётчика
    - Badge показывает количество входящих запросов (до 9+)
    - Выпадающее меню с запросами:
      - Заголовок "Запросы в друзья" с кнопкой "Все друзья"
      - Список до 5 последних запросов
      - Для каждого запроса: аватар, имя, дата, кнопки "Принять"/"Отклонить"
      - Кнопка "Показать все" если запросов больше 5
    - Клик вне меню → закрытие
    - Автоматическое обновление каждые 30 секунд
    - Файл: `main/frontend/app/components/layout/FriendsDropdown.tsx`
    - Хук: `main/frontend/app/hooks/useFriendRequests.ts`
  - **Виджет друзей на странице профиля**
    - Отображение до 6 друзей в правой колонке профиля
    - Сетка 3x2 с аватарами и именами
    - Счётчик друзей в заголовке
    - Кнопка "Все" для перехода к полному списку
    - Клик по другу → переход на его профиль
    - Пустое состояние: "Нет друзей"
    - Файл: `main/frontend/app/components/profile/FriendsListWidget.tsx`
    - Интеграция в: `main/frontend/app/(main)/[userId]/page.tsx`
  - **API клиент для друзей**
    - `getFriends()` - GET /api/friends (список друзей)
    - `getRequests()` - GET /api/friends/requests (входящие запросы)
    - `sendRequest(friendId)` - POST /api/friends/send (отправить запрос)
    - `acceptRequest(friendshipId)` - POST /api/friends/accept (принять запрос)
    - `rejectRequest(friendshipId)` - POST /api/friends/reject (отклонить запрос)
    - `removeFriend(friendshipId)` - DELETE /api/friends/remove (удалить из друзей)
    - `getStatus(friendId)` - GET /api/friends/status?friend_id={id} (статус дружбы)
    - Файл: `main/frontend/lib/api.ts`
    - Backend уже реализован в `main/backend/handlers/friends.go`
- **Расширенный список сервисов в разработке на странице `/services`**
  - Добавлен сервис "Зоомаркет" (маркетплейс товаров и услуг)
    - Каталог товаров для животных
    - Услуги для питомцев
    - Отзывы и рейтинги
    - Интеграция с доставкой
  - Добавлен сервис "Афиша" (события и мероприятия)
    - Календарь мероприятий
    - Выставки и конкурсы
    - Регистрация на события
    - Фотоотчёты
  - Добавлен сервис "Учебный центр" (обучение владельцев)
    - Онлайн-курсы
    - Видеоуроки
    - Сертификация
    - База знаний
  - **Добавлен сервис "Кабинет муниципалитета" (государственное управление)**
    - Управление программой ОСВВ
    - Контроль работы приютов
    - Статистика по городу
    - Отчётность в гос. органы
    - Бюджет на зоозащиту
  - **Добавлен сервис "Управление ветеринарии" (надзор и контроль)**
    - Контроль ветклиник
    - Эпидемиологический надзор
    - Лицензирование клиник
    - Статистика вакцинации
    - Контроль бешенства
  - **Добавлен сервис "Петиции" (общественные инициативы)**
    - Создание петиций
    - Сбор подписей
    - Голосование
    - Отслеживание статуса
    - Интеграция с властями
  - **Добавлен сервис "Горячая линия" (круглосуточная поддержка)**
    - Круглосуточная поддержка
    - Консультации ветеринаров
    - Помощь в экстренных случаях
    - База знаний
    - История обращений
  - **Добавлен сервис "Чат-бот помощник" (AI-ассистент)**
    - Ответы на вопросы 24/7
    - Рекомендации по уходу
    - Юридическая зоопомощь
    - Поиск ветклиник
    - Помощь в пристройстве
  - **Добавлен сервис "Кабинет заводчика/питомника" (племенное разведение)**
    - Управление племенным разведением
    - Родословные и документы
    - Продажа щенков/котят
    - Репутация и отзывы
    - Интеграция с РКФ/WCF
  - **Добавлен сервис "Кабинет зоогостиницы" (передержка)**
    - Бронирование мест
    - Календарь занятости
    - Условия содержания
    - Фото/видео отчёты владельцам
    - Медицинский контроль
  - **Добавлен сервис "Кабинет специалиста" (кинологи, грумеры, психологи)**
    - Онлайн-консультации
    - Программы дрессировки
    - Груминг-услуги
    - Коррекция поведения
    - История работы с питомцем
  - **Добавлен сервис "Ритуальные услуги" (контроль смертности)**
    - Кремация
    - Захоронение
    - Памятные услуги
    - Поддержка владельцев
    - Контроль смертности
  - **Добавлен сервис "Кабинет зоозащитной инспекции" (борьба с жестокостью)**
    - Приём жалоб на жестокое обращение
    - Координация с полицией
    - База нарушителей
    - Статистика по городу
    - Работа с судами
  - **Всего в разработке: 15 сервисов**
  - **Обновлена статистика: 6 активных, 15 в разработке, 21 всего планируется**
  - Добавлены новые иконки: BuildingLibraryIcon, DocumentTextIcon, PhoneIcon, ChatBubbleLeftRightIcon, SparklesIcon, HomeModernIcon, ScaleIcon
  - Файл: `main/frontend/app/(main)/services/page.tsx`

### Fixed
- **Исправлены неиспользуемые параметры в handlers избранного**
  - Проблема: Go линтер предупреждал о неиспользуемых параметрах `r *http.Request`
  - Решение: заменены на `_ *http.Request` (blank identifier)
  - Исправлены функции: `getFavorites`, `addFavorite`, `removeFavorite`
  - Файл: `main/backend/handlers/favorites.go`

### Changed
- **Обновлён файл `docs/MVP.md` - добавлен полный список планируемых сервисов**
  - Добавлен новый раздел "📋 Планируемые микросервисы (версии 1.1.0+)"
  - Сервисы разделены по категориям:
    - Коммерческие сервисы (6 шт): зоомагазин, зоомаркет, заводчик, зоогостиница, специалист, ритуальные услуги
    - Государственные сервисы (3 шт): муниципалитет, управление ветеринарии, зоозащитная инспекция
    - Общественные сервисы (4 шт): фонд/НКО, петиции, афиша, учебный центр
    - Сервисы поддержки (2 шт): горячая линия, чат-бот помощник
  - Указано: "Итого планируется: 21 микросервис (7 в MVP + 14 в разработке)"
  - Удалён раздел "Не планируется в ближайшее время" (все сервисы теперь в плане)
  - Файл: `docs/MVP.md`
- **Обновлён файл `README.md` - добавлен раздел с планируемыми сервисами**
  - Добавлен раздел "🚀 Планируемые микросервисы (версии 1.1.0+)" после описания текущих 7 микросервисов
  - Сервисы разделены по тем же категориям с эмодзи для визуальной идентификации
  - Указано общее количество: 21 микросервис
  - Раздел размещён перед "Структура проекта" для логичной последовательности
  - Файл: `README.md`
- **Переименован сервис "Кабинет фонда" → "Кабинет фонда/НКО"**
  - Старое название: "Кабинет фонда"
  - Новое название: "Кабинет фонда/НКО"
  - Причина: объединение фондов и волонтёрских организаций
  - Обновлено описание: "Управление проектами, сборы средств, волонтёры, отчётность"
  - Обновлены функции: добавлены "Координация волонтёров" и "Работа с донорами"
  - Файл: `main/frontend/app/(main)/services/page.tsx`
- **Переименован сервис "Кабинет зоогостиницы" → "Кабинет зоогостиницы/передержки"**
  - Старое название: "Кабинет зоогостиницы"
  - Новое название: "Кабинет зоогостиницы/передержки"
  - Причина: объединение зоогостиниц и частных передержек в один сервис
  - Обновлено описание: "Бронирование, календарь, фото/видео отчёты, передержки"
  - Обновлены функции: заменён "Медицинский контроль" на "Управление передержками"
  - Теперь сервис подходит как для коммерческих зоогостиниц, так и для частных передержек
  - Файл: `main/frontend/app/(main)/services/page.tsx`
- **Переименован сервис "Кабинет кинолога/зоопсихолога" → "Кабинет специалиста"**
  - Старое название: "Кабинет кинолога/зоопсихолога"
  - Новое название: "Кабинет специалиста"
  - Причина: объединение всех специалистов (кинологи, зоопсихологи, грумеры и др.)
  - Обновлено описание: "Кинологи, зоопсихологи, грумеры и другие специалисты"
  - Обновлены функции: добавлены "Груминг-услуги" и "История работы с питомцем"
  - Файл: `main/frontend/app/(main)/services/page.tsx`
- **Кабинет ветклиники - добавлены груминг-услуги в разработку**
  - Груминг-услуги будут интегрированы в кабинет ветклиники
  - Вместо отдельного кабинета груминг-салона
  - Файл: `main/frontend/app/(main)/services/page.tsx`
- **Улучшено выравнивание высоты карточек сервисов на странице `/services`**
  - Проблема: карточки имели разную высоту из-за разного количества функций
  - Решение 1: использован `auto-rows-fr` в grid для автоматического выравнивания строк
  - Решение 2: добавлена минимальная высота для списков функций (`min-h-[120px]` для основных, `min-h-[100px]` для "в разработке")
  - Решение 3: кнопка использует `mt-auto pt-5` для автоматического прижатия к низу
  - Добавлен `flex flex-col` для контейнера карточки
  - Добавлен `flex-1 flex flex-col` для секции с функциями
  - Применено для активных и "в разработке" сервисов
  - Файл: `main/frontend/app/(main)/services/page.tsx`
- **Удалён блок "ℹ️ Информация" на странице `/services`**
  - Причина: избыточная информация, не несущая ценности для пользователя
  - Удалены пункты о новой вкладке, SSO авторизации, localhost
  - Страница стала более компактной
  - Файл: `main/frontend/app/(main)/services/page.tsx`
- **Удалены иконки из карточек сервисов на странице `/services`**
  - Причина: иконки занимали место и не несли особой ценности
  - Удалены все импорты иконок из Heroicons (UserIcon, HomeIcon, HeartIcon и др.)
  - Удалено поле `icon` из интерфейса Service
  - Удалены все строки `icon: XxxIcon` из определений сервисов
  - Упрощена разметка градиентного заголовка карточки
  - Страница стала чище и компактнее
  - Файл: `main/frontend/app/(main)/services/page.tsx`

### Fixed
- **Исправлена ошибка TypeScript на странице `/services`**
  - Проблема: свойство `roles` не определено в интерфейсе `User`
  - Решение: использован type assertion `(user as any)?.roles` для доступа к roles из JWT токена
  - Удалён неиспользуемый импорт `useRouter`
  - Файл: `main/frontend/app/(main)/services/page.tsx`

### Added
- **Страница сервисов `/services`**
  - Каталог всех доступных сервисов платформы
  - Карточки сервисов с градиентными заголовками:
    - Кабинет владельца (порт 6100) - синий градиент
    - Кабинет зоопомощника (порт 6200) - розовый градиент
    - Кабинет приюта (порт 5100) - зелёный градиент
    - Кабинет ветклиники (порт 6300) - фиолетовый градиент
    - ЗооБаза/PetID (порт 4100) - оранжевый градиент 🔒
    - Админ-панель (порт 4000) - серый градиент 🔒
  - **Контроль доступа:**
    - ЗооБаза и Админ-панель видны всем пользователям
    - Badge "🔒 Админ" на карточках с ограниченным доступом
    - При клике проверяется роль `superadmin`
    - Обычные пользователи видят alert: "Доступ к этому сервису имеют только администраторы платформы"
    - Суперадмины могут открыть все сервисы
  - Секция "В разработке":
    - Кабинет зоомагазина (coming soon)
    - Кабинет фонда (coming soon)
  - Для каждого сервиса:
    - Название и описание
    - Иконка (Heroicons)
    - Номер порта
    - Список основных функций (4 пункта)
    - Кнопка "Открыть кабинет" (открывает в новой вкладке)
  - Статус сервисов:
    - "Доступные сервисы" - активные, можно открыть
    - "В разработке" - с badge "Скоро", кнопка disabled
  - Информационный блок с правилами использования
  - Статистика:
    - Активных сервисов (6 - все показываются)
    - В разработке (2)
    - Всего сервисов (8)
  - Адаптивная сетка: 1 колонка (mobile), 2 колонки (tablet), 3 колонки (desktop)
  - Hover эффекты: увеличение тени, изменение цвета границы
  - Клик по карточке → открытие сервиса в новой вкладке
  - Для сервисов "coming soon" → alert с уведомлением
  - Файл: `main/frontend/app/(main)/services/page.tsx`
  - Интеграция с `AuthContext` для проверки ролей
- **Страница избранных питомцев `/favorites`**
  - Отображение всех избранных питомцев пользователя
  - Карточки питомцев с полной информацией:
    - Фото, кличка, порода, пол, возраст
    - Статус (ищет дом, потерялся, найден, требуется помощь)
    - Город/регион
    - Организация (если есть)
    - Контактная информация
    - История (краткое описание)
  - Дата добавления в избранное для каждого питомца
  - Бадж "В избранном" на фото питомца
  - Градиентный заголовок с иконкой сердца (красно-розовый)
  - Счетчик избранных питомцев
  - Кнопка "Добавить ещё" → переход в каталог
  - Пустое состояние с призывом к действию:
    - Иконка разбитого сердца 💔
    - Текст "У вас пока нет избранных"
    - Кнопка "Перейти в каталог"
  - Действия с питомцами:
    - Кнопка "Удалить" (💔) - удаление из избранного с подтверждением
    - Кнопка "Поделиться" (📤) - копирование ссылки в буфер обмена
    - Кнопка "Подробнее" (👁️) - переход на страницу питомца
  - Клик по фото или кличке → переход на страницу питомца
  - Адаптивная сетка: 1 колонка (mobile), 2 колонки (tablet), 3 колонки (desktop)
  - Hover эффекты: увеличение фото, изменение цвета границы на красный
  - Файл: `main/frontend/app/(main)/favorites/page.tsx`
  - Использует API: `GET /api/favorites` (список избранных)
  - Использует API: `DELETE /api/favorites/{pet_id}` (удаление)
  - Использует API: `GET /api/pets/{id}` (информация о питомце)
  - Интеграция с `favorites-api.ts`
- **Вкладка "Избранное" в Sidebar**
  - Добавлена ссылка на страницу `/favorites`
  - Иконка HeartIcon (сердце)
  - Расположена после "Каталог", перед "Сервисы"
  - Файл: `main/frontend/app/components/layout/Sidebar.tsx`

### Changed
- **Обновлено описание кабинета приюта на странице сервисов**
  - Разделены секции "Основные функции" и "В разработке"
  - Основные функции (4 пункта):
    - Реестр животных приюта
    - Приём и выбытие
    - Управление волонтёрами
    - Статистика пристройств
  - В разработке (3 пункта):
    - 🔧 Приём заявок на отлов
    - 🔧 Программа ОСВВ (Отлов-Стерилизация-Вакцинация-Возврат)
    - 🔧 Отчётность в государственные органы
  - Добавлено новое поле `inDevelopment` в интерфейс Service
  - Визуальное разделение: зелёные галочки (✓) для готовых функций, оранжевые гаечные ключи (🔧) для функций в разработке
  - Файл: `main/frontend/app/(main)/services/page.tsx`
- **Обновлено описание кабинета зоопомощника на странице сервисов**
  - Добавлена секция "В разработке" (3 пункта):
    - 🔧 Организация сборов (создание и управление сборами средств для подопечных)
    - 🔧 Передержки (управление передержками животных)
    - 🔧 Репутация и бейджи (система достижений и рейтинга волонтёров)
  - Файл: `main/frontend/app/(main)/services/page.tsx`
- **Обновлено описание кабинета владельца на странице сервисов**
  - Добавлена секция "В разработке" (3 пункта):
    - 🔧 Регистрация новых питомцев (создание PetID)
    - 🔧 Внесение обработок (от блох, клещей, глистов)
    - 🔧 Напоминания о процедурах (вакцинация, обработки)
  - Файл: `main/frontend/app/(main)/services/page.tsx`
- **Обновлено описание кабинета ветклиники на странице сервисов**
  - Добавлена секция "В разработке" (3 пункта):
    - 🔧 Онлайн-запись на приём (запись владельцев к ветеринару)
    - 🔧 Подтверждение смерти (официальное подтверждение смерти животного)
    - 🔧 Выдача чипов (регистрация и выдача микрочипов)
  - Файл: `main/frontend/app/(main)/services/page.tsx`

### Technical
- Страница избранного загружает полную информацию о питомцах через PetID API
- Используется Promise.all для параллельной загрузки данных о питомцах
- Обработка ошибок при загрузке отдельных питомцев (не блокирует загрузку остальных)
- Фильтрация: показываются только питомцы, информация о которых успешно загружена

## [0.10.4] 🏢❤️ - 2025-01-13

### Added
- **Модерация организаций в Admin панели (Backend + Frontend)**
  
  **Backend API:**
  - Handler `AdminOrganizationsHandler` - GET список организаций с фильтрами
    - Фильтрация по статусу: all, verified, pending, rejected
    - Фильтрация по типу: shelter, vet_clinic, pet_shop, foundation, kennel, other
    - Поиск по названию и ИНН
    - Пагинация (page, limit)
    - Сортировка: сначала непроверенные, потом по дате создания
    - Возвращает список организаций с информацией о владельце
    - Endpoint: `GET /api/admin/organizations`
  - Handler `AdminVerifyOrganizationHandler` - PUT верификация/отклонение организации
    - Действия: verify (верифицировать), reject (отклонить)
    - Опциональная причина отклонения
    - Обновление полей `is_verified` и `status`
    - Логирование действия админа в таблицу `admin_logs`
    - Endpoint: `PUT /api/admin/organizations/{id}/verify`
  - Handler `AdminOrganizationStatsHandler` - GET статистика организаций
    - Всего организаций
    - Верифицированных
    - Ожидают проверки
    - Отклоненных
    - Статистика по типам организаций
    - Endpoint: `GET /api/admin/organizations/stats`
  - Файл: `admin/backend/handlers/organizations.go`
  - Все роуты защищены `SuperAdminMiddleware`
  - Роуты добавлены в `admin/backend/main.go`
  
  **Frontend:**
  - Страница модерации организаций `/organizations`
  - Компоненты:
    - Header с логотипом и меню пользователя (logout, переход на главную)
    - Sidebar с навигацией (активная вкладка "Организации")
    - 4 карточки статистики (StatsWidget):
      * Всего организаций (синяя, BuildingOfficeIcon)
      * Верифицировано (зелёная, CheckIcon)
      * Ожидают проверки (оранжевая, ClipboardDocumentListIcon)
      * Отклонено (фиолетовая, XMarkIcon)
    - Фильтры (TableWidget actions):
      * По статусу: все, ожидают проверки, верифицированы, отклонены
      * По типу: все типы, приют, ветклиника, зоомагазин, фонд, питомник, другое
      * Поиск по названию и ИНН (live search)
    - Таблица организаций:
      * Колонки: ID, название, тип, владелец, контакты, статус, дата создания, действия
      * Цветовые индикаторы статуса:
        - Верифицирована: зелёный badge
        - На проверке: жёлтый badge
        - Отклонена: красный badge
      * Кнопки действий (только для непроверенных):
        - Верифицировать (зелёная, CheckIcon)
        - Отклонить (красная, XMarkIcon)
    - Модальное окно верификации/отклонения:
      * Название организации и ИНН
      * Поле для причины отклонения (textarea, опционально)
      * Кнопки: Отмена, Верифицировать/Отклонить
    - Пагинация:
      * Информация: "Страница X из Y (всего Z)"
      * Кнопки: Назад, Вперёд (disabled при достижении границ)
  - Функционал:
    - Загрузка данных с Backend API
    - Автоматическое обновление после действий
    - Фильтрация с автосбросом на страницу 1
    - Responsive design (mobile menu)
    - Защита от несанкционированного доступа (redirect на /auth)
  - Файл: `admin/frontend/app/organizations/page.tsx`
  - Использует компоненты: `StatsWidget`, `TableWidget`
  - Использует стили: `admin/frontend/app/page.module.css`
  - Интеграция с Backend API:
    - `GET /api/admin/organizations` (с параметрами)
    - `PUT /api/admin/organizations/{id}/verify`
    - `GET /api/admin/organizations/stats`
    - `GET /api/admin/me` (проверка авторизации)
- **Вкладка "Организации" в главной админке**
  - Добавлена иконка `BuildingOfficeIcon` в sidebar
  - Редирект на страницу `/organizations` при клике
  - Сохранение активной вкладки в localStorage
  - Файл: `admin/frontend/app/page.tsx`
- **Интеграция организаций с питомцами (ЭТАП 3 - Каталог питомцев)**
  
  **Примечание:** Каталог содержит питомцев из двух источников:
  - Питомцы от организаций (приюты, ветклиники, фонды, питомники)
  - Питомцы от волонтёров/зоопомощников (частные лица, кураторы)
  
  **Backend API:**
  - Handler `SetPetOrganizationHandler` - привязка питомца к организации
    - Endpoint: `PUT /api/pets/{id}/organization`
    - Параметры: `organization_id` (int или null для отвязки)
    - Проверка прав:
      * Владелец питомца может привязать к любой организации
      * Член организации (owner, admin, moderator) может привязать питомца к своей организации
    - Проверка верификации организации (только verified и active)
    - Логирование действий
    - Файл: `petbase/backend/handlers/pet_organization.go`
  - Обновлен `CatalogHandler` - добавлен JOIN с таблицей organizations
    - Возвращает `organization_id`, `organization_name`, `organization_type`
    - Новые фильтры:
      * `organization` - фильтр по типу организации (shelter, vet_clinic, foundation, kennel)
      * `from_organization` - фильтр "только из организаций" (true/false)
    - Все фильтры обновлены для работы с JOIN (алиас `p.` для pets, `o.` для organizations)
    - Файл: `petbase/backend/handlers/pets.go`
  - Обновлена структура `Pet`:
    - Добавлены поля: `OrganizationName`, `OrganizationType`
    - Файл: `petbase/backend/handlers/pets.go`
  - Роутинг обновлен в `PetDetailHandler`:
    - Добавлена обработка `/api/pets/{id}/organization`
    - Файл: `petbase/backend/handlers/pets.go`
  
  **Frontend:**
  - Обновлена страница каталога `/catalog`
    - Добавлены новые фильтры:
      * Фильтр по типу организации (все, приюты, ветклиники, фонды, питомники)
      * Чекбокс "Только из организаций" (исключает питомцев от частных лиц/волонтёров)
    - Обновлен интерфейс `Pet`:
      * Добавлены поля: `organization_id`, `organization_name`, `organization_type`
    - Отображение организации в карточке питомца:
      * Синий badge с иконкой 🏢 и названием организации
      * Отображается под информацией о местоположении
    - Обновлена логика фильтрации:
      * Фильтрация по типу организации
      * Фильтрация "только из организаций"
    - Файл: `main/frontend/app/(main)/catalog/page.tsx`
- **Быстрые действия в каталоге питомцев**
  - Кнопка "Поделиться" (📤):
    - Копирование ссылки на питомца в буфер обмена
    - Визуальная обратная связь "✓ Скопировано" на 2 секунды
    - Ссылка формата: `/petid/{id}`
  - Кнопка "Избранное" (❤️):
    - Placeholder для будущей функциональности
    - Временное уведомление о разработке
  - Кнопка "Написать" (💬):
    - Отображается только если есть контактный телефон
    - Placeholder для интеграции с мессенджером
    - Синяя кнопка с акцентом
  - Расположение: под описанием питомца, над контактами
  - Адаптивный дизайн: 3 кнопки в ряд (или 2, если нет контакта)
  - Файл: `main/frontend/app/(main)/catalog/page.tsx`
- **Система избранных питомцев (Favorites)**
  
  **База данных:**
  - Таблица `favorites`:
    - Поля: `id`, `user_id`, `pet_id`, `created_at`
    - UNIQUE constraint на пару (user_id, pet_id)
    - Индексы для быстрого поиска
    - Каскадное удаление при удалении пользователя или питомца
  - Миграция: `database/migrations/026_create_favorites.sql`
  
  **Backend API:**
  - Handler `FavoritesHandler`:
    - GET `/api/favorites` - получить список избранных питомцев
    - POST `/api/favorites` - добавить питомца в избранное
  - Handler `FavoriteDetailHandler`:
    - DELETE `/api/favorites/{pet_id}` - удалить из избранного
  - Проверка существования питомца перед добавлением
  - Обработка дубликатов (UNIQUE constraint)
  - Защита авторизацией (AuthMiddleware)
  - Файл: `main/backend/handlers/favorites.go`
  - Роуты добавлены в `main/backend/main.go`
  
  **Frontend:**
  - API клиент для работы с избранным:
    - `getFavorites()` - получить список
    - `addFavorite(petId)` - добавить
    - `removeFavorite(petId)` - удалить
    - Файл: `main/frontend/lib/favorites-api.ts`
  - Обновлена страница каталога:
    - Загрузка избранных при монтировании
    - Состояние `favoritePetIds` (Set) для быстрой проверки
    - Кнопка "Избранное" с визуальной индикацией:
      * 🤍 "Избранное" - не в избранном (серая)
      * ❤️ "В избранном" - в избранном (красная)
      * ⏳ - загрузка
    - Toggle функционал (добавить/удалить одной кнопкой)
    - Обработка ошибок (уведомление об авторизации)
    - Файл: `main/frontend/app/(main)/catalog/page.tsx`

### Changed
- **Обновлен MVP.md**
  - Отмечены выполненными задачи:
    * "Модерация организаций (Admin)"
    * "Интеграция организаций с питомцами"
    * "Быстрые действия в каталоге" (полная реализация избранного)
  - Обновлена статистика: 188/334 задач (~56%)
  - Дата обновления: 13 января 2025
- **Обновлен ROADMAP.md**
  - Текущая версия: 0.10.3
  - Добавлено упоминание модерации организаций
  - Дата обновления: 13 января 2025

### Technical
- Поле `is_verified` уже существует в таблице `organizations` (миграция 019)
- Используется существующая система логирования админских действий
- CORS настроен для admin frontend (localhost:4000)

## [0.10.2] 🏢 - 2025-01-12

### Added
- **Поддержка публикации постов от имени организации**
  - В CreatePost добавлен dropdown выбора автора: "От себя" или организация
  - Пользователи с ролью owner/admin могут публиковать от имени организации
  - Добавлены поля в модель `CreatePostRequest`:
    - `AuthorType` - тип автора ("user" или "organization")
    - `OrganizationID` - ID организации если публикация от организации
  - Добавлена проверка прав на публикацию от имени организации в `createPost`:
    - Проверяется наличие записи в `organization_members` с `can_post = 1`
    - Если прав нет - возвращается 403 Forbidden
  - Посты от организаций сохраняются с `author_type = 'organization'` и `author_id = organization_id`
  - Файлы:
    - `main/backend/models/post.go` (добавлены поля `AuthorType`, `OrganizationID`, `Organization`)
    - `main/backend/handlers/posts.go` (обновлён `createPost`, добавлена проверка прав)
    - `main/frontend/app/components/posts/CreatePost.tsx` (dropdown выбора автора)
- **API endpoint для получения постов организации**
  - Новый endpoint: `GET /api/posts/organization/{id}`
  - Возвращает все опубликованные посты организации
  - Включает данные организации (name, short_name, logo)
  - Сортировка по дате создания (новые первыми)
  - Файлы:
    - `main/backend/handlers/posts.go` (функции `OrganizationPostsHandler`, `getOrganizationPosts`)
    - `main/backend/main.go` (регистрация роута)
    - `main/frontend/lib/api.ts` (метод `getOrganizationPosts`)
- **Отображение постов организации на странице организации**
  - Загрузка и отображение постов на странице `/org/{id}`
  - Посты отображаются с логотипом и коротким названием организации
  - Файл: `main/frontend/app/(main)/org/[id]/page.tsx`

### Fixed
- **Исправлена загрузка организаций в CreatePost**
  - Проблема: dropdown выбора автора поста не показывал организации пользователя, только "От себя"
  - API endpoint `/api/organizations/my` возвращал 500 Internal Server Error
  - Причина: поля `short_name` и `type` в таблице `organizations` могут быть NULL, но в Go структуре были объявлены как `string` (не поддерживает NULL)
  - SQL ошибка: `sql: Scan error on column index 2, name "short_name": converting NULL to string is unsupported`
  - Решение: изменены типы полей на `sql.NullString` в `GetMyOrganizationsHandler`:
    - `shortName` изменён с `string` на `sql.NullString`
    - `orgType` изменён с `string` на `sql.NullString`
    - Добавлена проверка `Valid` перед использованием значений: `if shortName.Valid { org["short_name"] = shortName.String }`
  - Теперь API корректно возвращает организации где пользователь owner/admin с правом публикации
  - Файл: `main/backend/handlers/organizations.go` (функция `GetMyOrganizationsHandler`, строки 714-745)

### Changed
- **Использование короткого названия организации в CreatePost**
  - В dropdown выбора автора теперь отображается `short_name` вместо полного `name`
  - Fallback на полное название если короткое не указано: `org.short_name || org.name`
  - Изменено в двух местах:
    - В кнопке выбора автора (строка 566-569)
    - В списке организаций dropdown (строка 620)
  - Файл: `main/frontend/app/components/posts/CreatePost.tsx`
- **Отображение автора организации в PostCard**
  - Проверка `post.author_type` для определения типа автора
  - Если `author_type === 'organization'`:
    - Отображается логотип организации вместо аватара пользователя
    - Отображается короткое название организации (`short_name`) или полное (`name`)
  - Если `author_type === 'user'`:
    - Отображается аватар и имя пользователя как раньше
  - Добавлено поле `organization?: Organization` в интерфейс `Post`
  - Файлы:
    - `main/frontend/app/components/posts/PostCard.tsx` (строки 170-200)
    - `main/frontend/lib/api.ts` (интерфейс Post)
- **Обновлены SQL запросы для загрузки постов с данными организации**
  - Добавлен `LEFT JOIN organizations o` во все запросы получения постов
  - Загружаются поля: `o.name`, `o.short_name`, `o.logo`
  - Обновлена функция `scanPost` для чтения данных организации из результата запроса
  - Если `author_type = 'organization'`, создаётся объект `Organization` и добавляется в `post.Organization`
  - Обновлены запросы в функциях:
    - `getAllPosts` - лента всех постов
    - `getDrafts` - черновики пользователя
    - `getUserPosts` - посты пользователя
    - `getPetPosts` - посты питомца
    - `getPostByID` - получение поста по ID
  - Файл: `main/backend/handlers/posts.go`
  
### Technical
- **Добавлено детальное логирование в GetMyOrganizationsHandler для отладки**
  - Логирование извлечения userID из контекста
  - Логирование выполнения SQL запроса
  - Логирование каждой найденной организации с деталями (id, name, role, can_post)
  - Логирование ошибок сканирования строк
  - Логирование общего количества найденных организаций
  - Файл: `main/backend/handlers/organizations.go`
- **Добавлена загрузка организаций при монтировании CreatePost**
  - useEffect вызывает `loadOrganizations()` при монтировании компонента
  - Добавлено логирование: `console.log('📋 Загружены организации:', response.data)`
  - Файл: `main/frontend/app/components/posts/CreatePost.tsx`
  - Причина 1: организации загружались только при открытии модального окна, но на этих страницах форма отображается сразу
  - Причина 2: backend handler `GetMyOrganizationsHandler` падал с panic при извлечении userID из контекста
  - Решение: 
    - Добавлена загрузка организаций при монтировании компонента
    - Добавлена безопасная проверка типа userID в backend handler
    - Добавлено детальное логирование ошибок
  - Теперь dropdown показывает все организации пользователя с правами публикации
  - Файлы: 
    - `main/frontend/app/components/posts/CreatePost.tsx`
    - `main/backend/handlers/organizations.go`

## [0.10.1] - 2025-01-12 🐾

### Fixed
- **Исправлена ошибка callback в CreatePost на странице профиля**
  - Проблема: `ReferenceError: loadPosts is not defined` на странице профиля пользователя
  - В компоненте `CreatePost` использовался несуществующий callback `loadPosts`
  - Решение: заменён на правильный callback `loadUserProfile`, который загружает посты через `postsApi.getUserPosts(userId)`
  - Файл: `main/frontend/app/(main)/[userId]/page.tsx`

- **Исправлена проверка прав на странице организации**
  - Проблема: компонент `CreatePost` не отображался на странице организации для owner/admin
  - Причина: функция `isOwnerOrAdmin()` проверяла массив `members` до его загрузки
  - Решение: 
    - Добавлена проверка `membersLoading` в функцию `isOwnerOrAdmin()`
    - Функция возвращает `false` пока идёт загрузка членов организации
    - Добавлено логирование для отладки (user, members, isOwnerOrAdmin)
  - Файл: `main/frontend/app/(main)/org/[id]/page.tsx`
  - Теперь форма создания поста корректно отображается для owner и admin организации

### Added
- **CreatePost на странице организации**
  - Форма создания поста добавлена на страницу организации `/org/[id]`
  - Отображается только для owner и admin с правом публикации
  - Автоматическое обновление списка постов после создания
  - Callback `onPostCreated` вызывает `loadPosts()`
  - Файл: `main/frontend/app/(main)/org/[id]/page.tsx`
- **CreatePost на странице профиля пользователя**
  - Форма создания поста добавлена на страницу профиля `/[userId]`
  - Отображается только на своем профиле (isOwnProfile = true)
  - Размещена в табе "Публикации" перед списком постов
  - Автоматическое обновление списка постов после создания
  - Файл: `main/frontend/app/(main)/[userId]/page.tsx`

### Changed
- **Структура постов на странице организации**
  - CreatePost обёрнут в отдельный белый блок с тенью и отступами
  - Соответствует дизайну главной страницы с метками
  - Улучшена визуальная иерархия между формой создания и списком постов
  - Файл: `main/frontend/app/(main)/org/[id]/page.tsx`

- **Структура постов на странице профиля пользователя**
  - CreatePost обёрнут в отдельный белый блок с тенью и отступами
  - Единообразный дизайн с главной страницей и страницей организации
  - Файл: `main/frontend/app/(main)/[userId]/page.tsx`
  - Секция "Публикации" теперь включает CreatePost и список постов
  - CreatePost отображается первым элементом
  - Список постов обернут в отдельный div с заголовком
  - Файл: `main/frontend/app/(main)/org/[id]/page.tsx`

### Technical
- Импортирован компонент CreatePost в обе страницы
- Используется проверка прав доступа перед отображением формы
- Callback onPostCreated обеспечивает обновление списка постов

## [0.9.1] 🏢 - 2025-01-12
  - Кнопка с именем автора и стрелкой вниз в модальном окне создания поста
  - Выбор "От себя" (user) или "От организации X" (organization)
  - Загрузка списка организаций пользователя при открытии модального окна
  - Фильтрация организаций: только где role = owner или admin и can_post = true
  - Отображение аватара выбранного автора (пользователь или организация)
  - Автоматическое закрытие dropdown при выборе автора
  - State управление: `selectedAuthor`, `selectedOrganizationId`, `showAuthorDropdown`
  - Файл: `main/frontend/app/components/posts/CreatePost.tsx`
- **API endpoint GET /api/organizations/my**
  - Возвращает организации где пользователь owner или admin с правом публикации
  - Фильтрация по `can_post = 1`
  - Сортировка по названию организации (ASC)
  - Возвращает: id, name, short_name, type, logo, bio, role, can_post
  - Handler: `GetMyOrganizationsHandler`
  - Файл: `main/backend/handlers/organizations.go`
  - Роут зарегистрирован в `main/backend/main.go`
- **Сохранение автора поста в БД**
  - Поля `author_type` и `organization_id` отправляются в POST /api/posts
  - `author_type`: 'user' (от себя) или 'organization' (от организации)
  - `organization_id`: NULL если user, ID организации если organization
  - `user_id`: всегда содержит ID пользователя, который создал пост
  - Обновлен `handleSubmit` в CreatePost для отправки новых полей
- **Функция loadOrganizations**
  - Загружает организации при открытии модального окна
  - useEffect срабатывает при `showModal = true`
  - Обработка ошибок с fallback на пустой массив
  - Файл: `main/frontend/app/components/posts/CreatePost.tsx`

### Fixed
- **Исправлена функция отправки JSON ответа в GetMyOrganizationsHandler**
  - Проблема: использовалась несуществующая функция `sendJSONResponse`
  - Решение: заменено на `sendJSONSuccess` (существующая helper функция)
  - Файл: `main/backend/handlers/organizations.go`
  - Backend успешно компилируется

### Technical
- Поля `author_type` и `organization_id` уже существуют в таблице `posts`
- Используется существующая структура БД без дополнительных миграций
- Индексы уже созданы: `idx_posts_organization_id`, `idx_posts_author_type`

## [0.10.0] 🏢 - 2025-01-12

### Added
- **ЭТАП 0 - ДРУЗЬЯ (Frontend)**
  - Компонент `FriendButton` на странице профиля (Добавить в друзья / Принять / Отклонить / Удалить)
  - Страница `/friends` - список всех друзей с поиском
  - Страница `/friends/requests` - входящие и исходящие запросы в друзья
  - Компонент `FriendsList` для отображения списка друзей с аватарами
  - Счётчик входящих запросов в Header (красный badge)
  - Backend handlers уже были реализованы ранее (таблица `friendships`, API endpoints)
- **ЭТАП 1 - ЛАЙКИ И КОММЕНТАРИИ (Frontend)**
  - Кнопка лайка на постах с анимацией и счётчиком
  - Модальное окно `LikersModal` со списком пользователей, поставивших лайк
  - Кнопка "Комментарии" на постах с счётчиком
  - Форма добавления комментария под постом
  - Список комментариев с аватарами, именами и датами
  - Кнопка удаления своих комментариев
  - Backend handlers уже были реализованы ранее
- **ЭТАП 2 - ОРГАНИЗАЦИИ: Редактирование и управление командой**
  - **Страница редактирования организации** (`/org/[id]/edit`)
    - Структура как у редактирования профиля с 3 вкладками
    - **Вкладка "Основная информация"**: логотип, обложка, название, краткое и полное описание
    - **Вкладка "Контакты"**: email, телефон, веб-сайт, полный адрес, город
    - **Вкладка "Команда"**: компонент `MembersManager` для управления членами организации
    - Загрузка и превью логотипа и обложки
    - Счетчики символов для описаний (200/1000)
    - Индикаторы заполненности разделов (зелёные точки)
    - Подсказки для каждой вкладки
    - Валидация полей и обработка ошибок
  - **Backend API для редактирования организаций**
    - `OrganizationHandler` - общий handler для маршрутизации GET/PUT запросов
    - `UpdateOrganizationHandler` - обновление данных организации
    - Проверка прав доступа (только члены с `can_edit = true`)
    - Обновление только переданных полей
    - Поддержка всех полей: name, short_name, bio, description, email, phone, website, address_full, address_city
  - **API методы для управления членами организации**
    - `addMember()` - добавление нового участника
    - `updateMember()` - изменение роли и должности
    - `removeMember()` - удаление участника
    - Добавлены в `organizations-api.ts`
  - **Компонент MembersManager**
    - Список участников с аватарами, ролями и должностями
    - Модальное окно добавления участника с поиском пользователей
    - Модальное окно редактирования роли и должности
    - Удаление участников с подтверждением
    - Цветовая индикация ролей (owner - фиолетовый, admin - синий, moderator - зелёный, member - серый)
    - Защита от удаления owner и самого себя
  - **Блок управления организацией на странице организации**
    - Показывается только для owner и admin
    - Кнопка "Система управления" открывает dashboard соответствующего сервиса
    - Автоматический переход в dashboard с `orgId` в URL
    - Кнопка "Редактировать профиль" ведёт на `/org/{id}/edit`

### Fixed
- **Исправлен ключ контекста в уведомлениях**
  - Унифицирован ключ на `"userID"` во всех handlers уведомлений
  - Исправлена ошибка вложенных кнопок в `NotificationsDropdown.tsx`
- **Исправлены ошибки компиляции Go**
  - `http.MethodPUT` → `http.MethodPut` в `organizations.go`
  - Удалён неиспользуемый параметр в `announcements.go`
- **Исправлены типы в TypeScript**
  - `OrganizationMember.user_name` теперь `string | undefined`
  - `Member.user_name` в `MembersManager` теперь опциональное
  - Добавлены проверки на `undefined` при отображении имён
  - Удалён неиспользуемый импорт `CheckIcon`

### Changed
- **Фильтр организаций**
  - Добавлен флаг `autoFilterDisabled` для предотвращения автосброса
  - Исправлена проблема постоянного срабатывания автофильтра при выборе "Все регионы"
- **Shelter dashboard**
  - Автоматическое сохранение `orgId` из URL в localStorage
  - Прямой переход в dashboard конкретной организации

### Technical
- Роут `/api/organizations/{id}` теперь обрабатывает GET и PUT через `OrganizationHandler`
- Добавлен `AuthMiddleware` для PUT запросов к организациям
- Улучшена обработка ошибок в API организаций

## [0.9.0] 🏢 - 2025-01-11

### Added
- **Страница создания организации** (`/org/create`)
  - Интеграция с DaData API для поиска по ИНН и автозаполнения
  - Автоопределение типа организации по ключевым словам
  - Яндекс.Карта для отображения местоположения
  - Создатель автоматически становится owner
- **Страница организации** (`/org/[id]`)
  - Компактная Яндекс.Карта (высота 250px)
  - Блок управления для owner/admin
  - Кнопка перехода в систему управления

## [0.8.0] 🏥 - 2025-12-31

### Added
- **Создан микросервис Clinic - Кабинет ветеринарной клиники (порты 6300/8600)**
  - **Multi-tenancy архитектура**: каждая клиника изолирована через `organization_id`
  - **TenantMiddleware**: проверка доступа через заголовок `X-Clinic-ID`
  - **Страницы выбора и создания клиники**: `/select` и `/create` (как в Shelter)
  - Backend на Go (порт 8600) с JWT middleware и hot reload
  - Frontend на Next.js 16 (порт 6300)
  - AdminLayout в стиле Admin/Shelter/Owner/Volunteer панелей
  - 3 вкладки: Обзор, Пациенты, Записи на приём
  - Градиентные карточки статистики (синяя, зеленая, фиолетовая)
  - Таблица с пациентами клиники
  - Страница авторизации `/auth` с автоматическим SSO редиректом
  - API endpoints: `/api/my-clinics`, `/api/clinics`, `/api/my-patients`, `/api/appointments`, `/api/profile`, `/api/organization`
  - Использование единой базы данных (таблица `organizations` с type='clinic')
  - Health endpoint для проверки работоспособности
  - Hot reload с Air для backend
  - CORS настроен для localhost:3000 и localhost:6300
  - Favicon и логотип в layout
  - README с полной документацией
- **Создан микросервис Owner - Кабинет владельца (порты 6100/8400)**
  - Backend на Go (порт 8400) с JWT middleware и hot reload
  - Frontend на Next.js 16 (порт 6100) - изменен с 7000 из-за конфликта с системными процессами macOS
  - AdminLayout в стиле Admin/Shelter панелей (светлое меню, градиентные карточки)
  - 3 вкладки: Обзор, Мои питомцы, Здоровье
  - Градиентные карточки статистики (синяя, зеленая, фиолетовая)
  - Таблица с питомцами пользователя
  - Страница авторизации `/auth` с автоматическим SSO редиректом
  - Проверка авторизации перед редиректом (если уже авторизован - сразу в дашборд)
  - API endpoints: `/api/my-pets`, `/api/pets/events`, `/api/profile`
  - Использование единой базы данных (таблица `pets` с полем `user_id`)
  - API клиент для взаимодействия с backend
  - Health endpoint для проверки работоспособности
  - Hot reload с Air для backend
  - CORS настроен для localhost:3000 и localhost:6100
  - Favicon и логотип в layout
  - README с полной документацией
- **Создан микросервис Volunteer - Кабинет зоопомощника (порты 6200/8500)**
  - Backend на Go (порт 8500) с JWT middleware и hot reload
  - Frontend на Next.js 16 (порт 6200)
  - AdminLayout в стиле Admin/Shelter/Owner панелей
  - 3 вкладки: Обзор, Мои подопечные, Задачи
  - Градиентные карточки статистики (синяя, зеленая, фиолетовая)
  - Таблица с подопечными питомцами (где `curator_id = user_id`)
  - Страница авторизации `/dashboard` с автоматическим SSO редиректом
  - API endpoints: `/api/my-pets`, `/api/my-tasks`, `/api/profile`
  - Использование единой базы данных (таблица `pets` с полем `curator_id`)
  - Health endpoint для проверки работоспособности
  - Hot reload с Air для backend
  - CORS настроен для localhost:3000 и localhost:6200
  - Favicon и логотип в layout
  - README с полной документацией
- **Инфраструктура**
  - Обновлен скрипт `./run` для запуска Clinic на портах 6300/8600
  - Обновлен скрипт `./run` для запуска Owner на порту 6100
  - Обновлен скрипт `./run` для запуска Volunteer на портах 6200/8500
  - Улучшена функция `kill_port()` с 3 попытками убить процесс (SIGTERM, SIGKILL, sudo SIGKILL)
  - Добавлена финальная проверка портов перед запуском сервисов
  - Добавлена очистка портов 6100, 6200, 6300, 8400, 8500, 8600
  - Добавлена проверка портов Owner, Volunteer и Clinic при запуске
  - Обновлен главный README.md с портами 6100 (Owner), 6200 (Volunteer), 6300 (Clinic)
  - Добавлена очистка кэша для Owner, Volunteer и Clinic frontend

### Fixed
- **Owner Backend**
  - Исправлено: использование `user_id` вместо `owner_id` в SQL запросах
  - Исправлено: handler `GetMyPets` теперь корректно загружает питомцев из единой базы
  - Исправлено: handler `GetPetEvents` использует правильное поле `user_id`
- **Volunteer Backend**
  - Исправлено: JWT_SECRET синхронизирован с Main backend для корректной SSO авторизации
  - Исправлено: убрана неиспользуемая переменная `userID` в handler `GetMyTasks`
- **Volunteer Frontend**
  - Исправлено: установлены зависимости Next.js и React для корректного запуска
- **Clinic Backend**
  - Исправлено: CORS теперь разрешает заголовок `X-Clinic-ID` для multi-tenancy
  - Исправлено: добавлен `X-Clinic-ID` в `Access-Control-Allow-Headers` в обоих CORS middleware
  - Исправлено: убрана неиспользуемая переменная `userID` в handler `GetMyPatients`
  - Backend успешно компилируется и готов к запуску
  - Исправлено: установлены зависимости Next.js и React для корректного запуска
- **Clinic Backend**
  - Исправлено: убрана неиспользуемая переменная `userID` в handler `GetMyPatients`
  - Backend успешно компилируется и готов к запуску

## [0.7.0] - 2024-12-31 🏠

### Added
- **Микросервис Shelter - Кабинет приюта**
  - Backend на Go (порт 8200) с Chi router и JWT middleware
  - Frontend на Next.js 16 (порт 5100) с AdminLayout
  - 4 вкладки: Статистика, Животные приюта, Пристройство, Волонтеры
  - Градиентные карточки статистики (синяя, зеленая, оранжевая, фиолетовая)
  - Виджеты: StatsWidget, TableWidget (скопированы из PetBase)
  - Страница авторизации `/auth` с SSO интеграцией
  - **Страница выбора приюта `/select`** - пользователь выбирает приют перед входом
  - **Страница создания приюта `/create`** - форма для создания нового приюта
  - API endpoint `/api/shelters` (POST) для создания приюта
  - Если у пользователя нет приютов - предлагается создать новый
  - Дашборд перемещен в `/dashboard`
  - API endpoint `/api/my-shelters` для получения списка приютов пользователя
  - Автоматический редирект: если приют один - сразу в дашборд
  - Сохранение выбранного приюта в localStorage
  - API клиент для взаимодействия с backend
  - Health endpoint для проверки работоспособности
  - Hot reload с Air для backend
  - CORS настроен для localhost:5100
  - README с полной документацией
- **Multi-tenancy архитектура для Shelter**
  - Каждый приют работает в изолированной области (tenant)
  - Таблицы: `shelter_animals`, `shelter_volunteers`, `adoption_requests`, `shelter_events`
  - Все таблицы имеют `organization_id` для изоляции данных
  - Middleware для автоматического определения текущего приюта
  - Пользователь может быть членом нескольких приютов
  - Автоматическая фильтрация всех запросов по tenant
  - Использование существующей системы Organizations
  - Миграция 022: создание таблиц приюта с multi-tenancy
- **Инфраструктура**
  - Обновлен скрипт `./run` для запуска Shelter
  - Добавлена очистка портов 5100 и 8200
  - Добавлена проверка портов Shelter при запуске
  - Обновлен главный README.md с информацией о Shelter
  - Admin backend: добавлен CORS для localhost:5100

### Fixed
- **Shelter Backend - критические ошибки компиляции**
  - Добавлена функция `AuthMiddleware` в `shelter/backend/middleware/auth.go`
  - Добавлена константа `UserIDKey` для единообразного использования context keys
  - Исправлен импорт database пакета (используется `database.DB`)
  - Исправлена регистрация routes (handlers обёрнуты в `http.HandlerFunc`)
  - Создан `.env` файл с правильным JWT_SECRET (синхронизирован с main backend)
  - Исправлен SQL запрос в `GetOrganization` (добавлены `COALESCE` для nullable полей)
  - Backend теперь успешно компилируется и запускается
  - Multi-tenancy работает корректно - пользователь видит только данные своего приюта

## [0.6.0] - 2024-12-30 💬

### Added
- **Мессенджер (личные чаты 1-на-1)**
  - Страница `/messenger` с полноценным интерфейсом чата
  - Список диалогов с аватарами, последними сообщениями и временем
  - Отправка и получение текстовых сообщений
  - Автоматическое создание чата при первом сообщении
  - Кнопка "Написать" на странице профиля пользователя
  - Счетчик непрочитанных сообщений в сайдбаре (красный бадж)
  - Автоматическая отметка сообщений как прочитанных
  - Сортировка чатов по времени последнего сообщения
  - Временные метки: "только что", "5 мин назад", "вчера"
  - Градиентные аватары с первой буквой имени
  - Placeholder для новых диалогов с подсказкой
  - Индикатор "Новый диалог" для несозданных чатов
- **Backend API для мессенджера**
  - `GET /api/chats` - список диалогов пользователя
  - `GET /api/chats/:id/messages` - сообщения конкретного чата
  - `POST /api/messages/send` - отправка сообщения
  - `GET /api/messages/unread` - количество непрочитанных
  - Автоматическое создание чата при отправке первого сообщения
  - Проверка участия пользователя в чате
- **База данных**
  - Таблица `chats` для хранения диалогов
  - Таблица `messages` для хранения сообщений
  - Таблица `message_attachments` для будущих вложений
  - Индексы для оптимизации запросов

### Fixed
- Исправлена проблема с редиректом при обновлении страницы мессенджера
- Исправлена ошибка "interface conversion: interface {} is nil, not int" в handlers
- Исправлено несоответствие ключей контекста: "userID" vs "user_id"
- Добавлена обработка ответа API при загрузке информации о пользователе
- Добавлена проверка наличия receiverId перед отправкой сообщения

### Technical
- Добавлены модели `Chat`, `Message` в `main/backend/models/chat.go`
- Добавлены handlers в `main/backend/handlers/messenger.go`
- Создан хук `useUnreadMessages.ts` для счетчика непрочитанных
- Обновлен `Sidebar.tsx` с реальным счетчиком непрочитанных
- Миграция `021_create_messenger.sql`

## [0.5.0] - 2024-12-30 🎨

### Added
- **Каталог питомцев `/catalog`**
  - Публичный endpoint `/api/catalog` без авторизации
  - Отображение питомцев со статусами: ищет дом, потерялся, найден, требуется помощь
  - Карточный дизайн с фото, информацией и контактами
  - Фильтры: поиск по кличке/породе, статус, вид животного
  - Счетчик найденных питомцев
  - Цветовая кодировка статусов (оранжевый, красный, синий, фиолетовый)
  - Бадж "СРОЧНО" для срочных случаев
  - Отображение города/региона и контактной информации
- **Улучшенный дизайн админ-панелей (стиль 2GIS)**
  - Увеличена ширина контента: 1400px → 1600px
  - Увеличена ширина бокового меню: 200px → 240px
  - Светлый фон меню (white вместо gray-100)
  - Увеличен размер шрифта в меню: text-sm → text-base
  - Применено ко всем сервисам: Admin, PetBase, Shared
  - Улучшена страница статистики ЗооБазы с градиентными карточками
- **Обновление навигации**
  - Переименована "Лента" → "Метки"
  - Добавлена вкладка "Каталог" в главное меню
  - Скрыты вкладки не для MVP: Зоожурнал, Афиша, Зоомаркет, Учебный центр, Фонд "ЗооПомощь"

### Fixed
- Исправлена структура Pet в Go handlers (добавлены недостающие поля)
- Исправлен go.mod: github.com/golang-jwt/jwt/v5 перемещен в прямые зависимости
- Исправлен роутинг в PetBase (корневой роут `/` перемещен в конец)
- Исправлен SQL запрос в CatalogHandler (убраны несуществующие поля)

### Changed
- Обновлена документация MVP с детальным описанием каталога

## [0.4.0] - 2024-12-30 🏢

### Added
- **Система профилей организаций**
  - База данных: 3 таблицы (`organizations`, `organization_members`, `organization_pets`)
  - Таблица `organizations` с 37 полями (юридическая информация, контакты, адрес, координаты)
  - 6 типов организаций: приют, ветклиника, зоомагазин, фонд, кинологический центр, другое
  - Система ролей участников (owner, admin, moderator, member)
  - Права доступа (can_post, can_edit, can_manage_members)
  - Настройки приватности профиля
  - Статус верификации организаций
  - Go модели и CRUD handlers
  - TypeScript API клиент
  - Страница профиля организации `/org/[id]` (3-колоночный layout)
  - Документация `docs/ORGANIZATIONS.md` с описанием системы верификации
- **Страница списка организаций `/org`**
  - 2-колоночный layout (средняя колонка 600px, правая 320px)
  - Фильтры: тип организации, регион, город
  - Поиск по названию и описанию
  - Счетчик найденных организаций
  - Автоопределение города пользователя через геолокацию
  - Автоматическая фильтрация по городу пользователя при первом заходе
  - Сохранение города в localStorage
  - Горизонтальные карточки организаций с логотипом, типом, описанием, городом
  - Значок верификации для проверенных организаций
- **Импорт реальных организаций**
  - Скрипты импорта из 2GIS API (`import-from-2gis.js`)
  - Скрипт импорта по ИНН через DaData API (`import-by-inn.js`)
  - Автоматическое определение города через building_id
  - Импортировано 30 реальных организаций из Удмуртии (Ижевск, Сарапул, Сигаево)
  - Типы: ветклиники, зоомагазины, приюты, фонды
  - 3 организации с ИНН и верификацией
- **Справочник городов России**
  - Полный справочник: 86 регионов, 1048 городов
  - JSON файл `russia-cities-full.json` с официальными названиями
  - Скрипт генерации справочника `create-full-cities-list.js`
  - Динамическая загрузка справочника на фронтенде
  - Фильтрация городов по выбранному региону

### Changed
- Обновлены все организации в базе: добавлено поле `address_region = 'Удмуртская Респ'`
- Backend API теперь возвращает `address_region` и `address_city` для фильтрации

### Technical
- Миграция `019_create_organizations.sql`
- Интеграция с 2GIS API для поиска организаций
- Интеграция с DaData API для получения данных по ИНН
- Интеграция с Nominatim (OpenStreetMap) для обратного геокодирования
- Использование `useRef` для предотвращения race conditions при автоустановке фильтров

## [0.3.5] - 2024-12-29 🐾

### Added
- **Страница питомца с постами**
  - Отдельная страница `/pets/[id]` для просмотра информации о питомце
  - Обложка с аватаром питомца (как в профиле пользователя)
  - 3-колоночный layout с `gap-2.5` между блоками
  - Левая колонка с отдельными карточками:
    - Фото питомца (большое изображение)
    - "О питомце" (возраст, пол, окрас)
    - "Медицинская информация" (прививки, стерилизация, чипирование)
    - "Посты с {имя питомца}" - список всех постов, где упоминается питомец
  - Правая колонка с карточками:
    - "Действия" (в избранное, поделиться)
    - "Информация" (кличка, вид, порода, пол, возраст)
    - "Здоровье" (краткая медицинская информация)
  - Клик по чипу питомца в посте открывает страницу питомца (не модальное окно)
- **API endpoint для постов питомца**
  - `GET /api/posts/pet/:id` - получение всех постов, где упоминается питомец
  - Использует таблицу `post_pets` для быстрого поиска
  - Возвращает только опубликованные посты (`status = 'published'`)
  - Сортировка по дате создания (новые первые)

### Changed
- **Обновлены TypeScript типы**
  - `Post` интерфейс теперь соответствует новой структуре с `attached_pets`, `attachments`, `tags`
  - `Pet` интерфейс дополнен полями: `breed`, `gender`, `birth_date`, `color`, `is_sterilized`, `is_vaccinated`, `chip_number`
  - `Poll` интерфейс дополнен полями: `allow_vote_changes`, `anonymous_voting`, `total_voters`, `is_expired`, `voters`
  - Добавлен интерфейс `PollVoter` для отображения проголосовавших
  - Добавлен интерфейс `Attachment` для медиа-вложений
- **PostCard теперь принимает опциональный `comments_count`**
  - Исправлена совместимость типов между компонентами

### Technical
- Добавлена функция `getPetPosts` в `handlers/posts.go`
- Добавлен handler `PetPostsHandler` для обработки запросов постов питомца
- Добавлен метод `postsApi.getPetPosts(petId)` в frontend API клиент

## [0.3.4] - 2024-12-29 🐾

### Added
- **Добавление питомцев из профиля пользователя**
  - Компонент `AddPetModal` для создания питомцев
  - Кнопка "Добавить" в разделе "Питомцы" на странице профиля
  - Форма с полями: имя (обязательно), вид (обязательно), порода, пол, дата рождения, окрас, статус
  - Автоматическое обновление списка питомцев после добавления
  - Интеграция с PetBase API (localhost:8100)
- **Отображение владельца в карточке питомца (ЗооБаза)**
  - Загрузка полной информации о владельце из Main API
  - Карточка владельца с аватаркой, полным именем (имя + фамилия), email и ID
  - Кликабельная ссылка на профиль владельца на основном сайте
  - Красивое оформление с hover эффектом и иконкой внешней ссылки
  - Обработка случаев, когда у пользователя нет фамилии
- **Прикрепление питомцев к постам**
  - Исправлена загрузка списка питомцев при создании поста
  - Модальное окно "Добавить питомца" теперь показывает реальный список питомцев пользователя
  - Используется endpoint `/api/pets/user/:id` вместо `/api/pets`

### Fixed
- **API endpoint `/api/users/:id` теперь возвращает полную информацию**
  - Добавлены поля: `last_name`, `avatar`, `bio`, `location`, `phone`
  - Исправлен SQL запрос в `main/backend/handlers/users.go`
  - Теперь возвращает 9 полей вместо 4
- **API endpoint `/api/users` (список пользователей)**
  - Добавлены поля: `last_name`, `avatar`
  - Улучшена консистентность данных между endpoints

### Changed
- **Компонент CreatePost**
  - Функция `loadPets()` теперь использует `user.id` для загрузки питомцев конкретного пользователя
  - Добавлена проверка наличия `user` перед загрузкой
- **Карточка питомца в ЗооБазе**
  - Раздел "Владелец и куратор" теперь показывает полную информацию о владельце
  - Добавлен state `owner` для хранения данных пользователя
  - Запрос к Main API с `credentials: 'include'` для передачи JWT токена

## [0.3.3] - 2024-12-29 🔒

### Added
- **Полноценная система питомцев в ЗооБазе (PetID)**
  - Таблица `pets` с 44 полями для полной информации о питомце
  - Миграция `011_create_pet_cards.sql` - создание базовой структуры
  - Миграция `012_add_pet_passport_fields.sql` - поля ветеринарного паспорта
  - Миграция `013_create_medical_records_tables.sql` - медицинские записи
  - Миграция `014_add_tag_number.sql` - бирка ОСВВ для приютов
  - Миграция `015_add_curator_and_location.sql` - куратор и местоположение
  - Миграция `016_add_sterilization_date.sql` - дата стерилизации
- **Детальная карточка питомца** (`petbase/frontend/app/pets/[id]/page.tsx`)
  - Hero Section с фото, именем, ID и датой создания
  - 6 вкладок с полной информацией:
    - **Основная информация** - вид, порода, пол, возраст, окрас, размер, вес
    - **Владелец и куратор** - информация о владельце или кураторе, контакты
    - **Идентификация** - микрочип, тату, бирка ОСВВ, ветпаспорт
    - **Родословная** - номер родословной, организация регистрации (РКФ, FCI, WCF, TICA)
    - **Медицинская информация** - пол, возраст, вес, группа крови, стерилизация с датой, аллергии, хронические заболевания, текущие препараты
    - **История** - все события жизни питомца (placeholder)
  - Визуальные индикаторы: зелёные галочки для заполненных полей, серые для пустых
  - Цветные блоки для важной информации (микрочип, тату, бирка)
  - Большие числа для идентификаторов
- **Система владения и кураторства**
  - Животное может иметь ЛИБО владельца, ЛИБО куратора, ЛИБО никого
  - Владелец - постоянный хозяин (животное живёт дома)
  - Куратор - временный опекун (животное на улице, передержке или в приюте)
  - Поля: `owner_name`, `owner_address`, `owner_phone`, `owner_email`
  - Поля: `curator_id`, `curator_name`, `curator_phone`
  - Документация в `docs/CURATOR_AND_LOCATION.md`
- **Система местоположения питомца**
  - 4 типа локации: `home` (дома), `street` (на улице), `foster` (передержка), `shelter` (приют)
  - Поля: `location`, `foster_address`, `shelter_name`
  - Логика: владелец → дома, куратор → улица/передержка/приют
- **Ветеринарный паспорт**
  - Все поля из бумажного ветпаспорта
  - Идентификация: микрочип, тату, бирка ОСВВ
  - Владелец: ФИО, адрес, телефон, email
  - Медицина: группа крови, аллергии, хронические заболевания, текущие препараты
  - Родословная: номер, организация регистрации
  - Особые приметы и отличительные черты
  - Документация в `docs/PET_PASSPORT.md`
- **Бирка ОСВВ (Отлов-Стерилизация-Вакцинация-Возврат)**
  - Поле `ear_tag_number` для номера бирки в ухе
  - Используется в приютах по программе ОСВВ
  - Отображается в разделе "Идентификация"
- **Медицинские записи** (таблицы созданы, функционал в разработке)
  - `pet_vaccinations` - вакцинации
  - `pet_parasite_treatments` - обработки от паразитов
  - `pet_surgeries` - операции
  - `pet_medical_history` - история визитов к ветеринару
- **Список питомцев в ЗооБазе**
  - Таблица со всеми питомцами пользователя
  - Колонки: ID, Имя, Вид, Порода, Пол, Возраст, Статус
  - Кликабельные строки для перехода к детальной карточке
  - Кнопка "Добавить питомца"
- **Справочники ЗооБазы**
  - Таблица `species` - виды животных (собаки, кошки, птицы и т.д.)
  - Таблица `breeds` - породы с привязкой к видам
  - Таблица `pet_cards` - информационные карточки о породах
  - Handlers: `species.go`, `breeds.go`, `cards.go`
  - Публичные API endpoints (без аутентификации)
- **Система безопасности ЗооБазы (PetID)**
  - JWT аутентификация с поддержкой Bearer токенов
  - Middleware `RequireAuth` для защищённых routes
  - Middleware `OptionalAuth` для опциональной аутентификации
  - Функции `GenerateToken(userID)` и `ValidateToken(token)`
  - Проверка владельца при update/delete операциях
  - User ID берётся из JWT токена, а не из запроса (защита от подделки)
  - CORS с проверкой разрешённых origins (настраивается через ALLOWED_ORIGINS)
  - Блокировка запросов с неразрешённых origins (403 Forbidden)
  - Логирование заблокированных запросов
  - Временная поддержка X-User-ID заголовка для разработки
- **API клиент для основного сайта**
  - Файл `main/frontend/lib/petbase-api.ts` с полным API клиентом
  - Поддержка JWT токенов в заголовке Authorization
  - Методы: `setAuthToken()`, `clearAuthToken()`, `setUserId()`
  - Обработка ошибок 401 (Unauthorized) и 403 (Forbidden)
  - Все CRUD операции для питомцев с аутентификацией
- **Документация по безопасности**
  - `docs/SECURITY.md` - полная документация по безопасности
  - `docs/PETBASE_INTEGRATION.md` - архитектура интеграции
  - `petbase/backend/README_AUTH.md` - инструкция по аутентификации
  - `petbase/TESTING_SECURITY.md` - тесты безопасности
  - `petbase/INTEGRATION_SUMMARY.md` - сводка по интеграции
  - `petbase/backend/.env.example` - пример конфигурации
  - `docs/PET_PASSPORT.md` - документация ветпаспорта
  - `docs/CURATOR_AND_LOCATION.md` - документация системы владения
- **Обновлена документация API**
  - `docs/API_ENDPOINTS.md` обновлён с информацией о безопасности
  - Разделение на публичные и защищённые endpoints
  - Примеры запросов с JWT токенами и X-User-ID
  - Коды ошибок безопасности (401, 403, 429)
  - Рекомендации по безопасности для клиентов

### Changed
- **Защищённые endpoints ЗооБазы**
  - `/api/pets` - теперь требует аутентификацию
  - `/api/pets/:id` - требует аутентификацию для GET, PUT, DELETE
  - `/api/pets/user/:userId` - требует аутентификацию
  - `/api/pets/status/:status` - требует аутентификацию
- **Публичные endpoints** (без аутентификации):
  - `/api/health` - проверка здоровья
  - `/api/species` - список видов
  - `/api/breeds` - список пород
  - `/api/cards` - информационные карточки
- **Структура Pet**
  - Поле `SterilizationDate` изменено на `*string` (nullable)
  - Исправлена обработка NULL значений в SQL запросах
- **Frontend ЗооБазы**
  - Добавлен X-User-ID заголовок во все запросы к API
  - Страница списка питомцев (`petbase/frontend/app/page.tsx`)
  - Страница детальной карточки (`petbase/frontend/app/pets/[id]/page.tsx`)
- **CORS настройки**
  - Разрешённые origins: localhost:3000, localhost:4100
  - Настраивается через переменную окружения ALLOWED_ORIGINS
  - Блокировка неразрешённых origins с логированием

### Fixed
- Исправлена ошибка компиляции в `handlers/pets.go` (дублирование полей в Scan)
- Исправлена обработка NULL значений для `sterilization_date`
- Исправлена ошибка "no new variables on left side of :=" в updatePet
- Исправлены все SQL Scan операции (удалено дублирование `&pet.IsSterilized`)
- Питомцы теперь отображаются на фронтенде ЗооБазы
- Детальные карточки питомцев открываются без ошибок

### Security
- ✅ JWT аутентификация реализована
- ✅ Проверка владельца при изменении/удалении
- ✅ User ID из токена, а не из запроса
- ✅ CORS с проверкой origins
- ✅ Все тесты безопасности проходят успешно
- ⏳ Rate limiting (в разработке)
- ⏳ Валидация входных данных (в разработке)

### Технологии
- Добавлено: github.com/golang-jwt/jwt/v5 для JWT аутентификации
- Добавлено: Air для hot-reload в petbase/backend

## [0.3.2] - 2025-12-28 🔧

### Fixed
- **Исправлена работа всех полей профиля**
  - Обновлен `updateProfile` в `api.ts` - добавлены все поля (last_name, настройки приватности)
  - Исправлен `profile.go` handler - убраны avatar и cover_photo из обычного сохранения профиля (для них есть отдельные endpoints)
  - Добавлен `refreshUser()` после сохранения - данные в AuthContext обновляются после редактирования
  - Добавлена зависимость от `currentUser` в useEffect на странице профиля - страница автоматически перезагружается при изменении данных
  - Все поля теперь корректно сохраняются и отображаются: имя, фамилия, биография, телефон, город, настройки приватности (5 полей)

### Added
- **Система друзей (Backend)**
  - Таблица `friendships` для хранения связей между пользователями
  - Статусы дружбы: pending (ожидание), accepted (принято), rejected (отклонено), blocked (заблокирован)
  - API endpoints:
    - `POST /api/friends/send` - отправить запрос в друзья
    - `POST /api/friends/accept` - принять запрос в друзья
    - `POST /api/friends/reject` - отклонить запрос в друзья
    - `DELETE /api/friends/remove` - удалить из друзей
    - `GET /api/friends` - получить список друзей
    - `GET /api/friends/requests` - получить входящие запросы
    - `GET /api/friends/status` - проверить статус дружбы с пользователем
  - Модели: Friendship, FriendshipResponse, FriendRequest, FriendActionRequest
  - Индексы для быстрого поиска по user_id, friend_id, status
  - Защита от добавления себя в друзья
  - Проверка существующих запросов
- **Страница редактирования профиля**
  - Трехколоночный layout: 2 колонки для формы, 1 для меню разделов
  - 3 раздела с динамическим переключением:
    - **Профиль** - обложка, аватар, имя, фамилия, краткая информация (все в одном месте)
    - **Контакты** - телефон, город, email (read-only)
    - **Приватность** - настройки видимости и безопасности
  - Индикаторы заполненности разделов (зеленая/серая точка)
  - Динамические заголовки и подсказки для каждого раздела
  - Информационный блок с советами в правой колонке
- **Настройки приватности**
  - Раздел "Приватность" на странице редактирования профиля
  - 5 настроек приватности:
    - **Видимость профиля** - кто может видеть профиль (все/друзья/только я)
    - **Видимость телефона** - кто может видеть номер телефона (все/друзья/никто)
    - **Видимость email** - кто может видеть email (все/друзья/никто)
    - **Личные сообщения** - кто может писать сообщения (все/друзья/никто)
    - **Онлайн-статус** - показывать или скрывать статус онлайн (да/нет)
  - Миграция базы данных: добавлены колонки `profile_visibility`, `show_phone`, `show_email`, `allow_messages`, `show_online`
  - Значения по умолчанию: профиль публичный, контакты скрыты, сообщения от всех, статус виден
  - Обновлены API endpoints для сохранения настроек приватности
- **Toast уведомления**
  - Красивые всплывающие уведомления в правом верхнем углу
  - 4 типа: success (зеленый), error (красный), warning (желтый), info (синий)
  - Автоматическое исчезновение через 5 секунд
  - Кнопка закрытия вручную
  - Плавная анимация появления/исчезновения
  - ToastContext и useToast hook для удобного использования
  - Заменены все alert() на toast уведомления
- **Имя и Фамилия в профиле**
  - Разделение поля "Имя" на "Имя" и "Фамилия"
  - Два отдельных поля на странице редактирования профиля (в одной строке)
  - Миграция базы данных: добавлена колонка `last_name` в таблицу `users`
  - Автоматическое разделение существующих имен (если есть пробел)
  - Helper функция `getFullName()` для отображения полного имени
  - Обновлены API endpoints для работы с фамилией
  - Полное имя отображается во всех компонентах: Header, Profile, Posts, Comments
- **Правила миграций базы данных**
  - Создан steering файл `.kiro/steering/database-migrations.md`
  - Критическое правило: всегда сохранять существующие данные
  - Примеры правильных и неправильных миграций
  - Checklist для безопасных миграций
  - Инструкции по резервному копированию и восстановлению
- **Загрузка аватара и обложки профиля**
  - Кнопки загрузки на странице редактирования профиля
  - Превью изображений в реальном времени
  - Валидация типа файла (только изображения)
  - Валидация размера (максимум 10MB)
  - Индикаторы загрузки (спиннеры)
  - Автоматическое обновление данных пользователя после загрузки
  - API endpoints: `POST /api/profile/avatar`, `POST /api/profile/cover`
  - Хранение файлов: `uploads/users/{user_id}/avatars/`, `uploads/users/{user_id}/covers/`
  - Метод `refreshUser()` в AuthContext для обновления данных
  - Полное логирование всех операций загрузки (успех, ошибки, предупреждения)
  - Helper функция `getMediaUrl()` для корректного отображения медиа
  - Раздача статических файлов из папки `uploads/` через backend
  - Кнопки удаления аватара и обложки с подтверждением
  - API endpoints: `DELETE /api/profile/avatar/delete`, `DELETE /api/profile/cover/delete`
  - Красивый градиентный фон по умолчанию для обложки (синий → фиолетовый → розовый) с инициалом пользователя
  - Обложка не отображается, если не загружена (вместо градиента по умолчанию)
  - Кнопка "Добавить обложку профиля" на странице редактирования, когда обложки нет
- **Галерея медиа в профиле**
  - Вкладки: Публикации, Медиа (объединяет фото и видео)
  - Отображение всех загруженных фото и видео пользователя
  - Фильтры: Все / Фото / Видео
  - Сетка 3x3 с превью
  - Lightbox для просмотра в полном размере с поддержкой видео
  - Воспроизведение видео в lightbox с контролами
  - Информация о файле при наведении
  - Компонент `MediaGallery` для отображения галереи

### Fixed
- **Исправлена ошибка при обновлении страницы профиля**
  - Исправлена обработка NULL значений в полях `bio`, `phone`, `location`, `avatar`, `cover_photo` в `MeHandler`
  - Использование `sql.NullString` для корректного сканирования nullable полей из базы данных
  - Теперь жесткое обновление страницы (Shift+Cmd+R) не выбрасывает на страницу авторизации
  - Добавлена проверка `typeof window === 'undefined'` в `AuthContext` для предотвращения SSR запросов
- **Исправлена независимость удаления аватара и обложки**
  - Изменен порядок операций: сначала `refreshUser()`, затем обновление локального превью
  - Удаление обложки больше не влияет на аватар и наоборот
  - Компонент `MediaLightbox` для просмотра фото и видео
- **Статистика медиа**
  - Количество фотографий и видео
  - Общий размер файлов
  - Отображается только в своем профиле
  - Компонент `MediaStats`
- **API для медиа**
  - `GET /api/media/user/:userId` - получить все медиа пользователя
  - Query параметр `type` для фильтрации (photo/video)
  - `GET /api/media/stats` - статистика медиа текущего пользователя

### Changed
- Профиль пользователя теперь имеет 2 вкладки: Публикации и Медиа
- Вкладка "Медиа" объединяет фото и видео в одном месте
- Все медиа из постов автоматически сохраняются в личное облако
- Счетчики фильтров всегда показывают реальное количество файлов
- Фильтрация медиа происходит на клиенте для корректного отображения счетчиков
- Аватары теперь отображаются корректно во всех компонентах (Header, Feed, CreatePost, Profile)
- Компонент `Image` из Next.js заменен на обычный `img` для аватаров и обложек
- Все компоненты используют helper функцию `getMediaUrl()` для формирования URL медиа
- Backend теперь раздает статические файлы из папки `uploads/` через FileServer

### Fixed
- Исправлена проблема с неправильными счетчиками в фильтрах медиа
- Исправлено воспроизведение видео в lightbox (был PhotoLightbox, стал MediaLightbox)
- Исправлено отображение аватаров (добавлен полный URL с http://localhost:8000)
- Добавлено поле `updated_at` в таблицу `users`

## [0.3.1] - 2025-12-28 🎬

### Added
- **Поддержка видео**
  - Загрузка видео файлов (mp4, webm, quicktime, mpeg)
  - Максимальный размер видео: 100MB
  - Отображение видео в постах с нативным плеером
  - Видео в карусели вместе с фото
  - Индикатор "Видео" на превью
- **Chunked Upload для видео**
  - Загрузка видео по частям (chunks) по 5MB
  - Реальный прогресс загрузки каждой части
  - Отображение "Загрузка 3/10 частей"
  - Возможность возобновления при обрыве связи (в будущем)
  - API endpoints: `/api/media/chunked/initiate`, `/upload`, `/complete`
  - Handler `ChunkedUploadHandler` с методами InitiateUpload, UploadChunk, CompleteUpload
  - Временное хранилище chunks в `uploads/temp/{upload_id}/`
- **Оптимизация видео при загрузке (сохраняет разрешение)**
  - Автоматическое сжатие видео с помощью FFmpeg
  - Сохранение оригинального разрешения (FullHD → FullHD, 720p → 720p)
  - Агрессивное сжатие: CRF 28, preset medium
  - Снижение битрейта аудио до 64k
  - Ограничение FPS до 30
  - Экономия 50-90% размера файла
  - Оптимизация для веб-воспроизведения (faststart)
- **Асинхронная оптимизация видео**
  - Видео сохраняется в БД сразу после загрузки (без ожидания оптимизации)
  - Оптимизация выполняется в фоновом режиме (goroutine)
  - Пользователь видит видео немедленно, оптимизация происходит незаметно
  - Индикатор "🎬 Оптимизация в фоне..." для видео в процессе обработки
  - База данных автоматически обновляется после завершения оптимизации
  - Флаг `optimizing: true` в API ответе для отслеживания статуса
- **UX улучшения загрузки видео**
  - Превью видео (первый кадр) показывается сразу после выбора
  - Круговой прогресс-бар с процентами в правом верхнем углу
  - Индикатор статуса: "📤 Загрузка 5/10 частей" или "🎬 Оптимизация..."
  - Полупрозрачное превью во время обработки
  - Детальное логирование в консоли для отладки
  - Функция `createVideoThumbnail()` для генерации превью из первого кадра
- **Облачное хранилище медиа**
  - Структура хранения: `uploads/users/{user_id}/photos|videos/YYYY/MM/`
  - Таблица `user_media` с метаданными
  - API endpoints: upload, get, delete, stats
  - Поддержка фото (10MB) и видео (100MB)
  - Автоматическая генерация UUID имен файлов
- **Карусель фото/видео**
  - Квадратный формат (1:1) как в Instagram
  - Навигация: стрелки, точки, клавиатура (← →), свайпы
  - Счетчик медиа (1/5) на фото
  - Адаптивное отображение разных соотношений сторон
  - Поддержка видео в карусели
- **Модальное окно выбора питомцев**
  - Список всех питомцев пользователя
  - Множественный выбор питомцев
  - Превью выбранных питомцев в виде чипсов
  - Заглушка "У вас пока нет питомцев"

### Changed
- Загрузка видео теперь использует chunked upload вместо обычной загрузки
- Оптимизация видео больше не блокирует пользователя
- Видео доступно для просмотра сразу после загрузки

### Technical Details
- Backend: Go handlers для chunked upload в `main/backend/handlers/chunked_upload.go`
- Frontend: React hook `useChunkedUpload` для управления загрузкой по частям
- FFmpeg используется для оптимизации с параметрами: CRF 28, preset medium, audio 64k
- Goroutines для асинхронной обработки видео в фоне

## [0.3.0] - 2025-12-27 🐾

### Added
- **Универсальная система постов**
  - Модель `Post` с поддержкой вложений, меток, планирования
  - Поля: `author_type`, `attached_pets`, `attachments`, `tags`, `status`, `scheduled_at`
  - Типы статусов: `published`, `scheduled`, `draft`
  - Прикрепление питомцев к постам (массив `attached_pets`)
  - Медиа-вложения: фото и видео (структура `Attachment`)
  - Метки/теги: "ищет дом", "потерян", "найден"
  - Модальное окно создания поста с полным функционалом
  - Кнопка "Что нового?" для быстрого создания поста
- **Черновики и планирование постов**
  - Сохранение постов как черновиков (status: "draft")
  - Просмотр и управление черновиками
  - Загрузка черновика для редактирования
  - Удаление черновиков
  - Планирование публикации на определенную дату и время
  - Календарь выбора даты с навигацией по месяцам
  - Выбор времени публикации
  - Визуальная индикация запланированных постов
- **Настройки ответов на посты**
  - Выбор кто может отвечать: "Кто угодно", "Подписчики", "Подписки", "Упомянутые"
  - Опция "Проверять и одобрять ответы"
  - Dropdown меню с настройками
- **Система голосований (Polls)** - полноценные опросы в постах
  - Создание опросов с 2-10 вариантами ответа
  - Настройки: множественный выбор, изменение голоса, анонимность, срок действия
  - Отображение результатов с процентами и прогресс-барами
  - Список проголосовавших пользователей (для неанонимных опросов)
  - Компактное отображение voters в виде "pills" с аватарами
  - Возможность отменить голос (если разрешено настройками)
  - Таблицы БД: `polls`, `poll_options`, `poll_votes`
  - Компоненты: `PollCreator`, `PollDisplay`
  - Интеграция опросов в систему постов
- **Улучшения системы комментариев**
  - Комментарии теперь отображаются сразу после добавления (без перезагрузки)
  - Умный скролл: сохранение позиции при добавлении комментария
  - Offset 100px для новых корневых комментариев (видимость нового комментария)
  - В модальном окне комментарии всегда видны (убрана кнопка "Показать/Скрыть")
  - Sticky input форма внизу модального окна
  - Синий цвет (#1B76FF) для текста "Ответ для [username]"
  - Унифицированное управление состоянием комментариев в PostModal
- **URL-навигация для постов**
  - Формат URL: `/?metka=123` для открытия конкретного поста
  - Автоматическое открытие модального окна при переходе по ссылке
  - Обновление URL при открытии/закрытии модального окна
  - Поддержка прямых ссылок на посты

### Changed
- **Полная переработка системы постов**
  - Переход от простых постов к универсальной системе в стиле Threads
  - Модальное окно вместо inline-редактора
  - Расширенная модель данных с поддержкой вложений и меток
  - Новый UI для создания постов (минималистичный, как в Threads)
- Модальное окно поста теперь всегда показывает комментарии
- Форма ввода комментария закреплена внизу (sticky footer)
- Улучшена синхронизация счетчика комментариев
- Оптимизирован процесс добавления комментариев (без моргания)
- Кнопка создания поста теперь открывает модальное окно
- Аватары пользователей с fallback на UserIcon
- Автоматическое изменение высоты textarea при вводе текста

### Fixed
- Исправлена проблема с отображением voters в опросах
- Исправлена функция отмены голоса
- Исправлено дублирование обработчиков в PostComments
- Исправлена проблема с отображением имени пользователя в "Ответ для"
- Исправлен скролл при добавлении комментариев (теперь остается на месте)
- Исправлено закрытие модального окна по ESC и клику на backdrop

## [0.2.0] - 2025-12-14 🐾

### Added
- **ЗооБаза (petbase)** - новый микросервис справочника домашних животных
  - Backend API на Go (порт 8100) с полным CRUD для видов, пород и карточек
  - Frontend на Next.js (порт 4100) с AdminLayout
  - SSO интеграция с проверкой прав суперадмина
  - Таблицы БД: `species`, `breeds`, `pet_cards`
  - Тестовые данные в `petbase/backend/seed.sql`
- Переиспользуемый шаблон админ-панели в `shared/src/components/admin/`
  - Компонент `AdminLayout` с header, sidebar и адаптивностью
  - Виджеты: `StatsWidget`, `TableWidget`, `ChartWidget`
  - Полная документация в `shared/src/components/admin/README.md`

### Fixed
- Исправлена проблема с шириной контента в админках (overflow-x)
- `AdminLayout`: изменён `w-screen` на `w-full` для корректного отображения
- `TableWidget`: добавлен `max-w-full` и правильная обработка overflow
- CSS модули админки: убран `width: 100vw`, добавлен `width: 100%`
- Таблицы теперь корректно скроллятся горизонтально на мобильных устройствах

### Changed
- Обновлён скрипт `./run` для запуска ЗооБазы
- CORS в admin backend настроен для порта 4100
- Виджеты: `StatsWidget`, `TableWidget`, `ChartWidget`
- Документация по использованию админ-шаблона
- Мобильная адаптивность для админки (гамбургер-меню, overlay)

### Changed
- Улучшен дизайн админки: виджеты с тенями, анимации, градиенты
- Максимальная ширина контента 1400px для больших экранов
- Увеличены отступы и скругления виджетов
- Добавлены hover-эффекты и transitions

## [0.1.3] - 2025-12-14

### Fixed
- **Критическое исправление:** Tailwind CSS v4 не применял стили к компонентам из @pet/shared
- Исправлены пути к database модулю после реструктуризации проекта
- `main/backend/go.mod`: путь к database изменен с `../database` на `../../database`
- `database/db.go`: путь к БД изменен на `../../database/data.db` для корректной работы из `main/backend`
- Компоненты с Tailwind классами теперь копируются напрямую в каждый сервис
- AuthForm скопирован в main/frontend и admin/frontend
- Страница авторизации админки теперь отображается корректно
- Backend сервисы теперь запускаются без ошибок после переноса в папки main/ и admin/

### Changed
- Обновлена архитектура работы с shared компонентами
- Tailwind config очищен от неработающих путей к node_modules
- Компоненты импортируются локально вместо @pet/shared

### Added
- Документация `shared/README.md` с объяснением проблемы Tailwind v4
- Steering правило `.kiro/steering/shared-components.md` для работы с shared компонентами
- Инструкции по копированию компонентов между сервисами

### Technical Details
- Tailwind CSS v4 не сканирует файлы из node_modules по умолчанию
- Решение: копирование компонентов вместо импорта из пакета
- TypeScript типы, API клиенты и хуки можно импортировать из @pet/shared
- React компоненты с Tailwind классами нужно копировать
- После реструктуризации проекта все относительные пути обновлены

## [0.1.2] - 2025-12-12

### Added
- **Полноценная система комментариев** к постам
- Таблица `comments` в базе данных с поддержкой вложенности
- Вложенные комментарии (1 уровень, как в ВК)
- Кнопка "Ответить" под каждым комментарием
- Упоминание пользователя синим цветом при ответе (@Имя)
- Индикатор "Ответ для [Имя]" при написании ответа
- Кнопка отмены ответа
- Удаление комментариев (только автор)
- Форматирование времени комментариев ("только что", "5 мин назад")
- Счетчик комментариев обновляется в реальном времени
- Кнопка "Показать/Скрыть комментарии"
- Поля `parent_id` и `reply_to_user_id` в таблице comments
- Модель `Comment` с поддержкой вложенности и ответов
- API endpoints для комментариев (GET, POST, DELETE)
- Компонент `PostComments` с полным функционалом
- Callback `onCountChange` для обновления счетчика в родительском компоненте

### Changed
- Отказ от концепции "стены" в пользу комментариев
- Лента теперь использует комментарии вместо записей на стене
- Профиль пользователя показывает только его посты с комментариями
- Счетчик комментариев теперь динамический (обновляется без перезагрузки)
- Backend возвращает дерево комментариев (корневые + replies)
- Страница редактирования профиля вынесена на отдельный URL `/profile/edit`

### Fixed
- Адаптивность главной страницы на маленьких экранах
- Feed теперь занимает 100% ширины на мобильных устройствах
- Header адаптирован для мобильных (скрыт CityDetector, сокращен placeholder)
- Профиль пользователя адаптирован (обложка, аватар, кнопки)
- Табы в профиле адаптивны (overflow-x-auto)
- Layout уменьшен padding на маленьких экранах

### Removed
- Удалена концепция "стены" (записи на чужих профилях)
- Удален компонент `WriteOnWall`
- Удален handler `WallPostHandler`
- Удалено поле `wall_author_id` из таблицы posts

## [0.1.1] - 2025-12-12

### Fixed
- **Критическое исправление:** Авторизация при создании постов теперь работает корректно
- Middleware теперь извлекает `userID` из JWT токена и добавляет его в контекст запроса
- Handler создания постов теперь получает `userID` из контекста
- Исправлено "мигание" кнопки "Войти" при обновлении страницы
- Добавлен skeleton loader во время проверки авторизации
- UserMenu теперь проверяет флаг `isLoading` перед отображением

### Changed
- AuthMiddleware теперь парсит claims из JWT и добавляет `userID` в `context.Context`
- UserMenu показывает placeholder (серый кружок) во время загрузки состояния авторизации

## [0.1.0] - 2025-12-12

### Added
- **Админ-панель (Frontend)** на Next.js
- Страница входа в админ-панель (`/auth`) с формой авторизации
- Дашборд с вкладками: Пользователи, Посты, Статистика, Логирование
- Сворачиваемое боковое меню с иконками Heroicons
- Таблицы с данными пользователей и постов
- Модерация постов (одобрение/отклонение/удаление)
- Управление пользователями (просмотр/удаление)
- Статистика платформы (пользователи, посты, активность)
- **Система логирования** для всей платформы
- Таблица `system_logs` для хранения всех событий системы
- Вкладка "Логирование" в админ-панели с отображением всех логов
- Автоматическое логирование входа пользователей
- Функция `logSystemEvent()` для логирования событий
- Уровни логов: info, warning, error, debug
- Категории логов: auth, user, post, pet, profile, admin, system, security
- Цветные бейджи для разных типов действий в логах
- Air (hot reload) для Admin Backend
- Tailwind CSS в админ-панели
- Heroicons для всех иконок в админ-панели

### Changed
- Admin Backend теперь запускается с Air для hot reload
- Порт Admin Frontend: 4000 (вместо "в разработке")
- Серый фон у сайдбара, белый у основного контента (инверсия)
- Активный пункт меню выделяется белым фоном с тенью
- Боковое меню теперь широкое (200px) с текстом и иконками
- Кнопка сворачивания меню перенесена вниз
- SQL запросы в Admin Backend адаптированы под SQLite (вместо PostgreSQL)
- Обработка NULL значений в SQL запросах через `sql.NullString`
- Endpoint `/api/admin/stats` переименован в `/api/admin/stats/overview`
- Цвет текста на главном сайте зафиксирован (#171717) независимо от темной темы

### Fixed
- Исправлена проблема со светлым текстом при вводе на главном сайте
- Исправлены SQL placeholder'ы ($1, $2 → ?) для SQLite
- Исправлена обработка пустых массивов в API логов
- Исправлена структура интерфейса Log (добавлено поле admin_name)

### Технологии
- Добавлено: Tailwind CSS 4.x в админ-панели
- Добавлено: Heroicons 2.x в админ-панели
- Добавлено: Air для Admin Backend

## [0.0.9] - 2025-12-12

### Added
- **SSO (Single Sign-On)** архитектура для всех микросервисов
- Поле `roles` в JWT токенах для управления правами доступа
- Поля `exp` и `iat` в JWT токенах
- Endpoint `/api/auth/verify` для проверки токенов микросервисами
- Функция `getUserRoles()` для получения ролей пользователя из таблицы admins
- Микросервис админ-панели в папке `admin/`
- Backend для админ-панели (Go, порт 8081)
- Таблицы `admins` и `admin_logs` в базе данных
- Middleware для проверки прав суперадмина (с поддержкой SSO)
- API endpoints для управления пользователями
- API endpoints для модерации постов
- API endpoints для статистики
- API endpoints для просмотра логов
- Скрипт `create-superadmin.sh` для назначения первого суперадмина
- Скрипт `tests/debug-sso.sh` для отладки SSO
- Документ `docs/SSO_ARCHITECTURE.md` с архитектурой SSO системы
- Документ `docs/SSO_FLOW.md` с визуальной схемой SSO
- Документ `admin/SSO_SETUP.md` с инструкцией по настройке
- Документ `admin/IMPLEMENTATION_STATUS.md` со статусом реализации
- Логирование всех действий администраторов

### Changed
- JWT токены теперь включают массив `roles` для определения прав доступа
- Cookie `auth_token` теперь общий для всех микросервисов (SSO)
- Admin Backend использует общий токен `auth_token` вместо `admin_token`
- Middleware в Admin Backend проверяет роли из JWT вместо отдельной авторизации
- Функция `GenerateToken()` теперь принимает параметр `roles` и добавляет `exp`, `iat`
- Функция `ParseToken()` теперь возвращает `Roles` в структуре `TokenClaims`
- Скрипт `run` теперь запускает все сервисы включая Admin Backend
- Mobile порт изменен с 8081 на 8082 (8081 используется Admin Backend)
- Функция `InitDB()` теперь имеет вариант `InitDBWithPath()` для гибкости путей

### Removed
- Отдельная авторизация для админ-панели (заменена на SSO)

### Fixed
- Исправлены HTTP method константы в admin handlers (MethodGet вместо MethodGET)
- **Исправлена критическая ошибка:** JWT Secret теперь инициализируется ПОСЛЕ загрузки .env файла
- Исправлена валидация JWT токенов в Admin Backend
- Исправлены порты: Mobile (8081), Admin Backend (9000) для избежания конфликтов

## [0.0.8] - 2025-12-12

### Added
- Файл `.kiro/steering/changelog-rules.md` с правилами ведения CHANGELOG
- Комментарий-напоминание о правилах в начале CHANGELOG.md
- Правило: в секции "Технологии" указывать только новые/измененные технологии
- Папка `docs/` для всей документации проекта
- Файл `docs/README.md` с описанием всей документации
- Файл `docs/ROADMAP.md` с планом развития проекта и критериями версионирования
- Файл `docs/PERFORMANCE.md` с метриками производительности и рекомендациями
- Файл `docs/STRUCTURE.md` с описанием архитектуры проекта
- Файл `docs/TEMPLATES.md` с шаблонами страниц и компонентов
- Файл `docs/API_KEYS.md` с конфиденциальной информацией (не в git)
- Папка `tests/` для всех тестов проекта
- Скрипт `tests/performance-test.sh` для автоматического тестирования скорости
- Скрипт `tests/compare-with-vk.sh` для сравнения с VK.com
- Скрипт `tests/web-vitals-test.sh` для измерения Web Vitals через Lighthouse
- Файл `tests/README.md` с документацией по тестам
- Сравнительный анализ производительности с VK.com
- Целевые показатели Web Vitals (TTFB, FCP, LCP, TBT, CLS)
- Инструкции по измерению Web Vitals через Chrome DevTools и Lighthouse CLI

### Changed
- Исправлены все предыдущие версии в CHANGELOG согласно новому правилу
- Секция "Технологии" теперь содержит только изменения, а не весь стек
- Полный стек технологий перенесен в README.md
- Скрипты тестирования перенесены из `scripts/` в `tests/`
- Вся документация перенесена в папку `docs/`
- Обновлены все ссылки на документацию во всех файлах

### Fixed
- Убрано дублирование полного списка технологий в каждой версии CHANGELOG

## [0.0.7] - 2025-12-12

### Added
- Динамические URL профилей пользователей в формате /id{номер}
- Страница профиля пользователя доступна по адресу /id1, /id2 и т.д.
- Возможность просматривать профили других пользователей
- Определение "свой/чужой" профиль для отображения кнопок редактирования
- Кликабельные имена пользователей в постах с переходом на их профиль
- Динамическая ссылка "Профиль" в боковом меню (ведет на /id{userId})
- Динамическая ссылка "Управление аккаунтом" в меню пользователя

### Changed
- Страница /profile теперь редиректит на /id{userId} текущего пользователя
- Sidebar использует AuthContext для получения ID текущего пользователя
- Кнопки редактирования профиля показываются только владельцу
- Кнопка "Добавить питомца" показывается только владельцу профиля

### Технологии
- Next.js Dynamic Routes ([userId])

## [0.0.6] - 2025-12-12

### Added
- Полная интеграция профиля с базой данных
- Расширенная модель пользователя: bio, phone, location, avatar, cover_photo
- Таблица posts для хранения публикаций пользователей
- Таблица pets для хранения питомцев пользователей
- API endpoint PUT /api/profile для обновления профиля
- API endpoints для постов: GET /api/posts, POST /api/posts, PUT /api/posts/:id, DELETE /api/posts/:id
- API endpoint GET /api/posts/user/:id для получения постов пользователя
- API endpoints для питомцев: GET /api/pets/user/:id, POST /api/pets, DELETE /api/pets/:id
- Модели Post и Pet в backend
- Handlers для profile, posts, pets
- Реальное создание постов через форму CreatePost
- Отображение реальных постов в ленте из базы данных
- Отображение реальных постов и питомцев в профиле
- Форматирование времени публикации (только что, X мин назад, X ч назад)
- Загрузка данных пользователя при входе (bio, phone, location, avatar, cover_photo)
- Аватар пользователя в CreatePost и PostCard
- Защита endpoints постов и питомцев через JWT middleware

### Changed
- Модель User расширена новыми полями
- AuthContext теперь возвращает полные данные пользователя
- Endpoint /api/auth/me возвращает все поля пользователя
- Feed компонент использует реальные данные из API
- PostCard компонент адаптирован под реальную структуру Post
- Profile page отображает реальные данные из базы
- CreatePost компонент создает реальные посты в базе данных

### Fixed
- Исправлена структура ответов API (единый формат с success/data/error)
- Добавлены helper функции sendSuccessResponse и sendErrorResponse

## [0.0.5] - 2025-12-12

### Added
- Интеграция DaData API для поиска организаций по ИНН, ОГРН и названию
- Компонент OrganizationSearch с автодополнением
- Страница "Организации" с детальной информацией
- Отображение реквизитов организации (ИНН, ОГРН, КПП)
- Полная информация об адресе (индекс, регион, город, улица, дом)
- Координаты организации (geo_lat, geo_lon) из DaData
- Контактная информация (телефоны, email)
- Информация о руководстве организации
- Статус организации (Действующая/Ликвидирована)
- Дата регистрации и организационно-правовая форма
- Интеграция Яндекс.Карт для отображения местоположения организаций
- Компонент YandexMap с поддержкой геокодирования адресов
- Геокодирование через Яндекс.Карты API для точного определения координат
- Fallback на координаты из DaData при ошибке геокодирования
- Красная метка на карте с информацией об организации
- Элементы управления картой (zoom, fullscreen)
- Balloon с названием организации и адресом при клике на метку
- Файл API_KEYS.md для хранения всех API ключей проекта
- API_KEYS.md добавлен в .gitignore для безопасности

### Changed
- Определение города через DaData API вместо ip-api.com
- Карта использует геокодирование по адресу как основной метод
- Координаты из DaData используются как запасной вариант
- Высота карты установлена на 400px
- Цвет спиннера загрузки карты изменен на #1B76FF (цвет логотипа)

### Fixed
- Исправлена проблема с загрузкой карты (карта всегда в DOM)
- Убраны отладочные console.log из компонента организаций
- Исправлена ошибка геокодирования через HTTP API
- Улучшена обработка ошибок при загрузке карты
- Исправлена ошибка 413 при поиске организаций (убран фильтр status)

### Технологии
- Добавлено: DaData API (Party, Address) для поиска организаций
- Добавлено: Yandex Maps API 2.1 для отображения карт

## [0.0.4] - 2025-12-12

### Added
- Автоматическое определение города пользователя по IP (ipapi.co, ip-api.com)
- Компонент CityDetector с поддержкой русского языка
- Возможность ручного изменения города
- Умная лента с объединением постов и объявлений
- Типы постов: обычный пост, объявление, потерялся, нашелся, помощь
- Компонент выбора типа поста при создании
- Фильтры ленты в правой колонке (Все, Посты, Объявления, Потеряшки, Найдены, Помощь)
- Цветные бейджи для разных типов постов
- Динамические плейсхолдеры в зависимости от типа поста
- Favicon с логотипом ЗооПлатформы

### Changed
- Название сайта изменено на "ЗооПлатформа"
- Логотип обновлен (favicon.svg с сердечком и мордой собаки)
- Все синие цвета кнопок заменены на #1B76FF (цвет из логотипа)
- Красный цвет уведомлений изменен на #FC2B2B (цвет из логотипа)
- Убран пункт "Главная" из меню (дублировал "Ленту")
- Убран пункт "Объявления" из меню (интегрировано в ленту)
- Шрифт сайта: -apple-system, BlinkMacSystemFont, Roboto
- Ширина сайта уменьшена до 1150px (стиль VK)
- Боковое меню уменьшено до 180px
- Размер шрифта логотипа: text-sm с uppercase
- Фон поиска в хедере соответствует фону сайта (градиент)
- Уменьшен размер круга уведомлений (w-4 h-4)
- Уплотнены дополнительные пункты меню (py-1)
- Бейдж мессенджера остался овальным (px-2 py-0.5)
- Фильтры перенесены из центральной колонки в правую панель

### Fixed
- Исправлена ошибка с серверными компонентами (добавлен 'use client' в RightPanel)
- Улучшена поддержка русских названий городов
- Добавлен словарь перевода популярных городов России

## [0.0.3] - 2025-12-11

### Added
- Heroicons библиотека для иконок
- Минималистичный дизайн UI в стиле ЗооПлатформа
- Боковое меню с иконками Heroicons
- Разделители между секциями меню
- Badge для мессенджера (43 непрочитанных)
- Обновленный хедер с поиском и уведомлениями

### Changed
- Полностью переработан дизайн интерфейса
- Sidebar: чистый минималистичный стиль без фоновых подложек
- Hover эффекты с закругленными углами
- Header: выровнен с контентом страницы
- Логотип в хедере выровнен с боковым меню
- Layout: центральная колонка 580px, правая панель расширена
- Меню включает: Главная, Лента, Объявления, Профиль, Мессенджер, Зоожурнал, Афиша, Организации, Зоомаркет, Учебный центр, Сервисы, Мои питомцы, Горячая линия
- Дополнительные ссылки: О платформе, Фонд "ЗооПомощь", Статистика, Техподдержка, Команда

### Технологии
- Добавлено: Heroicons 2.x для иконок

## [0.0.2] - 2025-12-11

### Added
- React Native мобильное приложение (Expo)
- Папка `shared/` для общей логики между платформами
- Air для hot reload Go бекенда
- Metro config для React Native
- Пример интеграции mobile с backend API
- SQLite база данных
- Отдельный модуль `database/` для работы с БД
- Таблица `users` с полем password
- Автоматическая инициализация БД при старте сервера
- REST API структура (handlers, models)
- JWT авторизация
- Endpoints для регистрации и входа (`/api/auth/register`, `/api/auth/login`)
- Middleware для защиты endpoints
- Bcrypt хеширование паролей
- CRUD endpoints для users (GET, POST, PUT, DELETE)
- TypeScript типы для всех API запросов/ответов
- Стандартный формат ответов API (success, data, error)
- Документация структуры проекта (STRUCTURE.md)

### Changed
- CORS настроен на `*` для поддержки всех клиентов
- CORS headers включают Authorization
- Скрипт `run` теперь запускает три сервиса (backend, web, mobile)
- API клиент вынесен в shared (для web) и продублирован в mobile
- Обновлен Go до версии 1.25.5
- База данных вынесена в отдельный модуль в корне проекта
- Endpoints `/api/users` теперь защищены JWT токеном

### Технологии
- Добавлено: React Native 0.81.5 + Expo 54.0.27 для мобильного приложения
- Добавлено: Air 1.63.4 для hot reload Go бекенда
- Добавлено: SQLite 3 для базы данных
- Добавлено: JWT (golang-jwt/jwt/v5) для авторизации
- Добавлено: Bcrypt для хеширования паролей

## [0.0.1] - 2025-12-11

### Added
- Инициализация проекта с Go backend и Next.js frontend
- Go HTTP сервер на порту 8080 с базовыми эндпоинтами (/, /api/health)
- CORS middleware для взаимодействия с фронтендом
- Next.js приложение с TypeScript на порту 3000
- Tailwind CSS для стилизации
- Структура компонентов: ui/, shared/, layout/
- API клиент (`lib/api.ts`) для связи с бекендом
- Базовые компоненты: Button с вариантами, Header
- Environment variables конфигурация (.env.local)
- Скрипт `run` для запуска проекта с автоматической очисткой портов
- Документация: README.md и CHANGELOG.md
- Git репозиторий с тегом v0.0.1

### Технологии
- Go 1.25.5
- Node.js 25.2.0
- Next.js 16.0.8 (App Router)
- React 19.2.1
- TypeScript 5.x
- Tailwind CSS 4.x
