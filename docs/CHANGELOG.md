# Changelog

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/),
и этот проект придерживается [Semantic Versioning](https://semver.org/lang/ru/).

<!-- 
ПРАВИЛО: В секции "Технологии" указывать ТОЛЬКО новые или измененные технологии.
НЕ перечислять весь стек проекта в каждой версии!

✅ Правильно:
### Технологии
- Добавлено: DaData API для поиска организаций
- Обновлено: React 18 → 19

❌ Неправильно:
### Технологии
- Go 1.25.5
- Node.js 25.2.0
- Next.js 16.0.8
- (весь стек проекта)

Полный стек технологий находится в README.md
Подробные правила: ../.kiro/steering/changelog-rules.md
-->

## [Unreleased]

### Fixed
- **Исправлена ошибка callback в CreatePost на странице профиля**
  - Проблема: `ReferenceError: loadPosts is not defined` на странице профиля пользователя
  - В компоненте `CreatePost` использовался несуществующий callback `loadPosts`
  - Решение: заменён на правильный callback `loadUserProfile`, который загружает посты через `postsApi.getUserPosts(userId)`
  - Файл: `main/frontend/app/(main)/[userId]/page.tsx`

- **Исправлена проверка прав на странице организации**
  - Проблема: компонент `CreatePost` не отображался на странице организации для owner/admin
  - Причина: функция `isOwnerOrAdmin()` проверяла массив `members` до его загрузки
  - Решение: 
    - Добавлена проверка `membersLoading` в функцию `isOwnerOrAdmin()`
    - Функция возвращает `false` пока идёт загрузка членов организации
    - Добавлено логирование для отладки (user, members, isOwnerOrAdmin)
  - Файл: `main/frontend/app/(main)/org/[id]/page.tsx`
  - Теперь форма создания поста корректно отображается для owner и admin организации

### Added
- **CreatePost на странице организации**
  - Форма создания поста добавлена на страницу организации `/org/[id]`
  - Отображается только для owner и admin с правом публикации
  - Автоматическое обновление списка постов после создания
  - Callback `onPostCreated` вызывает `loadPosts()`
  - Файл: `main/frontend/app/(main)/org/[id]/page.tsx`
- **CreatePost на странице профиля пользователя**
  - Форма создания поста добавлена на страницу профиля `/[userId]`
  - Отображается только на своем профиле (isOwnProfile = true)
  - Размещена в табе "Публикации" перед списком постов
  - Автоматическое обновление списка постов после создания
  - Файл: `main/frontend/app/(main)/[userId]/page.tsx`

### Changed
- **Структура постов на странице организации**
  - CreatePost обёрнут в отдельный белый блок с тенью и отступами
  - Соответствует дизайну главной страницы с метками
  - Улучшена визуальная иерархия между формой создания и списком постов
  - Файл: `main/frontend/app/(main)/org/[id]/page.tsx`

- **Структура постов на странице профиля пользователя**
  - CreatePost обёрнут в отдельный белый блок с тенью и отступами
  - Единообразный дизайн с главной страницей и страницей организации
  - Файл: `main/frontend/app/(main)/[userId]/page.tsx`
  - Секция "Публикации" теперь включает CreatePost и список постов
  - CreatePost отображается первым элементом
  - Список постов обернут в отдельный div с заголовком
  - Файл: `main/frontend/app/(main)/org/[id]/page.tsx`

### Technical
- Импортирован компонент CreatePost в обе страницы
- Используется проверка прав доступа перед отображением формы
- Callback onPostCreated обеспечивает обновление списка постов

## [0.10.0] 🏢 - 2025-01-12
  - Кнопка с именем автора и стрелкой вниз в модальном окне создания поста
  - Выбор "От себя" (user) или "От организации X" (organization)
  - Загрузка списка организаций пользователя при открытии модального окна
  - Фильтрация организаций: только где role = owner или admin и can_post = true
  - Отображение аватара выбранного автора (пользователь или организация)
  - Автоматическое закрытие dropdown при выборе автора
  - State управление: `selectedAuthor`, `selectedOrganizationId`, `showAuthorDropdown`
  - Файл: `main/frontend/app/components/posts/CreatePost.tsx`
- **API endpoint GET /api/organizations/my**
  - Возвращает организации где пользователь owner или admin с правом публикации
  - Фильтрация по `can_post = 1`
  - Сортировка по названию организации (ASC)
  - Возвращает: id, name, short_name, type, logo, bio, role, can_post
  - Handler: `GetMyOrganizationsHandler`
  - Файл: `main/backend/handlers/organizations.go`
  - Роут зарегистрирован в `main/backend/main.go`
- **Сохранение автора поста в БД**
  - Поля `author_type` и `organization_id` отправляются в POST /api/posts
  - `author_type`: 'user' (от себя) или 'organization' (от организации)
  - `organization_id`: NULL если user, ID организации если organization
  - `user_id`: всегда содержит ID пользователя, который создал пост
  - Обновлен `handleSubmit` в CreatePost для отправки новых полей
- **Функция loadOrganizations**
  - Загружает организации при открытии модального окна
  - useEffect срабатывает при `showModal = true`
  - Обработка ошибок с fallback на пустой массив
  - Файл: `main/frontend/app/components/posts/CreatePost.tsx`

### Fixed
- **Исправлена функция отправки JSON ответа в GetMyOrganizationsHandler**
  - Проблема: использовалась несуществующая функция `sendJSONResponse`
  - Решение: заменено на `sendJSONSuccess` (существующая helper функция)
  - Файл: `main/backend/handlers/organizations.go`
  - Backend успешно компилируется

### Technical
- Поля `author_type` и `organization_id` уже существуют в таблице `posts`
- Используется существующая структура БД без дополнительных миграций
- Индексы уже созданы: `idx_posts_organization_id`, `idx_posts_author_type`

## [0.10.0] 🏢 - 2025-01-12

### Added
- **ЭТАП 0 - ДРУЗЬЯ (Frontend)**
  - Компонент `FriendButton` на странице профиля (Добавить в друзья / Принять / Отклонить / Удалить)
  - Страница `/friends` - список всех друзей с поиском
  - Страница `/friends/requests` - входящие и исходящие запросы в друзья
  - Компонент `FriendsList` для отображения списка друзей с аватарами
  - Счётчик входящих запросов в Header (красный badge)
  - Backend handlers уже были реализованы ранее (таблица `friendships`, API endpoints)
- **ЭТАП 1 - ЛАЙКИ И КОММЕНТАРИИ (Frontend)**
  - Кнопка лайка на постах с анимацией и счётчиком
  - Модальное окно `LikersModal` со списком пользователей, поставивших лайк
  - Кнопка "Комментарии" на постах с счётчиком
  - Форма добавления комментария под постом
  - Список комментариев с аватарами, именами и датами
  - Кнопка удаления своих комментариев
  - Backend handlers уже были реализованы ранее
- **ЭТАП 2 - ОРГАНИЗАЦИИ: Редактирование и управление командой**
  - **Страница редактирования организации** (`/org/[id]/edit`)
    - Структура как у редактирования профиля с 3 вкладками
    - **Вкладка "Основная информация"**: логотип, обложка, название, краткое и полное описание
    - **Вкладка "Контакты"**: email, телефон, веб-сайт, полный адрес, город
    - **Вкладка "Команда"**: компонент `MembersManager` для управления членами организации
    - Загрузка и превью логотипа и обложки
    - Счетчики символов для описаний (200/1000)
    - Индикаторы заполненности разделов (зелёные точки)
    - Подсказки для каждой вкладки
    - Валидация полей и обработка ошибок
  - **Backend API для редактирования организаций**
    - `OrganizationHandler` - общий handler для маршрутизации GET/PUT запросов
    - `UpdateOrganizationHandler` - обновление данных организации
    - Проверка прав доступа (только члены с `can_edit = true`)
    - Обновление только переданных полей
    - Поддержка всех полей: name, short_name, bio, description, email, phone, website, address_full, address_city
  - **API методы для управления членами организации**
    - `addMember()` - добавление нового участника
    - `updateMember()` - изменение роли и должности
    - `removeMember()` - удаление участника
    - Добавлены в `organizations-api.ts`
  - **Компонент MembersManager**
    - Список участников с аватарами, ролями и должностями
    - Модальное окно добавления участника с поиском пользователей
    - Модальное окно редактирования роли и должности
    - Удаление участников с подтверждением
    - Цветовая индикация ролей (owner - фиолетовый, admin - синий, moderator - зелёный, member - серый)
    - Защита от удаления owner и самого себя
  - **Блок управления организацией на странице организации**
    - Показывается только для owner и admin
    - Кнопка "Система управления" открывает dashboard соответствующего сервиса
    - Автоматический переход в dashboard с `orgId` в URL
    - Кнопка "Редактировать профиль" ведёт на `/org/{id}/edit`

### Fixed
- **Исправлен ключ контекста в уведомлениях**
  - Унифицирован ключ на `"userID"` во всех handlers уведомлений
  - Исправлена ошибка вложенных кнопок в `NotificationsDropdown.tsx`
- **Исправлены ошибки компиляции Go**
  - `http.MethodPUT` → `http.MethodPut` в `organizations.go`
  - Удалён неиспользуемый параметр в `announcements.go`
- **Исправлены типы в TypeScript**
  - `OrganizationMember.user_name` теперь `string | undefined`
  - `Member.user_name` в `MembersManager` теперь опциональное
  - Добавлены проверки на `undefined` при отображении имён
  - Удалён неиспользуемый импорт `CheckIcon`

### Changed
- **Фильтр организаций**
  - Добавлен флаг `autoFilterDisabled` для предотвращения автосброса
  - Исправлена проблема постоянного срабатывания автофильтра при выборе "Все регионы"
- **Shelter dashboard**
  - Автоматическое сохранение `orgId` из URL в localStorage
  - Прямой переход в dashboard конкретной организации

### Technical
- Роут `/api/organizations/{id}` теперь обрабатывает GET и PUT через `OrganizationHandler`
- Добавлен `AuthMiddleware` для PUT запросов к организациям
- Улучшена обработка ошибок в API организаций

## [0.9.0] 🏢 - 2025-01-11

### Added
- **Страница создания организации** (`/org/create`)
  - Интеграция с DaData API для поиска по ИНН и автозаполнения
  - Автоопределение типа организации по ключевым словам
  - Яндекс.Карта для отображения местоположения
  - Создатель автоматически становится owner
- **Страница организации** (`/org/[id]`)
  - Компактная Яндекс.Карта (высота 250px)
  - Блок управления для owner/admin
  - Кнопка перехода в систему управления

## [0.8.0] 🏥 - 2025-12-31

### Added
- **Создан микросервис Clinic - Кабинет ветеринарной клиники (порты 6300/8600)**
  - **Multi-tenancy архитектура**: каждая клиника изолирована через `organization_id`
  - **TenantMiddleware**: проверка доступа через заголовок `X-Clinic-ID`
  - **Страницы выбора и создания клиники**: `/select` и `/create` (как в Shelter)
  - Backend на Go (порт 8600) с JWT middleware и hot reload
  - Frontend на Next.js 16 (порт 6300)
  - AdminLayout в стиле Admin/Shelter/Owner/Volunteer панелей
  - 3 вкладки: Обзор, Пациенты, Записи на приём
  - Градиентные карточки статистики (синяя, зеленая, фиолетовая)
  - Таблица с пациентами клиники
  - Страница авторизации `/auth` с автоматическим SSO редиректом
  - API endpoints: `/api/my-clinics`, `/api/clinics`, `/api/my-patients`, `/api/appointments`, `/api/profile`, `/api/organization`
  - Использование единой базы данных (таблица `organizations` с type='clinic')
  - Health endpoint для проверки работоспособности
  - Hot reload с Air для backend
  - CORS настроен для localhost:3000 и localhost:6300
  - Favicon и логотип в layout
  - README с полной документацией
- **Создан микросервис Owner - Кабинет владельца (порты 6100/8400)**
  - Backend на Go (порт 8400) с JWT middleware и hot reload
  - Frontend на Next.js 16 (порт 6100) - изменен с 7000 из-за конфликта с системными процессами macOS
  - AdminLayout в стиле Admin/Shelter панелей (светлое меню, градиентные карточки)
  - 3 вкладки: Обзор, Мои питомцы, Здоровье
  - Градиентные карточки статистики (синяя, зеленая, фиолетовая)
  - Таблица с питомцами пользователя
  - Страница авторизации `/auth` с автоматическим SSO редиректом
  - Проверка авторизации перед редиректом (если уже авторизован - сразу в дашборд)
  - API endpoints: `/api/my-pets`, `/api/pets/events`, `/api/profile`
  - Использование единой базы данных (таблица `pets` с полем `user_id`)
  - API клиент для взаимодействия с backend
  - Health endpoint для проверки работоспособности
  - Hot reload с Air для backend
  - CORS настроен для localhost:3000 и localhost:6100
  - Favicon и логотип в layout
  - README с полной документацией
- **Создан микросервис Volunteer - Кабинет зоопомощника (порты 6200/8500)**
  - Backend на Go (порт 8500) с JWT middleware и hot reload
  - Frontend на Next.js 16 (порт 6200)
  - AdminLayout в стиле Admin/Shelter/Owner панелей
  - 3 вкладки: Обзор, Мои подопечные, Задачи
  - Градиентные карточки статистики (синяя, зеленая, фиолетовая)
  - Таблица с подопечными питомцами (где `curator_id = user_id`)
  - Страница авторизации `/dashboard` с автоматическим SSO редиректом
  - API endpoints: `/api/my-pets`, `/api/my-tasks`, `/api/profile`
  - Использование единой базы данных (таблица `pets` с полем `curator_id`)
  - Health endpoint для проверки работоспособности
  - Hot reload с Air для backend
  - CORS настроен для localhost:3000 и localhost:6200
  - Favicon и логотип в layout
  - README с полной документацией
- **Инфраструктура**
  - Обновлен скрипт `./run` для запуска Clinic на портах 6300/8600
  - Обновлен скрипт `./run` для запуска Owner на порту 6100
  - Обновлен скрипт `./run` для запуска Volunteer на портах 6200/8500
  - Улучшена функция `kill_port()` с 3 попытками убить процесс (SIGTERM, SIGKILL, sudo SIGKILL)
  - Добавлена финальная проверка портов перед запуском сервисов
  - Добавлена очистка портов 6100, 6200, 6300, 8400, 8500, 8600
  - Добавлена проверка портов Owner, Volunteer и Clinic при запуске
  - Обновлен главный README.md с портами 6100 (Owner), 6200 (Volunteer), 6300 (Clinic)
  - Добавлена очистка кэша для Owner, Volunteer и Clinic frontend

### Fixed
- **Owner Backend**
  - Исправлено: использование `user_id` вместо `owner_id` в SQL запросах
  - Исправлено: handler `GetMyPets` теперь корректно загружает питомцев из единой базы
  - Исправлено: handler `GetPetEvents` использует правильное поле `user_id`
- **Volunteer Backend**
  - Исправлено: JWT_SECRET синхронизирован с Main backend для корректной SSO авторизации
  - Исправлено: убрана неиспользуемая переменная `userID` в handler `GetMyTasks`
- **Volunteer Frontend**
  - Исправлено: установлены зависимости Next.js и React для корректного запуска
- **Clinic Backend**
  - Исправлено: CORS теперь разрешает заголовок `X-Clinic-ID` для multi-tenancy
  - Исправлено: добавлен `X-Clinic-ID` в `Access-Control-Allow-Headers` в обоих CORS middleware
  - Исправлено: убрана неиспользуемая переменная `userID` в handler `GetMyPatients`
  - Backend успешно компилируется и готов к запуску
  - Исправлено: установлены зависимости Next.js и React для корректного запуска
- **Clinic Backend**
  - Исправлено: убрана неиспользуемая переменная `userID` в handler `GetMyPatients`
  - Backend успешно компилируется и готов к запуску

## [0.7.0] - 2024-12-31 🏠

### Added
- **Микросервис Shelter - Кабинет приюта**
  - Backend на Go (порт 8200) с Chi router и JWT middleware
  - Frontend на Next.js 16 (порт 5100) с AdminLayout
  - 4 вкладки: Статистика, Животные приюта, Пристройство, Волонтеры
  - Градиентные карточки статистики (синяя, зеленая, оранжевая, фиолетовая)
  - Виджеты: StatsWidget, TableWidget (скопированы из PetBase)
  - Страница авторизации `/auth` с SSO интеграцией
  - **Страница выбора приюта `/select`** - пользователь выбирает приют перед входом
  - **Страница создания приюта `/create`** - форма для создания нового приюта
  - API endpoint `/api/shelters` (POST) для создания приюта
  - Если у пользователя нет приютов - предлагается создать новый
  - Дашборд перемещен в `/dashboard`
  - API endpoint `/api/my-shelters` для получения списка приютов пользователя
  - Автоматический редирект: если приют один - сразу в дашборд
  - Сохранение выбранного приюта в localStorage
  - API клиент для взаимодействия с backend
  - Health endpoint для проверки работоспособности
  - Hot reload с Air для backend
  - CORS настроен для localhost:5100
  - README с полной документацией
- **Multi-tenancy архитектура для Shelter**
  - Каждый приют работает в изолированной области (tenant)
  - Таблицы: `shelter_animals`, `shelter_volunteers`, `adoption_requests`, `shelter_events`
  - Все таблицы имеют `organization_id` для изоляции данных
  - Middleware для автоматического определения текущего приюта
  - Пользователь может быть членом нескольких приютов
  - Автоматическая фильтрация всех запросов по tenant
  - Использование существующей системы Organizations
  - Миграция 022: создание таблиц приюта с multi-tenancy
- **Инфраструктура**
  - Обновлен скрипт `./run` для запуска Shelter
  - Добавлена очистка портов 5100 и 8200
  - Добавлена проверка портов Shelter при запуске
  - Обновлен главный README.md с информацией о Shelter
  - Admin backend: добавлен CORS для localhost:5100

### Fixed
- **Shelter Backend - критические ошибки компиляции**
  - Добавлена функция `AuthMiddleware` в `shelter/backend/middleware/auth.go`
  - Добавлена константа `UserIDKey` для единообразного использования context keys
  - Исправлен импорт database пакета (используется `database.DB`)
  - Исправлена регистрация routes (handlers обёрнуты в `http.HandlerFunc`)
  - Создан `.env` файл с правильным JWT_SECRET (синхронизирован с main backend)
  - Исправлен SQL запрос в `GetOrganization` (добавлены `COALESCE` для nullable полей)
  - Backend теперь успешно компилируется и запускается
  - Multi-tenancy работает корректно - пользователь видит только данные своего приюта

## [0.6.0] - 2024-12-30 💬

### Added
- **Мессенджер (личные чаты 1-на-1)**
  - Страница `/messenger` с полноценным интерфейсом чата
  - Список диалогов с аватарами, последними сообщениями и временем
  - Отправка и получение текстовых сообщений
  - Автоматическое создание чата при первом сообщении
  - Кнопка "Написать" на странице профиля пользователя
  - Счетчик непрочитанных сообщений в сайдбаре (красный бадж)
  - Автоматическая отметка сообщений как прочитанных
  - Сортировка чатов по времени последнего сообщения
  - Временные метки: "только что", "5 мин назад", "вчера"
  - Градиентные аватары с первой буквой имени
  - Placeholder для новых диалогов с подсказкой
  - Индикатор "Новый диалог" для несозданных чатов
- **Backend API для мессенджера**
  - `GET /api/chats` - список диалогов пользователя
  - `GET /api/chats/:id/messages` - сообщения конкретного чата
  - `POST /api/messages/send` - отправка сообщения
  - `GET /api/messages/unread` - количество непрочитанных
  - Автоматическое создание чата при отправке первого сообщения
  - Проверка участия пользователя в чате
- **База данных**
  - Таблица `chats` для хранения диалогов
  - Таблица `messages` для хранения сообщений
  - Таблица `message_attachments` для будущих вложений
  - Индексы для оптимизации запросов

### Fixed
- Исправлена проблема с редиректом при обновлении страницы мессенджера
- Исправлена ошибка "interface conversion: interface {} is nil, not int" в handlers
- Исправлено несоответствие ключей контекста: "userID" vs "user_id"
- Добавлена обработка ответа API при загрузке информации о пользователе
- Добавлена проверка наличия receiverId перед отправкой сообщения

### Technical
- Добавлены модели `Chat`, `Message` в `main/backend/models/chat.go`
- Добавлены handlers в `main/backend/handlers/messenger.go`
- Создан хук `useUnreadMessages.ts` для счетчика непрочитанных
- Обновлен `Sidebar.tsx` с реальным счетчиком непрочитанных
- Миграция `021_create_messenger.sql`

## [0.5.0] - 2024-12-30 🎨

### Added
- **Каталог питомцев `/catalog`**
  - Публичный endpoint `/api/catalog` без авторизации
  - Отображение питомцев со статусами: ищет дом, потерялся, найден, требуется помощь
  - Карточный дизайн с фото, информацией и контактами
  - Фильтры: поиск по кличке/породе, статус, вид животного
  - Счетчик найденных питомцев
  - Цветовая кодировка статусов (оранжевый, красный, синий, фиолетовый)
  - Бадж "СРОЧНО" для срочных случаев
  - Отображение города/региона и контактной информации
- **Улучшенный дизайн админ-панелей (стиль 2GIS)**
  - Увеличена ширина контента: 1400px → 1600px
  - Увеличена ширина бокового меню: 200px → 240px
  - Светлый фон меню (white вместо gray-100)
  - Увеличен размер шрифта в меню: text-sm → text-base
  - Применено ко всем сервисам: Admin, PetBase, Shared
  - Улучшена страница статистики ЗооБазы с градиентными карточками
- **Обновление навигации**
  - Переименована "Лента" → "Метки"
  - Добавлена вкладка "Каталог" в главное меню
  - Скрыты вкладки не для MVP: Зоожурнал, Афиша, Зоомаркет, Учебный центр, Фонд "ЗооПомощь"

### Fixed
- Исправлена структура Pet в Go handlers (добавлены недостающие поля)
- Исправлен go.mod: github.com/golang-jwt/jwt/v5 перемещен в прямые зависимости
- Исправлен роутинг в PetBase (корневой роут `/` перемещен в конец)
- Исправлен SQL запрос в CatalogHandler (убраны несуществующие поля)

### Changed
- Обновлена документация MVP с детальным описанием каталога

## [0.4.0] - 2024-12-30 🏢

### Added
- **Система профилей организаций**
  - База данных: 3 таблицы (`organizations`, `organization_members`, `organization_pets`)
  - Таблица `organizations` с 37 полями (юридическая информация, контакты, адрес, координаты)
  - 6 типов организаций: приют, ветклиника, зоомагазин, фонд, кинологический центр, другое
  - Система ролей участников (owner, admin, moderator, member)
  - Права доступа (can_post, can_edit, can_manage_members)
  - Настройки приватности профиля
  - Статус верификации организаций
  - Go модели и CRUD handlers
  - TypeScript API клиент
  - Страница профиля организации `/org/[id]` (3-колоночный layout)
  - Документация `docs/ORGANIZATIONS.md` с описанием системы верификации
- **Страница списка организаций `/org`**
  - 2-колоночный layout (средняя колонка 600px, правая 320px)
  - Фильтры: тип организации, регион, город
  - Поиск по названию и описанию
  - Счетчик найденных организаций
  - Автоопределение города пользователя через геолокацию
  - Автоматическая фильтрация по городу пользователя при первом заходе
  - Сохранение города в localStorage
  - Горизонтальные карточки организаций с логотипом, типом, описанием, городом
  - Значок верификации для проверенных организаций
- **Импорт реальных организаций**
  - Скрипты импорта из 2GIS API (`import-from-2gis.js`)
  - Скрипт импорта по ИНН через DaData API (`import-by-inn.js`)
  - Автоматическое определение города через building_id
  - Импортировано 30 реальных организаций из Удмуртии (Ижевск, Сарапул, Сигаево)
  - Типы: ветклиники, зоомагазины, приюты, фонды
  - 3 организации с ИНН и верификацией
- **Справочник городов России**
  - Полный справочник: 86 регионов, 1048 городов
  - JSON файл `russia-cities-full.json` с официальными названиями
  - Скрипт генерации справочника `create-full-cities-list.js`
  - Динамическая загрузка справочника на фронтенде
  - Фильтрация городов по выбранному региону

### Changed
- Обновлены все организации в базе: добавлено поле `address_region = 'Удмуртская Респ'`
- Backend API теперь возвращает `address_region` и `address_city` для фильтрации

### Technical
- Миграция `019_create_organizations.sql`
- Интеграция с 2GIS API для поиска организаций
- Интеграция с DaData API для получения данных по ИНН
- Интеграция с Nominatim (OpenStreetMap) для обратного геокодирования
- Использование `useRef` для предотвращения race conditions при автоустановке фильтров

## [0.3.5] - 2024-12-29 🐾

### Added
- **Страница питомца с постами**
  - Отдельная страница `/pets/[id]` для просмотра информации о питомце
  - Обложка с аватаром питомца (как в профиле пользователя)
  - 3-колоночный layout с `gap-2.5` между блоками
  - Левая колонка с отдельными карточками:
    - Фото питомца (большое изображение)
    - "О питомце" (возраст, пол, окрас)
    - "Медицинская информация" (прививки, стерилизация, чипирование)
    - "Посты с {имя питомца}" - список всех постов, где упоминается питомец
  - Правая колонка с карточками:
    - "Действия" (в избранное, поделиться)
    - "Информация" (кличка, вид, порода, пол, возраст)
    - "Здоровье" (краткая медицинская информация)
  - Клик по чипу питомца в посте открывает страницу питомца (не модальное окно)
- **API endpoint для постов питомца**
  - `GET /api/posts/pet/:id` - получение всех постов, где упоминается питомец
  - Использует таблицу `post_pets` для быстрого поиска
  - Возвращает только опубликованные посты (`status = 'published'`)
  - Сортировка по дате создания (новые первые)

### Changed
- **Обновлены TypeScript типы**
  - `Post` интерфейс теперь соответствует новой структуре с `attached_pets`, `attachments`, `tags`
  - `Pet` интерфейс дополнен полями: `breed`, `gender`, `birth_date`, `color`, `is_sterilized`, `is_vaccinated`, `chip_number`
  - `Poll` интерфейс дополнен полями: `allow_vote_changes`, `anonymous_voting`, `total_voters`, `is_expired`, `voters`
  - Добавлен интерфейс `PollVoter` для отображения проголосовавших
  - Добавлен интерфейс `Attachment` для медиа-вложений
- **PostCard теперь принимает опциональный `comments_count`**
  - Исправлена совместимость типов между компонентами

### Technical
- Добавлена функция `getPetPosts` в `handlers/posts.go`
- Добавлен handler `PetPostsHandler` для обработки запросов постов питомца
- Добавлен метод `postsApi.getPetPosts(petId)` в frontend API клиент

## [0.3.4] - 2024-12-29 🐾

### Added
- **Добавление питомцев из профиля пользователя**
  - Компонент `AddPetModal` для создания питомцев
  - Кнопка "Добавить" в разделе "Питомцы" на странице профиля
  - Форма с полями: имя (обязательно), вид (обязательно), порода, пол, дата рождения, окрас, статус
  - Автоматическое обновление списка питомцев после добавления
  - Интеграция с PetBase API (localhost:8100)
- **Отображение владельца в карточке питомца (ЗооБаза)**
  - Загрузка полной информации о владельце из Main API
  - Карточка владельца с аватаркой, полным именем (имя + фамилия), email и ID
  - Кликабельная ссылка на профиль владельца на основном сайте
  - Красивое оформление с hover эффектом и иконкой внешней ссылки
  - Обработка случаев, когда у пользователя нет фамилии
- **Прикрепление питомцев к постам**
  - Исправлена загрузка списка питомцев при создании поста
  - Модальное окно "Добавить питомца" теперь показывает реальный список питомцев пользователя
  - Используется endpoint `/api/pets/user/:id` вместо `/api/pets`

### Fixed
- **API endpoint `/api/users/:id` теперь возвращает полную информацию**
  - Добавлены поля: `last_name`, `avatar`, `bio`, `location`, `phone`
  - Исправлен SQL запрос в `main/backend/handlers/users.go`
  - Теперь возвращает 9 полей вместо 4
- **API endpoint `/api/users` (список пользователей)**
  - Добавлены поля: `last_name`, `avatar`
  - Улучшена консистентность данных между endpoints

### Changed
- **Компонент CreatePost**
  - Функция `loadPets()` теперь использует `user.id` для загрузки питомцев конкретного пользователя
  - Добавлена проверка наличия `user` перед загрузкой
- **Карточка питомца в ЗооБазе**
  - Раздел "Владелец и куратор" теперь показывает полную информацию о владельце
  - Добавлен state `owner` для хранения данных пользователя
  - Запрос к Main API с `credentials: 'include'` для передачи JWT токена

## [0.3.3] - 2024-12-29 🔒

### Added
- **Полноценная система питомцев в ЗооБазе (PetID)**
  - Таблица `pets` с 44 полями для полной информации о питомце
  - Миграция `011_create_pet_cards.sql` - создание базовой структуры
  - Миграция `012_add_pet_passport_fields.sql` - поля ветеринарного паспорта
  - Миграция `013_create_medical_records_tables.sql` - медицинские записи
  - Миграция `014_add_tag_number.sql` - бирка ОСВВ для приютов
  - Миграция `015_add_curator_and_location.sql` - куратор и местоположение
  - Миграция `016_add_sterilization_date.sql` - дата стерилизации
- **Детальная карточка питомца** (`petbase/frontend/app/pets/[id]/page.tsx`)
  - Hero Section с фото, именем, ID и датой создания
  - 6 вкладок с полной информацией:
    - **Основная информация** - вид, порода, пол, возраст, окрас, размер, вес
    - **Владелец и куратор** - информация о владельце или кураторе, контакты
    - **Идентификация** - микрочип, тату, бирка ОСВВ, ветпаспорт
    - **Родословная** - номер родословной, организация регистрации (РКФ, FCI, WCF, TICA)
    - **Медицинская информация** - пол, возраст, вес, группа крови, стерилизация с датой, аллергии, хронические заболевания, текущие препараты
    - **История** - все события жизни питомца (placeholder)
  - Визуальные индикаторы: зелёные галочки для заполненных полей, серые для пустых
  - Цветные блоки для важной информации (микрочип, тату, бирка)
  - Большие числа для идентификаторов
- **Система владения и кураторства**
  - Животное может иметь ЛИБО владельца, ЛИБО куратора, ЛИБО никого
  - Владелец - постоянный хозяин (животное живёт дома)
  - Куратор - временный опекун (животное на улице, передержке или в приюте)
  - Поля: `owner_name`, `owner_address`, `owner_phone`, `owner_email`
  - Поля: `curator_id`, `curator_name`, `curator_phone`
  - Документация в `docs/CURATOR_AND_LOCATION.md`
- **Система местоположения питомца**
  - 4 типа локации: `home` (дома), `street` (на улице), `foster` (передержка), `shelter` (приют)
  - Поля: `location`, `foster_address`, `shelter_name`
  - Логика: владелец → дома, куратор → улица/передержка/приют
- **Ветеринарный паспорт**
  - Все поля из бумажного ветпаспорта
  - Идентификация: микрочип, тату, бирка ОСВВ
  - Владелец: ФИО, адрес, телефон, email
  - Медицина: группа крови, аллергии, хронические заболевания, текущие препараты
  - Родословная: номер, организация регистрации
  - Особые приметы и отличительные черты
  - Документация в `docs/PET_PASSPORT.md`
- **Бирка ОСВВ (Отлов-Стерилизация-Вакцинация-Возврат)**
  - Поле `ear_tag_number` для номера бирки в ухе
  - Используется в приютах по программе ОСВВ
  - Отображается в разделе "Идентификация"
- **Медицинские записи** (таблицы созданы, функционал в разработке)
  - `pet_vaccinations` - вакцинации
  - `pet_parasite_treatments` - обработки от паразитов
  - `pet_surgeries` - операции
  - `pet_medical_history` - история визитов к ветеринару
- **Список питомцев в ЗооБазе**
  - Таблица со всеми питомцами пользователя
  - Колонки: ID, Имя, Вид, Порода, Пол, Возраст, Статус
  - Кликабельные строки для перехода к детальной карточке
  - Кнопка "Добавить питомца"
- **Справочники ЗооБазы**
  - Таблица `species` - виды животных (собаки, кошки, птицы и т.д.)
  - Таблица `breeds` - породы с привязкой к видам
  - Таблица `pet_cards` - информационные карточки о породах
  - Handlers: `species.go`, `breeds.go`, `cards.go`
  - Публичные API endpoints (без аутентификации)
- **Система безопасности ЗооБазы (PetID)**
  - JWT аутентификация с поддержкой Bearer токенов
  - Middleware `RequireAuth` для защищённых routes
  - Middleware `OptionalAuth` для опциональной аутентификации
  - Функции `GenerateToken(userID)` и `ValidateToken(token)`
  - Проверка владельца при update/delete операциях
  - User ID берётся из JWT токена, а не из запроса (защита от подделки)
  - CORS с проверкой разрешённых origins (настраивается через ALLOWED_ORIGINS)
  - Блокировка запросов с неразрешённых origins (403 Forbidden)
  - Логирование заблокированных запросов
  - Временная поддержка X-User-ID заголовка для разработки
- **API клиент для основного сайта**
  - Файл `main/frontend/lib/petbase-api.ts` с полным API клиентом
  - Поддержка JWT токенов в заголовке Authorization
  - Методы: `setAuthToken()`, `clearAuthToken()`, `setUserId()`
  - Обработка ошибок 401 (Unauthorized) и 403 (Forbidden)
  - Все CRUD операции для питомцев с аутентификацией
- **Документация по безопасности**
  - `docs/SECURITY.md` - полная документация по безопасности
  - `docs/PETBASE_INTEGRATION.md` - архитектура интеграции
  - `petbase/backend/README_AUTH.md` - инструкция по аутентификации
  - `petbase/TESTING_SECURITY.md` - тесты безопасности
  - `petbase/INTEGRATION_SUMMARY.md` - сводка по интеграции
  - `petbase/backend/.env.example` - пример конфигурации
  - `docs/PET_PASSPORT.md` - документация ветпаспорта
  - `docs/CURATOR_AND_LOCATION.md` - документация системы владения
- **Обновлена документация API**
  - `docs/API_ENDPOINTS.md` обновлён с информацией о безопасности
  - Разделение на публичные и защищённые endpoints
  - Примеры запросов с JWT токенами и X-User-ID
  - Коды ошибок безопасности (401, 403, 429)
  - Рекомендации по безопасности для клиентов

### Changed
- **Защищённые endpoints ЗооБазы**
  - `/api/pets` - теперь требует аутентификацию
  - `/api/pets/:id` - требует аутентификацию для GET, PUT, DELETE
  - `/api/pets/user/:userId` - требует аутентификацию
  - `/api/pets/status/:status` - требует аутентификацию
- **Публичные endpoints** (без аутентификации):
  - `/api/health` - проверка здоровья
  - `/api/species` - список видов
  - `/api/breeds` - список пород
  - `/api/cards` - информационные карточки
- **Структура Pet**
  - Поле `SterilizationDate` изменено на `*string` (nullable)
  - Исправлена обработка NULL значений в SQL запросах
- **Frontend ЗооБазы**
  - Добавлен X-User-ID заголовок во все запросы к API
  - Страница списка питомцев (`petbase/frontend/app/page.tsx`)
  - Страница детальной карточки (`petbase/frontend/app/pets/[id]/page.tsx`)
- **CORS настройки**
  - Разрешённые origins: localhost:3000, localhost:4100
  - Настраивается через переменную окружения ALLOWED_ORIGINS
  - Блокировка неразрешённых origins с логированием

### Fixed
- Исправлена ошибка компиляции в `handlers/pets.go` (дублирование полей в Scan)
- Исправлена обработка NULL значений для `sterilization_date`
- Исправлена ошибка "no new variables on left side of :=" в updatePet
- Исправлены все SQL Scan операции (удалено дублирование `&pet.IsSterilized`)
- Питомцы теперь отображаются на фронтенде ЗооБазы
- Детальные карточки питомцев открываются без ошибок

### Security
- ✅ JWT аутентификация реализована
- ✅ Проверка владельца при изменении/удалении
- ✅ User ID из токена, а не из запроса
- ✅ CORS с проверкой origins
- ✅ Все тесты безопасности проходят успешно
- ⏳ Rate limiting (в разработке)
- ⏳ Валидация входных данных (в разработке)

### Технологии
- Добавлено: github.com/golang-jwt/jwt/v5 для JWT аутентификации
- Добавлено: Air для hot-reload в petbase/backend

## [0.3.2] - 2025-12-28 🔧

### Fixed
- **Исправлена работа всех полей профиля**
  - Обновлен `updateProfile` в `api.ts` - добавлены все поля (last_name, настройки приватности)
  - Исправлен `profile.go` handler - убраны avatar и cover_photo из обычного сохранения профиля (для них есть отдельные endpoints)
  - Добавлен `refreshUser()` после сохранения - данные в AuthContext обновляются после редактирования
  - Добавлена зависимость от `currentUser` в useEffect на странице профиля - страница автоматически перезагружается при изменении данных
  - Все поля теперь корректно сохраняются и отображаются: имя, фамилия, биография, телефон, город, настройки приватности (5 полей)

### Added
- **Система друзей (Backend)**
  - Таблица `friendships` для хранения связей между пользователями
  - Статусы дружбы: pending (ожидание), accepted (принято), rejected (отклонено), blocked (заблокирован)
  - API endpoints:
    - `POST /api/friends/send` - отправить запрос в друзья
    - `POST /api/friends/accept` - принять запрос в друзья
    - `POST /api/friends/reject` - отклонить запрос в друзья
    - `DELETE /api/friends/remove` - удалить из друзей
    - `GET /api/friends` - получить список друзей
    - `GET /api/friends/requests` - получить входящие запросы
    - `GET /api/friends/status` - проверить статус дружбы с пользователем
  - Модели: Friendship, FriendshipResponse, FriendRequest, FriendActionRequest
  - Индексы для быстрого поиска по user_id, friend_id, status
  - Защита от добавления себя в друзья
  - Проверка существующих запросов
- **Страница редактирования профиля**
  - Трехколоночный layout: 2 колонки для формы, 1 для меню разделов
  - 3 раздела с динамическим переключением:
    - **Профиль** - обложка, аватар, имя, фамилия, краткая информация (все в одном месте)
    - **Контакты** - телефон, город, email (read-only)
    - **Приватность** - настройки видимости и безопасности
  - Индикаторы заполненности разделов (зеленая/серая точка)
  - Динамические заголовки и подсказки для каждого раздела
  - Информационный блок с советами в правой колонке
- **Настройки приватности**
  - Раздел "Приватность" на странице редактирования профиля
  - 5 настроек приватности:
    - **Видимость профиля** - кто может видеть профиль (все/друзья/только я)
    - **Видимость телефона** - кто может видеть номер телефона (все/друзья/никто)
    - **Видимость email** - кто может видеть email (все/друзья/никто)
    - **Личные сообщения** - кто может писать сообщения (все/друзья/никто)
    - **Онлайн-статус** - показывать или скрывать статус онлайн (да/нет)
  - Миграция базы данных: добавлены колонки `profile_visibility`, `show_phone`, `show_email`, `allow_messages`, `show_online`
  - Значения по умолчанию: профиль публичный, контакты скрыты, сообщения от всех, статус виден
  - Обновлены API endpoints для сохранения настроек приватности
- **Toast уведомления**
  - Красивые всплывающие уведомления в правом верхнем углу
  - 4 типа: success (зеленый), error (красный), warning (желтый), info (синий)
  - Автоматическое исчезновение через 5 секунд
  - Кнопка закрытия вручную
  - Плавная анимация появления/исчезновения
  - ToastContext и useToast hook для удобного использования
  - Заменены все alert() на toast уведомления
- **Имя и Фамилия в профиле**
  - Разделение поля "Имя" на "Имя" и "Фамилия"
  - Два отдельных поля на странице редактирования профиля (в одной строке)
  - Миграция базы данных: добавлена колонка `last_name` в таблицу `users`
  - Автоматическое разделение существующих имен (если есть пробел)
  - Helper функция `getFullName()` для отображения полного имени
  - Обновлены API endpoints для работы с фамилией
  - Полное имя отображается во всех компонентах: Header, Profile, Posts, Comments
- **Правила миграций базы данных**
  - Создан steering файл `.kiro/steering/database-migrations.md`
  - Критическое правило: всегда сохранять существующие данные
  - Примеры правильных и неправильных миграций
  - Checklist для безопасных миграций
  - Инструкции по резервному копированию и восстановлению
- **Загрузка аватара и обложки профиля**
  - Кнопки загрузки на странице редактирования профиля
  - Превью изображений в реальном времени
  - Валидация типа файла (только изображения)
  - Валидация размера (максимум 10MB)
  - Индикаторы загрузки (спиннеры)
  - Автоматическое обновление данных пользователя после загрузки
  - API endpoints: `POST /api/profile/avatar`, `POST /api/profile/cover`
  - Хранение файлов: `uploads/users/{user_id}/avatars/`, `uploads/users/{user_id}/covers/`
  - Метод `refreshUser()` в AuthContext для обновления данных
  - Полное логирование всех операций загрузки (успех, ошибки, предупреждения)
  - Helper функция `getMediaUrl()` для корректного отображения медиа
  - Раздача статических файлов из папки `uploads/` через backend
  - Кнопки удаления аватара и обложки с подтверждением
  - API endpoints: `DELETE /api/profile/avatar/delete`, `DELETE /api/profile/cover/delete`
  - Красивый градиентный фон по умолчанию для обложки (синий → фиолетовый → розовый) с инициалом пользователя
  - Обложка не отображается, если не загружена (вместо градиента по умолчанию)
  - Кнопка "Добавить обложку профиля" на странице редактирования, когда обложки нет
- **Галерея медиа в профиле**
  - Вкладки: Публикации, Медиа (объединяет фото и видео)
  - Отображение всех загруженных фото и видео пользователя
  - Фильтры: Все / Фото / Видео
  - Сетка 3x3 с превью
  - Lightbox для просмотра в полном размере с поддержкой видео
  - Воспроизведение видео в lightbox с контролами
  - Информация о файле при наведении
  - Компонент `MediaGallery` для отображения галереи

### Fixed
- **Исправлена ошибка при обновлении страницы профиля**
  - Исправлена обработка NULL значений в полях `bio`, `phone`, `location`, `avatar`, `cover_photo` в `MeHandler`
  - Использование `sql.NullString` для корректного сканирования nullable полей из базы данных
  - Теперь жесткое обновление страницы (Shift+Cmd+R) не выбрасывает на страницу авторизации
  - Добавлена проверка `typeof window === 'undefined'` в `AuthContext` для предотвращения SSR запросов
- **Исправлена независимость удаления аватара и обложки**
  - Изменен порядок операций: сначала `refreshUser()`, затем обновление локального превью
  - Удаление обложки больше не влияет на аватар и наоборот
  - Компонент `MediaLightbox` для просмотра фото и видео
- **Статистика медиа**
  - Количество фотографий и видео
  - Общий размер файлов
  - Отображается только в своем профиле
  - Компонент `MediaStats`
- **API для медиа**
  - `GET /api/media/user/:userId` - получить все медиа пользователя
  - Query параметр `type` для фильтрации (photo/video)
  - `GET /api/media/stats` - статистика медиа текущего пользователя

### Changed
- Профиль пользователя теперь имеет 2 вкладки: Публикации и Медиа
- Вкладка "Медиа" объединяет фото и видео в одном месте
- Все медиа из постов автоматически сохраняются в личное облако
- Счетчики фильтров всегда показывают реальное количество файлов
- Фильтрация медиа происходит на клиенте для корректного отображения счетчиков
- Аватары теперь отображаются корректно во всех компонентах (Header, Feed, CreatePost, Profile)
- Компонент `Image` из Next.js заменен на обычный `img` для аватаров и обложек
- Все компоненты используют helper функцию `getMediaUrl()` для формирования URL медиа
- Backend теперь раздает статические файлы из папки `uploads/` через FileServer

### Fixed
- Исправлена проблема с неправильными счетчиками в фильтрах медиа
- Исправлено воспроизведение видео в lightbox (был PhotoLightbox, стал MediaLightbox)
- Исправлено отображение аватаров (добавлен полный URL с http://localhost:8000)
- Добавлено поле `updated_at` в таблицу `users`

## [0.3.1] - 2025-12-28 🎬

### Added
- **Поддержка видео**
  - Загрузка видео файлов (mp4, webm, quicktime, mpeg)
  - Максимальный размер видео: 100MB
  - Отображение видео в постах с нативным плеером
  - Видео в карусели вместе с фото
  - Индикатор "Видео" на превью
- **Chunked Upload для видео**
  - Загрузка видео по частям (chunks) по 5MB
  - Реальный прогресс загрузки каждой части
  - Отображение "Загрузка 3/10 частей"
  - Возможность возобновления при обрыве связи (в будущем)
  - API endpoints: `/api/media/chunked/initiate`, `/upload`, `/complete`
  - Handler `ChunkedUploadHandler` с методами InitiateUpload, UploadChunk, CompleteUpload
  - Временное хранилище chunks в `uploads/temp/{upload_id}/`
- **Оптимизация видео при загрузке (сохраняет разрешение)**
  - Автоматическое сжатие видео с помощью FFmpeg
  - Сохранение оригинального разрешения (FullHD → FullHD, 720p → 720p)
  - Агрессивное сжатие: CRF 28, preset medium
  - Снижение битрейта аудио до 64k
  - Ограничение FPS до 30
  - Экономия 50-90% размера файла
  - Оптимизация для веб-воспроизведения (faststart)
- **Асинхронная оптимизация видео**
  - Видео сохраняется в БД сразу после загрузки (без ожидания оптимизации)
  - Оптимизация выполняется в фоновом режиме (goroutine)
  - Пользователь видит видео немедленно, оптимизация происходит незаметно
  - Индикатор "🎬 Оптимизация в фоне..." для видео в процессе обработки
  - База данных автоматически обновляется после завершения оптимизации
  - Флаг `optimizing: true` в API ответе для отслеживания статуса
- **UX улучшения загрузки видео**
  - Превью видео (первый кадр) показывается сразу после выбора
  - Круговой прогресс-бар с процентами в правом верхнем углу
  - Индикатор статуса: "📤 Загрузка 5/10 частей" или "🎬 Оптимизация..."
  - Полупрозрачное превью во время обработки
  - Детальное логирование в консоли для отладки
  - Функция `createVideoThumbnail()` для генерации превью из первого кадра
- **Облачное хранилище медиа**
  - Структура хранения: `uploads/users/{user_id}/photos|videos/YYYY/MM/`
  - Таблица `user_media` с метаданными
  - API endpoints: upload, get, delete, stats
  - Поддержка фото (10MB) и видео (100MB)
  - Автоматическая генерация UUID имен файлов
- **Карусель фото/видео**
  - Квадратный формат (1:1) как в Instagram
  - Навигация: стрелки, точки, клавиатура (← →), свайпы
  - Счетчик медиа (1/5) на фото
  - Адаптивное отображение разных соотношений сторон
  - Поддержка видео в карусели
- **Модальное окно выбора питомцев**
  - Список всех питомцев пользователя
  - Множественный выбор питомцев
  - Превью выбранных питомцев в виде чипсов
  - Заглушка "У вас пока нет питомцев"

### Changed
- Загрузка видео теперь использует chunked upload вместо обычной загрузки
- Оптимизация видео больше не блокирует пользователя
- Видео доступно для просмотра сразу после загрузки

### Technical Details
- Backend: Go handlers для chunked upload в `main/backend/handlers/chunked_upload.go`
- Frontend: React hook `useChunkedUpload` для управления загрузкой по частям
- FFmpeg используется для оптимизации с параметрами: CRF 28, preset medium, audio 64k
- Goroutines для асинхронной обработки видео в фоне

## [0.3.0] - 2025-12-27 🐾

### Added
- **Универсальная система постов**
  - Модель `Post` с поддержкой вложений, меток, планирования
  - Поля: `author_type`, `attached_pets`, `attachments`, `tags`, `status`, `scheduled_at`
  - Типы статусов: `published`, `scheduled`, `draft`
  - Прикрепление питомцев к постам (массив `attached_pets`)
  - Медиа-вложения: фото и видео (структура `Attachment`)
  - Метки/теги: "ищет дом", "потерян", "найден"
  - Модальное окно создания поста с полным функционалом
  - Кнопка "Что нового?" для быстрого создания поста
- **Черновики и планирование постов**
  - Сохранение постов как черновиков (status: "draft")
  - Просмотр и управление черновиками
  - Загрузка черновика для редактирования
  - Удаление черновиков
  - Планирование публикации на определенную дату и время
  - Календарь выбора даты с навигацией по месяцам
  - Выбор времени публикации
  - Визуальная индикация запланированных постов
- **Настройки ответов на посты**
  - Выбор кто может отвечать: "Кто угодно", "Подписчики", "Подписки", "Упомянутые"
  - Опция "Проверять и одобрять ответы"
  - Dropdown меню с настройками
- **Система голосований (Polls)** - полноценные опросы в постах
  - Создание опросов с 2-10 вариантами ответа
  - Настройки: множественный выбор, изменение голоса, анонимность, срок действия
  - Отображение результатов с процентами и прогресс-барами
  - Список проголосовавших пользователей (для неанонимных опросов)
  - Компактное отображение voters в виде "pills" с аватарами
  - Возможность отменить голос (если разрешено настройками)
  - Таблицы БД: `polls`, `poll_options`, `poll_votes`
  - Компоненты: `PollCreator`, `PollDisplay`
  - Интеграция опросов в систему постов
- **Улучшения системы комментариев**
  - Комментарии теперь отображаются сразу после добавления (без перезагрузки)
  - Умный скролл: сохранение позиции при добавлении комментария
  - Offset 100px для новых корневых комментариев (видимость нового комментария)
  - В модальном окне комментарии всегда видны (убрана кнопка "Показать/Скрыть")
  - Sticky input форма внизу модального окна
  - Синий цвет (#1B76FF) для текста "Ответ для [username]"
  - Унифицированное управление состоянием комментариев в PostModal
- **URL-навигация для постов**
  - Формат URL: `/?metka=123` для открытия конкретного поста
  - Автоматическое открытие модального окна при переходе по ссылке
  - Обновление URL при открытии/закрытии модального окна
  - Поддержка прямых ссылок на посты

### Changed
- **Полная переработка системы постов**
  - Переход от простых постов к универсальной системе в стиле Threads
  - Модальное окно вместо inline-редактора
  - Расширенная модель данных с поддержкой вложений и меток
  - Новый UI для создания постов (минималистичный, как в Threads)
- Модальное окно поста теперь всегда показывает комментарии
- Форма ввода комментария закреплена внизу (sticky footer)
- Улучшена синхронизация счетчика комментариев
- Оптимизирован процесс добавления комментариев (без моргания)
- Кнопка создания поста теперь открывает модальное окно
- Аватары пользователей с fallback на UserIcon
- Автоматическое изменение высоты textarea при вводе текста

### Fixed
- Исправлена проблема с отображением voters в опросах
- Исправлена функция отмены голоса
- Исправлено дублирование обработчиков в PostComments
- Исправлена проблема с отображением имени пользователя в "Ответ для"
- Исправлен скролл при добавлении комментариев (теперь остается на месте)
- Исправлено закрытие модального окна по ESC и клику на backdrop

## [0.2.0] - 2025-12-14 🐾

### Added
- **ЗооБаза (petbase)** - новый микросервис справочника домашних животных
  - Backend API на Go (порт 8100) с полным CRUD для видов, пород и карточек
  - Frontend на Next.js (порт 4100) с AdminLayout
  - SSO интеграция с проверкой прав суперадмина
  - Таблицы БД: `species`, `breeds`, `pet_cards`
  - Тестовые данные в `petbase/backend/seed.sql`
- Переиспользуемый шаблон админ-панели в `shared/src/components/admin/`
  - Компонент `AdminLayout` с header, sidebar и адаптивностью
  - Виджеты: `StatsWidget`, `TableWidget`, `ChartWidget`
  - Полная документация в `shared/src/components/admin/README.md`

### Fixed
- Исправлена проблема с шириной контента в админках (overflow-x)
- `AdminLayout`: изменён `w-screen` на `w-full` для корректного отображения
- `TableWidget`: добавлен `max-w-full` и правильная обработка overflow
- CSS модули админки: убран `width: 100vw`, добавлен `width: 100%`
- Таблицы теперь корректно скроллятся горизонтально на мобильных устройствах

### Changed
- Обновлён скрипт `./run` для запуска ЗооБазы
- CORS в admin backend настроен для порта 4100
- Виджеты: `StatsWidget`, `TableWidget`, `ChartWidget`
- Документация по использованию админ-шаблона
- Мобильная адаптивность для админки (гамбургер-меню, overlay)

### Changed
- Улучшен дизайн админки: виджеты с тенями, анимации, градиенты
- Максимальная ширина контента 1400px для больших экранов
- Увеличены отступы и скругления виджетов
- Добавлены hover-эффекты и transitions

## [0.1.3] - 2025-12-14

### Fixed
- **Критическое исправление:** Tailwind CSS v4 не применял стили к компонентам из @pet/shared
- Исправлены пути к database модулю после реструктуризации проекта
- `main/backend/go.mod`: путь к database изменен с `../database` на `../../database`
- `database/db.go`: путь к БД изменен на `../../database/data.db` для корректной работы из `main/backend`
- Компоненты с Tailwind классами теперь копируются напрямую в каждый сервис
- AuthForm скопирован в main/frontend и admin/frontend
- Страница авторизации админки теперь отображается корректно
- Backend сервисы теперь запускаются без ошибок после переноса в папки main/ и admin/

### Changed
- Обновлена архитектура работы с shared компонентами
- Tailwind config очищен от неработающих путей к node_modules
- Компоненты импортируются локально вместо @pet/shared

### Added
- Документация `shared/README.md` с объяснением проблемы Tailwind v4
- Steering правило `.kiro/steering/shared-components.md` для работы с shared компонентами
- Инструкции по копированию компонентов между сервисами

### Technical Details
- Tailwind CSS v4 не сканирует файлы из node_modules по умолчанию
- Решение: копирование компонентов вместо импорта из пакета
- TypeScript типы, API клиенты и хуки можно импортировать из @pet/shared
- React компоненты с Tailwind классами нужно копировать
- После реструктуризации проекта все относительные пути обновлены

## [0.1.2] - 2025-12-12

### Added
- **Полноценная система комментариев** к постам
- Таблица `comments` в базе данных с поддержкой вложенности
- Вложенные комментарии (1 уровень, как в ВК)
- Кнопка "Ответить" под каждым комментарием
- Упоминание пользователя синим цветом при ответе (@Имя)
- Индикатор "Ответ для [Имя]" при написании ответа
- Кнопка отмены ответа
- Удаление комментариев (только автор)
- Форматирование времени комментариев ("только что", "5 мин назад")
- Счетчик комментариев обновляется в реальном времени
- Кнопка "Показать/Скрыть комментарии"
- Поля `parent_id` и `reply_to_user_id` в таблице comments
- Модель `Comment` с поддержкой вложенности и ответов
- API endpoints для комментариев (GET, POST, DELETE)
- Компонент `PostComments` с полным функционалом
- Callback `onCountChange` для обновления счетчика в родительском компоненте

### Changed
- Отказ от концепции "стены" в пользу комментариев
- Лента теперь использует комментарии вместо записей на стене
- Профиль пользователя показывает только его посты с комментариями
- Счетчик комментариев теперь динамический (обновляется без перезагрузки)
- Backend возвращает дерево комментариев (корневые + replies)
- Страница редактирования профиля вынесена на отдельный URL `/profile/edit`

### Fixed
- Адаптивность главной страницы на маленьких экранах
- Feed теперь занимает 100% ширины на мобильных устройствах
- Header адаптирован для мобильных (скрыт CityDetector, сокращен placeholder)
- Профиль пользователя адаптирован (обложка, аватар, кнопки)
- Табы в профиле адаптивны (overflow-x-auto)
- Layout уменьшен padding на маленьких экранах

### Removed
- Удалена концепция "стены" (записи на чужих профилях)
- Удален компонент `WriteOnWall`
- Удален handler `WallPostHandler`
- Удалено поле `wall_author_id` из таблицы posts

## [0.1.1] - 2025-12-12

### Fixed
- **Критическое исправление:** Авторизация при создании постов теперь работает корректно
- Middleware теперь извлекает `userID` из JWT токена и добавляет его в контекст запроса
- Handler создания постов теперь получает `userID` из контекста
- Исправлено "мигание" кнопки "Войти" при обновлении страницы
- Добавлен skeleton loader во время проверки авторизации
- UserMenu теперь проверяет флаг `isLoading` перед отображением

### Changed
- AuthMiddleware теперь парсит claims из JWT и добавляет `userID` в `context.Context`
- UserMenu показывает placeholder (серый кружок) во время загрузки состояния авторизации

## [0.1.0] - 2025-12-12

### Added
- **Админ-панель (Frontend)** на Next.js
- Страница входа в админ-панель (`/auth`) с формой авторизации
- Дашборд с вкладками: Пользователи, Посты, Статистика, Логирование
- Сворачиваемое боковое меню с иконками Heroicons
- Таблицы с данными пользователей и постов
- Модерация постов (одобрение/отклонение/удаление)
- Управление пользователями (просмотр/удаление)
- Статистика платформы (пользователи, посты, активность)
- **Система логирования** для всей платформы
- Таблица `system_logs` для хранения всех событий системы
- Вкладка "Логирование" в админ-панели с отображением всех логов
- Автоматическое логирование входа пользователей
- Функция `logSystemEvent()` для логирования событий
- Уровни логов: info, warning, error, debug
- Категории логов: auth, user, post, pet, profile, admin, system, security
- Цветные бейджи для разных типов действий в логах
- Air (hot reload) для Admin Backend
- Tailwind CSS в админ-панели
- Heroicons для всех иконок в админ-панели

### Changed
- Admin Backend теперь запускается с Air для hot reload
- Порт Admin Frontend: 4000 (вместо "в разработке")
- Серый фон у сайдбара, белый у основного контента (инверсия)
- Активный пункт меню выделяется белым фоном с тенью
- Боковое меню теперь широкое (200px) с текстом и иконками
- Кнопка сворачивания меню перенесена вниз
- SQL запросы в Admin Backend адаптированы под SQLite (вместо PostgreSQL)
- Обработка NULL значений в SQL запросах через `sql.NullString`
- Endpoint `/api/admin/stats` переименован в `/api/admin/stats/overview`
- Цвет текста на главном сайте зафиксирован (#171717) независимо от темной темы

### Fixed
- Исправлена проблема со светлым текстом при вводе на главном сайте
- Исправлены SQL placeholder'ы ($1, $2 → ?) для SQLite
- Исправлена обработка пустых массивов в API логов
- Исправлена структура интерфейса Log (добавлено поле admin_name)

### Технологии
- Добавлено: Tailwind CSS 4.x в админ-панели
- Добавлено: Heroicons 2.x в админ-панели
- Добавлено: Air для Admin Backend

## [0.0.9] - 2025-12-12

### Added
- **SSO (Single Sign-On)** архитектура для всех микросервисов
- Поле `roles` в JWT токенах для управления правами доступа
- Поля `exp` и `iat` в JWT токенах
- Endpoint `/api/auth/verify` для проверки токенов микросервисами
- Функция `getUserRoles()` для получения ролей пользователя из таблицы admins
- Микросервис админ-панели в папке `admin/`
- Backend для админ-панели (Go, порт 8081)
- Таблицы `admins` и `admin_logs` в базе данных
- Middleware для проверки прав суперадмина (с поддержкой SSO)
- API endpoints для управления пользователями
- API endpoints для модерации постов
- API endpoints для статистики
- API endpoints для просмотра логов
- Скрипт `create-superadmin.sh` для назначения первого суперадмина
- Скрипт `tests/debug-sso.sh` для отладки SSO
- Документ `docs/SSO_ARCHITECTURE.md` с архитектурой SSO системы
- Документ `docs/SSO_FLOW.md` с визуальной схемой SSO
- Документ `admin/SSO_SETUP.md` с инструкцией по настройке
- Документ `admin/IMPLEMENTATION_STATUS.md` со статусом реализации
- Логирование всех действий администраторов

### Changed
- JWT токены теперь включают массив `roles` для определения прав доступа
- Cookie `auth_token` теперь общий для всех микросервисов (SSO)
- Admin Backend использует общий токен `auth_token` вместо `admin_token`
- Middleware в Admin Backend проверяет роли из JWT вместо отдельной авторизации
- Функция `GenerateToken()` теперь принимает параметр `roles` и добавляет `exp`, `iat`
- Функция `ParseToken()` теперь возвращает `Roles` в структуре `TokenClaims`
- Скрипт `run` теперь запускает все сервисы включая Admin Backend
- Mobile порт изменен с 8081 на 8082 (8081 используется Admin Backend)
- Функция `InitDB()` теперь имеет вариант `InitDBWithPath()` для гибкости путей

### Removed
- Отдельная авторизация для админ-панели (заменена на SSO)

### Fixed
- Исправлены HTTP method константы в admin handlers (MethodGet вместо MethodGET)
- **Исправлена критическая ошибка:** JWT Secret теперь инициализируется ПОСЛЕ загрузки .env файла
- Исправлена валидация JWT токенов в Admin Backend
- Исправлены порты: Mobile (8081), Admin Backend (9000) для избежания конфликтов

## [0.0.8] - 2025-12-12

### Added
- Файл `.kiro/steering/changelog-rules.md` с правилами ведения CHANGELOG
- Комментарий-напоминание о правилах в начале CHANGELOG.md
- Правило: в секции "Технологии" указывать только новые/измененные технологии
- Папка `docs/` для всей документации проекта
- Файл `docs/README.md` с описанием всей документации
- Файл `docs/ROADMAP.md` с планом развития проекта и критериями версионирования
- Файл `docs/PERFORMANCE.md` с метриками производительности и рекомендациями
- Файл `docs/STRUCTURE.md` с описанием архитектуры проекта
- Файл `docs/TEMPLATES.md` с шаблонами страниц и компонентов
- Файл `docs/API_KEYS.md` с конфиденциальной информацией (не в git)
- Папка `tests/` для всех тестов проекта
- Скрипт `tests/performance-test.sh` для автоматического тестирования скорости
- Скрипт `tests/compare-with-vk.sh` для сравнения с VK.com
- Скрипт `tests/web-vitals-test.sh` для измерения Web Vitals через Lighthouse
- Файл `tests/README.md` с документацией по тестам
- Сравнительный анализ производительности с VK.com
- Целевые показатели Web Vitals (TTFB, FCP, LCP, TBT, CLS)
- Инструкции по измерению Web Vitals через Chrome DevTools и Lighthouse CLI

### Changed
- Исправлены все предыдущие версии в CHANGELOG согласно новому правилу
- Секция "Технологии" теперь содержит только изменения, а не весь стек
- Полный стек технологий перенесен в README.md
- Скрипты тестирования перенесены из `scripts/` в `tests/`
- Вся документация перенесена в папку `docs/`
- Обновлены все ссылки на документацию во всех файлах

### Fixed
- Убрано дублирование полного списка технологий в каждой версии CHANGELOG

## [0.0.7] - 2025-12-12

### Added
- Динамические URL профилей пользователей в формате /id{номер}
- Страница профиля пользователя доступна по адресу /id1, /id2 и т.д.
- Возможность просматривать профили других пользователей
- Определение "свой/чужой" профиль для отображения кнопок редактирования
- Кликабельные имена пользователей в постах с переходом на их профиль
- Динамическая ссылка "Профиль" в боковом меню (ведет на /id{userId})
- Динамическая ссылка "Управление аккаунтом" в меню пользователя

### Changed
- Страница /profile теперь редиректит на /id{userId} текущего пользователя
- Sidebar использует AuthContext для получения ID текущего пользователя
- Кнопки редактирования профиля показываются только владельцу
- Кнопка "Добавить питомца" показывается только владельцу профиля

### Технологии
- Next.js Dynamic Routes ([userId])

## [0.0.6] - 2025-12-12

### Added
- Полная интеграция профиля с базой данных
- Расширенная модель пользователя: bio, phone, location, avatar, cover_photo
- Таблица posts для хранения публикаций пользователей
- Таблица pets для хранения питомцев пользователей
- API endpoint PUT /api/profile для обновления профиля
- API endpoints для постов: GET /api/posts, POST /api/posts, PUT /api/posts/:id, DELETE /api/posts/:id
- API endpoint GET /api/posts/user/:id для получения постов пользователя
- API endpoints для питомцев: GET /api/pets/user/:id, POST /api/pets, DELETE /api/pets/:id
- Модели Post и Pet в backend
- Handlers для profile, posts, pets
- Реальное создание постов через форму CreatePost
- Отображение реальных постов в ленте из базы данных
- Отображение реальных постов и питомцев в профиле
- Форматирование времени публикации (только что, X мин назад, X ч назад)
- Загрузка данных пользователя при входе (bio, phone, location, avatar, cover_photo)
- Аватар пользователя в CreatePost и PostCard
- Защита endpoints постов и питомцев через JWT middleware

### Changed
- Модель User расширена новыми полями
- AuthContext теперь возвращает полные данные пользователя
- Endpoint /api/auth/me возвращает все поля пользователя
- Feed компонент использует реальные данные из API
- PostCard компонент адаптирован под реальную структуру Post
- Profile page отображает реальные данные из базы
- CreatePost компонент создает реальные посты в базе данных

### Fixed
- Исправлена структура ответов API (единый формат с success/data/error)
- Добавлены helper функции sendSuccessResponse и sendErrorResponse

## [0.0.5] - 2025-12-12

### Added
- Интеграция DaData API для поиска организаций по ИНН, ОГРН и названию
- Компонент OrganizationSearch с автодополнением
- Страница "Организации" с детальной информацией
- Отображение реквизитов организации (ИНН, ОГРН, КПП)
- Полная информация об адресе (индекс, регион, город, улица, дом)
- Координаты организации (geo_lat, geo_lon) из DaData
- Контактная информация (телефоны, email)
- Информация о руководстве организации
- Статус организации (Действующая/Ликвидирована)
- Дата регистрации и организационно-правовая форма
- Интеграция Яндекс.Карт для отображения местоположения организаций
- Компонент YandexMap с поддержкой геокодирования адресов
- Геокодирование через Яндекс.Карты API для точного определения координат
- Fallback на координаты из DaData при ошибке геокодирования
- Красная метка на карте с информацией об организации
- Элементы управления картой (zoom, fullscreen)
- Balloon с названием организации и адресом при клике на метку
- Файл API_KEYS.md для хранения всех API ключей проекта
- API_KEYS.md добавлен в .gitignore для безопасности

### Changed
- Определение города через DaData API вместо ip-api.com
- Карта использует геокодирование по адресу как основной метод
- Координаты из DaData используются как запасной вариант
- Высота карты установлена на 400px
- Цвет спиннера загрузки карты изменен на #1B76FF (цвет логотипа)

### Fixed
- Исправлена проблема с загрузкой карты (карта всегда в DOM)
- Убраны отладочные console.log из компонента организаций
- Исправлена ошибка геокодирования через HTTP API
- Улучшена обработка ошибок при загрузке карты
- Исправлена ошибка 413 при поиске организаций (убран фильтр status)

### Технологии
- Добавлено: DaData API (Party, Address) для поиска организаций
- Добавлено: Yandex Maps API 2.1 для отображения карт

## [0.0.4] - 2025-12-12

### Added
- Автоматическое определение города пользователя по IP (ipapi.co, ip-api.com)
- Компонент CityDetector с поддержкой русского языка
- Возможность ручного изменения города
- Умная лента с объединением постов и объявлений
- Типы постов: обычный пост, объявление, потерялся, нашелся, помощь
- Компонент выбора типа поста при создании
- Фильтры ленты в правой колонке (Все, Посты, Объявления, Потеряшки, Найдены, Помощь)
- Цветные бейджи для разных типов постов
- Динамические плейсхолдеры в зависимости от типа поста
- Favicon с логотипом ЗооПлатформы

### Changed
- Название сайта изменено на "ЗооПлатформа"
- Логотип обновлен (favicon.svg с сердечком и мордой собаки)
- Все синие цвета кнопок заменены на #1B76FF (цвет из логотипа)
- Красный цвет уведомлений изменен на #FC2B2B (цвет из логотипа)
- Убран пункт "Главная" из меню (дублировал "Ленту")
- Убран пункт "Объявления" из меню (интегрировано в ленту)
- Шрифт сайта: -apple-system, BlinkMacSystemFont, Roboto
- Ширина сайта уменьшена до 1150px (стиль VK)
- Боковое меню уменьшено до 180px
- Размер шрифта логотипа: text-sm с uppercase
- Фон поиска в хедере соответствует фону сайта (градиент)
- Уменьшен размер круга уведомлений (w-4 h-4)
- Уплотнены дополнительные пункты меню (py-1)
- Бейдж мессенджера остался овальным (px-2 py-0.5)
- Фильтры перенесены из центральной колонки в правую панель

### Fixed
- Исправлена ошибка с серверными компонентами (добавлен 'use client' в RightPanel)
- Улучшена поддержка русских названий городов
- Добавлен словарь перевода популярных городов России

## [0.0.3] - 2025-12-11

### Added
- Heroicons библиотека для иконок
- Минималистичный дизайн UI в стиле ЗооПлатформа
- Боковое меню с иконками Heroicons
- Разделители между секциями меню
- Badge для мессенджера (43 непрочитанных)
- Обновленный хедер с поиском и уведомлениями

### Changed
- Полностью переработан дизайн интерфейса
- Sidebar: чистый минималистичный стиль без фоновых подложек
- Hover эффекты с закругленными углами
- Header: выровнен с контентом страницы
- Логотип в хедере выровнен с боковым меню
- Layout: центральная колонка 580px, правая панель расширена
- Меню включает: Главная, Лента, Объявления, Профиль, Мессенджер, Зоожурнал, Афиша, Организации, Зоомаркет, Учебный центр, Сервисы, Мои питомцы, Горячая линия
- Дополнительные ссылки: О платформе, Фонд "ЗооПомощь", Статистика, Техподдержка, Команда

### Технологии
- Добавлено: Heroicons 2.x для иконок

## [0.0.2] - 2025-12-11

### Added
- React Native мобильное приложение (Expo)
- Папка `shared/` для общей логики между платформами
- Air для hot reload Go бекенда
- Metro config для React Native
- Пример интеграции mobile с backend API
- SQLite база данных
- Отдельный модуль `database/` для работы с БД
- Таблица `users` с полем password
- Автоматическая инициализация БД при старте сервера
- REST API структура (handlers, models)
- JWT авторизация
- Endpoints для регистрации и входа (`/api/auth/register`, `/api/auth/login`)
- Middleware для защиты endpoints
- Bcrypt хеширование паролей
- CRUD endpoints для users (GET, POST, PUT, DELETE)
- TypeScript типы для всех API запросов/ответов
- Стандартный формат ответов API (success, data, error)
- Документация структуры проекта (STRUCTURE.md)

### Changed
- CORS настроен на `*` для поддержки всех клиентов
- CORS headers включают Authorization
- Скрипт `run` теперь запускает три сервиса (backend, web, mobile)
- API клиент вынесен в shared (для web) и продублирован в mobile
- Обновлен Go до версии 1.25.5
- База данных вынесена в отдельный модуль в корне проекта
- Endpoints `/api/users` теперь защищены JWT токеном

### Технологии
- Добавлено: React Native 0.81.5 + Expo 54.0.27 для мобильного приложения
- Добавлено: Air 1.63.4 для hot reload Go бекенда
- Добавлено: SQLite 3 для базы данных
- Добавлено: JWT (golang-jwt/jwt/v5) для авторизации
- Добавлено: Bcrypt для хеширования паролей

## [0.0.1] - 2025-12-11

### Added
- Инициализация проекта с Go backend и Next.js frontend
- Go HTTP сервер на порту 8080 с базовыми эндпоинтами (/, /api/health)
- CORS middleware для взаимодействия с фронтендом
- Next.js приложение с TypeScript на порту 3000
- Tailwind CSS для стилизации
- Структура компонентов: ui/, shared/, layout/
- API клиент (`lib/api.ts`) для связи с бекендом
- Базовые компоненты: Button с вариантами, Header
- Environment variables конфигурация (.env.local)
- Скрипт `run` для запуска проекта с автоматической очисткой портов
- Документация: README.md и CHANGELOG.md
- Git репозиторий с тегом v0.0.1

### Технологии
- Go 1.25.5
- Node.js 25.2.0
- Next.js 16.0.8 (App Router)
- React 19.2.1
- TypeScript 5.x
- Tailwind CSS 4.x
