# ЗооБаза - Справочник домашних животных

Централизованная база данных с информацией о всех видах домашних животных.

## Описание

ЗооБаза - это справочная система, которая содержит подробную информацию о домашних животных:
- Виды животных (собаки, кошки, птицы, грызуны, рептилии и т.д.)
- Породы с характеристиками
- Особенности ухода
- Средняя продолжительность жизни
- Размеры и вес
- Темперамент и поведение
- Требования к содержанию
- Фотографии и описания

## Структура

```
petbase/
├── backend/          # Go API (порт 8100)
│   ├── main.go
│   ├── handlers/
│   │   ├── species.go    # Виды животных
│   │   ├── breeds.go     # Породы
│   │   └── cards.go      # Карточки животных
│   └── models/
│       ├── species.go
│       ├── breed.go
│       └── pet_card.go
├── frontend/         # Next.js UI (порт 4100)
│   └── app/
│       ├── page.tsx
│       └── components/
└── README.md
```

## API Endpoints

### Виды животных
- `GET /api/species` - Список всех видов
- `GET /api/species/:id` - Информация о виде
- `POST /api/species` - Создать вид (admin)
- `PUT /api/species/:id` - Обновить вид (admin)
- `DELETE /api/species/:id` - Удалить вид (admin)

### Породы
- `GET /api/breeds` - Список всех пород
- `GET /api/breeds/species/:id` - Породы конкретного вида
- `GET /api/breeds/:id` - Информация о породе
- `POST /api/breeds` - Создать породу (admin)
- `PUT /api/breeds/:id` - Обновить породу (admin)
- `DELETE /api/breeds/:id` - Удалить породу (admin)

### Карточки животных
- `GET /api/cards` - Все карточки
- `GET /api/cards/:id` - Конкретная карточка
- `GET /api/cards/breed/:id` - Карточки породы
- `POST /api/cards` - Создать карточку (admin)
- `PUT /api/cards/:id` - Обновить карточку (admin)
- `DELETE /api/cards/:id` - Удалить карточку (admin)

## База данных

### Таблица species (виды)
```sql
CREATE TABLE species (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,              -- Название (Собака, Кошка, и т.д.)
  name_en TEXT NOT NULL,           -- English name
  description TEXT,                -- Описание
  icon TEXT,                       -- Иконка/эмодзи
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### Таблица breeds (породы)
```sql
CREATE TABLE breeds (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  species_id INTEGER NOT NULL,     -- Связь с видом
  name TEXT NOT NULL,              -- Название породы
  name_en TEXT,                    -- English name
  description TEXT,                -- Описание
  origin TEXT,                     -- Страна происхождения
  size TEXT,                       -- Размер (маленький, средний, большой)
  weight_min REAL,                 -- Минимальный вес (кг)
  weight_max REAL,                 -- Максимальный вес (кг)
  lifespan_min INTEGER,            -- Мин. продолжительность жизни (лет)
  lifespan_max INTEGER,            -- Макс. продолжительность жизни (лет)
  temperament TEXT,                -- Темперамент
  care_level TEXT,                 -- Уровень ухода (легкий, средний, сложный)
  photo TEXT,                      -- URL фото
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (species_id) REFERENCES species(id)
);
```

### Таблица pet_cards (карточки)
```sql
CREATE TABLE pet_cards (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  breed_id INTEGER NOT NULL,       -- Связь с породой
  title TEXT NOT NULL,             -- Заголовок карточки
  description TEXT,                -- Полное описание
  characteristics TEXT,            -- Характеристики (JSON)
  care_tips TEXT,                  -- Советы по уходу
  health_info TEXT,                -- Информация о здоровье
  nutrition TEXT,                  -- Питание
  photos TEXT,                     -- Фотографии (JSON array)
  is_published BOOLEAN DEFAULT 0,  -- Опубликована ли
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (breed_id) REFERENCES breeds(id)
);
```

## Использование

### Запуск

```bash
# Backend
cd petbase/backend
go run main.go

# Frontend
cd petbase/frontend
npm run dev
```

### Доступ
- Frontend: http://localhost:4100
- Backend API: http://localhost:8100

## Интеграция с другими сервисами

Другие сервисы могут использовать API ЗооБазы для:
- Автозаполнения информации о питомцах пользователей
- Отображения справочной информации
- Рекомендаций по уходу
- Подбора породы

## Вкладки в админке

1. **Виды** - управление видами животных
2. **Породы** - управление породами
3. **Карточки** - создание и редактирование карточек
4. **Статистика** - количество видов, пород, карточек

## Текущий статус

✅ **Готово:**
- Базовая структура БД (species, breeds, pet_cards)
- Backend API с полным CRUD для всех сущностей
- Frontend с AdminLayout и 4 вкладками
- SSO интеграция с проверкой прав суперадмина
- Отображение данных в таблицах
- Тестовые данные (seed.sql)

🚧 **В разработке:**
- UI для создания/редактирования видов
- UI для создания/редактирования пород
- UI для создания/редактирования карточек
- Загрузка фотографий
- Поиск и фильтрация

## Roadmap

- [x] Создать базовую структуру БД
- [x] Реализовать CRUD для видов
- [x] Реализовать CRUD для пород
- [x] Реализовать CRUD для карточек
- [ ] Добавить UI для создания/редактирования
- [ ] Добавить поиск и фильтрацию
- [ ] Загрузка фотографий
- [ ] Экспорт данных в JSON
- [ ] API для внешних сервисов
