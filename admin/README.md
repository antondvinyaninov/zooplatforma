# Админ-панель ЗооПлатформы

Микросервис для управления платформой суперадминистратором.

## 🎯 Назначение

Отдельный микросервис для администрирования ЗооПлатформы с доступом только для суперадминов.

**URL:** `sadmin.zooplatforma.ru` (в production)  
**Локально:** `http://localhost:3001` (frontend), `http://localhost:8081` (backend)

---

## 🏗️ Архитектура

### Микросервис состоит из:

1. **Backend (Go)** - API для админ-панели
   - Порт: 8081
   - Отдельные handlers для админских операций
   - Middleware для проверки прав суперадмина
   - Логирование всех действий

2. **Frontend (Next.js)** - UI админ-панели
   - Порт: 3001
   - Дизайн в стиле VK Рекламы
   - Таблицы, фильтры, статистика
   - Отдельная авторизация

3. **Database** - Использует общую БД
   - Таблица `admins` для прав доступа
   - Таблица `admin_logs` для аудита действий

---

## 🎨 Дизайн

Интерфейс в стиле VK Рекламы:

### Цветовая схема:
- **Основной синий:** #0077FF
- **Фон:** #F5F5F5
- **Карточки:** #FFFFFF
- **Текст:** #000000, #818C99 (вторичный)
- **Границы:** #E7E8EC

### Компоненты:
- Sidebar (180px) - навигация
- Header (60px) - профиль, уведомления
- Content - таблицы и формы
- Модальные окна для действий

---

## 📋 Функционал

### Фаза 1 (MVP) - v0.1.0:
- [x] Структура проекта
- [ ] Авторизация суперадмина
- [ ] Просмотр списка пользователей
- [ ] Блокировка/разблокировка пользователей
- [ ] Просмотр всех постов
- [ ] Удаление постов (модерация)
- [ ] Базовая статистика (кол-во пользователей, постов)

### Фаза 2 - v0.2.0:
- [ ] Управление организациями
- [ ] Модерация объявлений
- [ ] Детальная статистика с графиками
- [ ] Логи действий администраторов
- [ ] Управление правами (добавление модераторов)
- [ ] Поиск и фильтры

### Фаза 3 - v0.3.0:
- [ ] Аналитика и дашборды
- [ ] Экспорт данных (CSV, Excel)
- [ ] Массовые операции
- [ ] Email рассылки пользователям
- [ ] Управление контентом (баннеры, новости)
- [ ] Настройки платформы

---

## 🔐 Безопасность

### 1. Аутентификация (SSO)
- **Single Sign-On** - единая авторизация для всех микросервисов
- Общий JWT токен в cookie `auth_token`
- Токены с временем жизни 7 дней
- Роли в JWT: `["user", "superadmin"]`
- Подробнее: `docs/SSO_ARCHITECTURE.md`

### 2. Авторизация
- Проверка роли `superadmin` в JWT токене на каждый запрос
- Middleware `SuperAdminMiddleware` с поддержкой SSO
- Разграничение прав (superadmin, moderator)

### 3. Аудит
- Логирование всех действий в `admin_logs`
- IP адрес, timestamp, действие, данные
- Невозможность удаления логов

### 4. Защита
- Rate limiting (10 запросов/минуту)
- CORS только для admin домена
- HTTPS обязательно в production
- Защита от CSRF

---

## 🗄️ База данных

### Новые таблицы:

```sql
-- Таблица администраторов
CREATE TABLE admins (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL REFERENCES users(id),
  role TEXT NOT NULL DEFAULT 'moderator', -- 'superadmin', 'moderator'
  permissions TEXT, -- JSON с детальными правами
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  created_by INTEGER REFERENCES admins(id)
);

-- Логи действий администраторов
CREATE TABLE admin_logs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  admin_id INTEGER NOT NULL REFERENCES admins(id),
  action TEXT NOT NULL, -- 'user_block', 'post_delete', etc.
  target_type TEXT, -- 'user', 'post', 'organization'
  target_id INTEGER,
  details TEXT, -- JSON с деталями
  ip_address TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Индексы для быстрого поиска
CREATE INDEX idx_admin_logs_admin_id ON admin_logs(admin_id);
CREATE INDEX idx_admin_logs_created_at ON admin_logs(created_at);
CREATE INDEX idx_admin_logs_action ON admin_logs(action);
```

---

## 🚀 Запуск

### Первоначальная настройка:

```bash
# 1. Создайте первого суперадмина
cd admin/backend

# Узнайте ID вашего пользователя
sqlite3 ../../database/data.db "SELECT id, name, email FROM users;"

# Назначьте суперадмином (замените 1 на ваш ID)
./create-superadmin.sh 1

# 2. Убедитесь, что JWT_SECRET одинаковый в обоих .env
# backend/.env и admin/backend/.env должны иметь одинаковый JWT_SECRET
```

### Локальная разработка:

```bash
# Backend (Main)
cd backend
go run main.go

# Backend (Admin)
cd admin/backend
go run main.go

# Frontend (в другом терминале)
cd admin/frontend
npm install
npm run dev
```

### Проверка SSO:

```bash
# Запустите тест SSO
./tests/sso-test.sh
```

### Production:

```bash
# Backend
cd admin/backend
go build -o admin-server
./admin-server

# Frontend
cd admin/frontend
npm run build
npm start
```

---

## 📡 API Endpoints

### Авторизация (SSO):
- `POST /api/admin/auth/logout` - Выход из всех сервисов
- `GET /api/admin/auth/me` - Текущий админ

**Примечание:** Вход осуществляется через основной сайт (`/api/auth/login`)

### Пользователи:
- `GET /api/admin/users` - Список пользователей
- `GET /api/admin/users/:id` - Детали пользователя
- `PUT /api/admin/users/:id/block` - Заблокировать
- `PUT /api/admin/users/:id/unblock` - Разблокировать
- `DELETE /api/admin/users/:id` - Удалить (soft delete)

### Посты:
- `GET /api/admin/posts` - Список постов
- `GET /api/admin/posts/:id` - Детали поста
- `DELETE /api/admin/posts/:id` - Удалить пост
- `PUT /api/admin/posts/:id/hide` - Скрыть пост

### Статистика:
- `GET /api/admin/stats/overview` - Общая статистика
- `GET /api/admin/stats/users` - Статистика пользователей
- `GET /api/admin/stats/posts` - Статистика постов

### Логи:
- `GET /api/admin/logs` - Логи действий
- `GET /api/admin/logs/:id` - Детали лога

---

## 🛠️ Технологии

### Backend:
- Go 1.25.5
- net/http (standard library)
- SQLite (общая БД с основным приложением)
- JWT для авторизации

### Frontend:
- Next.js 16.0.8
- React 19.2.1
- TypeScript 5.x
- Tailwind CSS 4.x
- Recharts (для графиков)
- React Table (для таблиц)

---

## 📊 Метрики

### Целевые показатели:
- API Response Time: <100ms
- Page Load Time: <1s
- Uptime: 99.9%

---

## 🔗 Связь с основным приложением

### Общие ресурсы:
- ✅ База данных (database/)
- ✅ Модели данных (можно переиспользовать)
- ❌ API endpoints (отдельные)
- ❌ Frontend (полностью отдельный)

### Коммуникация:
- Админ-панель читает/пишет напрямую в БД
- Можно добавить очереди (RabbitMQ) для асинхронных задач
- Можно добавить gRPC для межсервисной коммуникации

---

## 📝 Roadmap

### v0.1.0 (Январь 2026):
- Базовая структура
- Авторизация
- Управление пользователями
- Модерация постов

### v0.2.0 (Февраль 2026):
- Статистика и аналитика
- Управление организациями
- Логи и аудит

### v0.3.0 (Март 2026):
- Дашборды с графиками
- Экспорт данных
- Массовые операции
- Email рассылки

---

## 🤝 Разработка

### Правила:
1. Все действия логируются
2. Нельзя удалять данные без подтверждения
3. Критичные действия требуют повторного ввода пароля
4. Код ревью обязательно
5. Тесты для критичных функций

### Git workflow:
```bash
# Создание ветки
git checkout -b feature/admin-users-list

# Коммит
git commit -m "feat(admin): add users list page"

# Push
git push origin feature/admin-users-list
```

---

**Версия:** 0.0.1  
**Последнее обновление:** 12 декабря 2025  
**Статус:** В разработке
