# Правила работы с Main Workspace

## ⚠️ КРИТИЧЕСКОЕ ПРАВИЛО: Изменяй только Main, остальное - только читай!

**Когда работаешь в Main workspace, можно изменять ТОЛЬКО папку `main/`!**

---

## ✅ Что МОЖНО изменять:

### Main Frontend (`main/frontend/`)
- ✅ Все файлы в `main/frontend/app/`
- ✅ Все файлы в `main/frontend/lib/`
- ✅ Все файлы в `main/frontend/components/`
- ✅ Все файлы в `main/frontend/contexts/`
- ✅ Все файлы в `main/frontend/types/`
- ✅ `main/frontend/package.json`
- ✅ `main/frontend/next.config.ts`
- ✅ `main/frontend/tailwind.config.ts`

### Main Backend (`main/backend/`)
- ✅ Все файлы в `main/backend/handlers/`
- ✅ Все файлы в `main/backend/middleware/`
- ✅ Все файлы в `main/backend/models/`
- ✅ `main/backend/main.go`
- ✅ `main/backend/go.mod`

### Документация Main
- ✅ `main/README_API.md`

---

## 📖 Что можно ТОЛЬКО ЧИТАТЬ:

### Auth Service (`auth/`)
- 📖 Читай для понимания как работает авторизация
- 📖 Используй `/api/auth/me` для проверки токенов
- ❌ НЕ изменяй код Auth Service из Main workspace!

**Если нужно изменить Auth:**
1. Открой основной проект (весь репозиторий)
2. Внеси изменения в `auth/`
3. Протестируй на всех сервисах
4. Вернись в Main workspace

### PetBase (`petbase/`)
- 📖 Читай для понимания структуры данных о животных
- 📖 Используй API endpoints для получения данных
- ❌ НЕ изменяй код PetBase из Main workspace!

**Если нужно изменить PetBase:**
1. Открой основной проект
2. Внеси изменения в `petbase/`
3. Протестируй на всех сервисах
4. Вернись в Main workspace

### Shared (`shared/`)
- 📖 Читай типы, хуки, утилиты
- 📖 Используй компоненты из `@pet/shared`
- ⚠️ Изменяй ТОЛЬКО если уверен, что это нужно только для Main

**Если изменяешь Shared:**
- Помни: это влияет на ВСЕ сервисы!
- Проверь что изменения не сломают другие сервисы
- Лучше создай локальную копию компонента в `main/frontend/components/`

### Database (`database/`)
- 📖 Читай структуру БД
- 📖 Смотри миграции для понимания схемы
- ❌ НЕ создавай новые миграции из Main workspace!

**Если нужна новая миграция:**
1. Открой основной проект
2. Создай миграцию в `database/migrations/`
3. Протестируй на всех сервисах
4. Вернись в Main workspace

### Pkg (`pkg/`)
- 📖 Читай middleware и клиенты
- 📖 Используй `pkg/middleware` в Main Backend
- ❌ НЕ изменяй pkg из Main workspace!

### Uploads (`uploads/`)
- 📖 Смотри загруженные файлы
- ❌ НЕ удаляй файлы вручную!

---

## 🎯 Правила разработки в Main Workspace

### 1. Фокус на Main
**Цель workspace:** Работать только с главной страницей (Main)

**Что делать:**
- Разрабатывай новые фичи для Main
- Исправляй баги в Main
- Улучшай UI/UX главной страницы
- Оптимизируй производительность Main

**Что НЕ делать:**
- Не отвлекайся на другие сервисы
- Не пытайся "быстро пофиксить" что-то в Auth или PetBase
- Не создавай новые миграции БД

### 2. Использование зависимостей

**Auth Service:**
```typescript
// ✅ ПРАВИЛЬНО: используй API
const response = await fetch('http://localhost:7100/api/auth/me', {
  credentials: 'include',
});

// ❌ НЕПРАВИЛЬНО: не лезь в код Auth
import { checkAuth } from '../../auth/backend/utils'; // НЕТ!
```

**PetBase:**
```typescript
// ✅ ПРАВИЛЬНО: используй API
const breeds = await fetch('http://localhost:8100/api/breeds');

// ❌ НЕПРАВИЛЬНО: не читай напрямую из БД
import { getBreeds } from '../../petbase/backend/handlers'; // НЕТ!
```

**Shared:**
```typescript
// ✅ ПРАВИЛЬНО: используй через @pet/shared
import { User } from '@pet/shared/types';
import { useAuth } from '@pet/shared/hooks';

// ⚠️ ОСТОРОЖНО: изменения влияют на все сервисы
// Лучше скопируй компонент локально в main/frontend/components/
```

### 3. Git commits

**Коммить можно только изменения в Main:**

```bash
# ✅ ПРАВИЛЬНО
git add main/frontend/app/page.tsx
git commit -m "feat(main): добавил новую секцию на главной"

# ✅ ПРАВИЛЬНО
git add main/backend/handlers/posts.go
git commit -m "fix(main): исправил загрузку постов"

# ❌ НЕПРАВИЛЬНО
git add auth/backend/main.go
git commit -m "fix(auth): ..." # НЕТ! Открой основной проект!

# ❌ НЕПРАВИЛЬНО
git add petbase/backend/handlers/breeds.go
git commit -m "feat(petbase): ..." # НЕТ! Открой основной проект!
```

### 4. Тестирование

**Запускай только Main сервисы:**

```bash
# Запусти критические сервисы
cd auth/backend && go run main.go &
cd petbase/backend && go run main.go &

# Запусти Main
cd main/backend && go run main.go &
cd main/frontend && npm run dev &

# Тестируй только Main
open http://localhost:3000
```

**Не запускай другие сервисы:**
- Не нужен Admin (9000/4000)
- Не нужен Shelter (8200/5100)
- Не нужен Owner (8400/6100)
- Не нужен Volunteer (8500/6200)
- Не нужен Clinic (8600/6300)

---

## 🚨 Когда нужно открыть основной проект

### Открой основной проект если:

1. **Нужно изменить Auth Service**
   - Изменить логику авторизации
   - Добавить новый endpoint
   - Изменить структуру JWT токена

2. **Нужно изменить PetBase**
   - Добавить новый вид животного
   - Изменить структуру породы
   - Добавить новое поле в карточку

3. **Нужно создать миграцию БД**
   - Добавить новую таблицу
   - Изменить структуру существующей таблицы
   - Добавить индексы

4. **Нужно изменить Shared компоненты**
   - Изменить общий тип
   - Изменить общий хук
   - Изменить общий компонент (который используется везде)

5. **Нужно изменить Pkg**
   - Изменить middleware
   - Изменить API клиенты

### Процесс:

```bash
# 1. Сохрани изменения в Main workspace
git add main/
git commit -m "WIP: работа над Main"

# 2. Открой основной проект
# File → Open Folder → выбери корень проекта

# 3. Внеси изменения в Auth/PetBase/Database/Pkg

# 4. Протестируй на ВСЕХ сервисах
./run

# 5. Закоммить изменения
git add auth/ petbase/ database/ pkg/
git commit -m "feat: изменения в критических сервисах"

# 6. Вернись в Main workspace
# File → Open Workspace → main-workspace.code-workspace
```

---

## 📋 Checklist перед коммитом

- [ ] Изменения только в `main/` папке
- [ ] Не изменял `auth/`, `petbase/`, `database/`, `pkg/`
- [ ] Если изменял `shared/` - проверил что не сломал другие сервисы
- [ ] Протестировал Main локально
- [ ] Commit message начинается с `feat(main):` или `fix(main):`

---

## 🤖 Правила для AI Assistant

### Контекст окружения (EnvironmentContext)

Когда в запросе присутствует блок `<EnvironmentContext>`:
- Это информация о текущем состоянии редактора пользователя
- Содержит открытые файлы (`OPEN-EDITOR-FILES`) и активный файл (`ACTIVE-EDITOR-FILE`)
- **Учитывай этот контекст ТОЛЬКО если он релевантен запросу пользователя**
- Если пользователь не упоминает "текущий файл", "этот файл" или подобное - игнорируй EnvironmentContext
- Не упоминай наличие этого контекста в ответах, используй его молча когда нужно

**Пример:**
```
Пользователь: "Запиши это в свое правило main-workspace-rules"
EnvironmentContext показывает: main-workspace-rules.md открыт
→ Используй контекст, это релевантно

Пользователь: "Создай новый компонент Button"
EnvironmentContext показывает: main-workspace-rules.md открыт
→ Игнорируй контекст, это не релевантно
```

---

## 💡 Советы

### Если нужен компонент из Shared, но хочешь его изменить:

**Вариант 1: Скопируй локально**
```bash
# Скопируй компонент в main/frontend
cp -r shared/src/components/Button main/frontend/components/
# Теперь можешь изменять без влияния на другие сервисы
```

**Вариант 2: Создай обертку**
```typescript
// main/frontend/components/MainButton.tsx
import { Button } from '@pet/shared/components/buttons';

export function MainButton(props) {
  // Твои кастомные изменения только для Main
  return <Button {...props} className="custom-main-style" />;
}
```

### Если нужны данные из другого сервиса:

**Всегда используй API, не лезь в код:**

```typescript
// ✅ ПРАВИЛЬНО
const response = await fetch('http://localhost:8100/api/breeds');
const breeds = await response.json();

// ❌ НЕПРАВИЛЬНО
import { getBreeds } from '../../petbase/backend/handlers/breeds';
```

---

## ⚠️ ЗАПОМНИ:

**Main Workspace = Работа ТОЛЬКО с Main!**

Если нужно изменить:
- 🔐 Auth Service → Открой основной проект
- 🐾 PetBase → Открой основной проект
- 💾 Database → Открой основной проект
- 📦 Pkg → Открой основной проект

**Фокусируйся на Main, не отвлекайся на остальное!** 🎯
