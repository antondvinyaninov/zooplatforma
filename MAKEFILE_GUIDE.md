# 🚀 Makefile - Шпаргалка

Быстрый справочник по командам Makefile для ЗооПлатформы.

---

## 📖 Основные команды

### Справка
```bash
make help    # Показать все доступные команды
make info    # Информация о проекте
```

---

## 🚀 Запуск сервисов

### Отдельные сервисы
```bash
make main       # Main (соцсеть) - 3000/8000
make admin      # Main + Admin - 4000/9000
make petbase    # Main + PetBase - 4100/8100
make shelter    # Main + Admin + Shelter - 5100/8200
make owner      # Main + Owner - 6100/8400
make volunteer  # Main + Volunteer - 6200/8500
make clinic     # Main + Admin + Clinic - 6300/8600
```

### Все сразу
```bash
make all        # Запустить ВСЕ сервисы (как ./run)
```

---

## 🛑 Управление процессами

```bash
make stop                # Остановить все процессы
make restart-admin       # Перезапустить Admin
make restart-clinic      # Перезапустить Clinic
make status              # Показать запущенные процессы
```

---

## 📦 Установка

```bash
make install             # Установить npm зависимости
make install-go          # Установить Go зависимости
make install-all         # Установить всё (npm + Go)
```

---

## 💾 База данных

```bash
make db-backup           # Создать резервную копию
make db-restore          # Восстановить из бэкапа
make db-migrate          # Применить миграции
make db-status           # Показать статус БД
make backup-all          # Полный бэкап (БД + uploads)
```

---

## 🧪 Тестирование

```bash
make test                # Запустить все тесты
make test-api            # Тестировать API
make test-sso            # Тестировать SSO
make test-performance    # Тест производительности
make health              # Проверить здоровье сервисов
```

---

## 🧹 Очистка

```bash
make clean               # Очистить кэш (.next, tmp)
make clean-cache         # Очистить только .next
make clean-logs          # Очистить логи
make clean-all           # Очистить всё (включая node_modules)
```

---

## 🐳 Docker

```bash
make docker-build        # Собрать образы
make docker-up           # Запустить контейнеры
make docker-down         # Остановить контейнеры
make docker-logs         # Показать логи
make docker-restart      # Перезапустить Docker
```

---

## 🔧 Разработка

### С логами
```bash
make dev-main            # Main с логами в logs/main/
make dev-admin           # Admin с логами в logs/admin/
```

### Проверка кода
```bash
make lint-go             # Проверить Go код
make lint-ts             # Проверить TypeScript код
make format-go           # Форматировать Go код
make format-ts           # Форматировать TypeScript код
```

---

## 📝 Git

```bash
make git-status          # git status
make git-log             # Последние 10 коммитов
make git-tags            # Показать все теги
make git-pull            # git pull
make git-push            # git push + push tags
```

---

## 📊 Информация

```bash
make ports               # Показать все порты
make status              # Статус процессов
make check-ports         # Проверить занятые порты
make version             # Версия проекта
make info                # Полная информация
```

---

## 🔍 Логи

```bash
make logs-main           # Показать логи Main
make logs-admin          # Показать логи Admin
```

---

## 💡 Типичные сценарии

### Начало работы с Admin
```bash
make admin               # Запустить Main + Admin
# Работаете...
make stop                # Остановить
```

### Начало работы с Clinic
```bash
make clinic              # Запустить Main + Admin + Clinic
# Работаете...
make stop                # Остановить
```

### Полная очистка и переустановка
```bash
make clean-all           # Удалить всё
make install-all         # Переустановить всё
```

### Бэкап перед изменениями
```bash
make db-backup           # Создать бэкап БД
# Делаете изменения...
make db-restore          # Восстановить если что-то пошло не так
```

### Проверка здоровья
```bash
make health              # Проверить все API endpoints
make status              # Проверить процессы
make check-ports         # Проверить порты
```

### Работа с Git
```bash
make git-status          # Проверить статус
# Делаете коммит...
make git-push            # Отправить в репозиторий
```

---

## ⚡ Быстрые команды (самые частые)

```bash
make admin               # Запустить Admin
make clinic              # Запустить Clinic
make owner               # Запустить Owner
make stop                # Остановить всё
make clean-cache         # Очистить кэш
make db-backup           # Бэкап БД
make health              # Проверить здоровье
make help                # Справка
```

---

## 🎯 Преимущества Makefile

✅ **Не нужно помнить команды** - просто `make admin`  
✅ **Автоматические зависимости** - Main запускается автоматически  
✅ **Цветной вывод** - легко читать  
✅ **Самодокументируемый** - `make help` всегда под рукой  
✅ **60+ команд** - для всех задач разработки  
✅ **Стандартный инструмент** - работает везде  

---

## 🆚 Сравнение с ./run

| Задача | Старый способ | Новый способ |
|--------|---------------|--------------|
| Запустить всё | `./run` | `make all` |
| Запустить Admin | Вручную 3 команды | `make admin` |
| Остановить | `Ctrl+C` + kill | `make stop` |
| Бэкап БД | `./scripts/backup-database.sh` | `make db-backup` |
| Проверить порты | `lsof -i :3000 ...` | `make check-ports` |
| Справка | Смотреть в README | `make help` |

---

## 📚 Дополнительно

- Полный список команд: `make help`
- Информация о проекте: `make info`
- Версия: `make version`
- Порты всех сервисов: `make ports`

---

**Совет:** Добавьте `make help` в закладки терминала - это ваша главная шпаргалка! 🎯
