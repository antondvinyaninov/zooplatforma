# PostgreSQL Database Configuration
# Используй эти переменные окружения для подключения к PostgreSQL

# Connection String для всех backend сервисов
# Формат: postgres://username:password@host:port/database?sslmode=disable
DATABASE_URL=postgres://postgres_zp:7da0905cd3349f58f368@my_projects_bd_zooplatforma:5432/bd_zp?sslmode=disable

# Или используй отдельные переменные:
DB_HOST=my_projects_bd_zooplatforma
DB_PORT=5432
DB_USER=postgres_zp
DB_PASSWORD=7da0905cd3349f58f368
DB_NAME=bd_zp
DB_SSLMODE=disable

# Auth Service Configuration
AUTH_SERVICE_URL=http://localhost:7100
AUTH_SERVICE_PORT=7100

# Main Backend Configuration
MAIN_BACKEND_PORT=8000
MAIN_FRONTEND_PORT=3000

# Admin Backend Configuration
ADMIN_BACKEND_PORT=9000
ADMIN_FRONTEND_PORT=4000

# PetBase Configuration
PETBASE_BACKEND_PORT=8100
PETBASE_FRONTEND_PORT=4100

# Shelter Configuration
SHELTER_BACKEND_PORT=8200
SHELTER_FRONTEND_PORT=5100

# Owner Configuration
OWNER_BACKEND_PORT=8400
OWNER_FRONTEND_PORT=6100

# Volunteer Configuration
VOLUNTEER_BACKEND_PORT=8500
VOLUNTEER_FRONTEND_PORT=6200

# Clinic Configuration
CLINIC_BACKEND_PORT=8600
CLINIC_FRONTEND_PORT=6300

# JWT Secret (используется для подписи токенов)
JWT_SECRET=your-secret-key-here-change-in-production

# Environment
ENVIRONMENT=production
LOG_LEVEL=info
