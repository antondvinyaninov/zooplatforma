# 🤝 Volunteer - Кабинет зоопомощника

Микросервис для волонтеров, помогающих животным.

## Порты

- **Frontend:** http://localhost:6200
- **Backend:** http://localhost:8500

## Структура

```
volunteer/
├── backend/          # Go API (порт 8500)
│   ├── handlers/     # HTTP handlers
│   ├── middleware/   # JWT auth middleware
│   ├── models/       # Data models
│   ├── main.go       # Entry point
│   ├── go.mod        # Go dependencies
│   └── .env          # Environment variables
└── frontend/         # Next.js UI (порт 6200)
    ├── app/          # App Router pages
    │   ├── dashboard/    # Дашборд с 3 вкладками
    │   ├── auth/         # SSO авторизация
    │   ├── components/   # React компоненты
    │   └── page.tsx      # Главная страница (редирект)
    └── package.json  # Node dependencies
```

## Функционал

### Вкладки дашборда

1. **Обзор** - статистика и общая информация
   - Количество подопечных
   - Активные задачи
   - Выполнено за месяц

2. **Мои подопечные** - животные под опекой волонтера
   - Таблица с питомцами (curator_id = user_id)
   - Фото, кличка, вид, порода, статус
   - Ссылка на карточку питомца

3. **Задачи** - планирование и отчетность
   - Список задач волонтера
   - Создание новых задач
   - Отметка выполнения

### API Endpoints

**Защищённые (требуют JWT):**
- `GET /api/my-pets` - подопечные питомцы
- `GET /api/my-tasks` - задачи волонтера
- `GET /api/profile` - профиль пользователя

**Публичные:**
- `GET /api/health` - health check

## Запуск

### Backend

```bash
cd volunteer/backend
air  # hot reload
```

### Frontend

```bash
cd volunteer/frontend
npm install
npm run dev
```

## SSO Авторизация

Используется единая авторизация через Main API:

1. Пользователь заходит на http://localhost:6200
2. Проверка авторизации через Main API
3. Если не авторизован → редирект на http://localhost:3000/auth
4. После входа → возврат в дашборд

JWT токен хранится в cookie `auth_token` и автоматически передается всем микросервисам.

## База данных

Используется единая база данных `database/data.db`:

- **Таблица `pets`** - поле `curator_id` для связи с волонтером
- **Таблица `users`** - информация о пользователях
- **Таблица `tasks`** (планируется) - задачи волонтеров

## Дизайн

- AdminLayout в стиле Admin/Shelter/Owner
- Светлое боковое меню
- Градиентные карточки статистики (синяя, зеленая, фиолетовая)
- Таблицы с данными
- Responsive дизайн

## Environment Variables

```env
JWT_SECRET=your-secret-key-here-change-in-production
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:6200
```

## Технологии

**Backend:**
- Go 1.25.5
- net/http (standard library)
- JWT authentication
- Air (hot reload)
- SQLite

**Frontend:**
- Next.js 16.0.8
- React 19.2.1
- TypeScript 5.x
- Tailwind CSS 4.x
- Heroicons

## Интеграция

- **Main API** (localhost:8000) - авторизация, пользователи
- **Admin API** (localhost:9000) - роли и права
- **Database** - единая база данных

## Роль volunteer

Для доступа к кабинету зоопомощника пользователь должен иметь роль `volunteer` в Admin API.

Создание волонтера:

```bash
# В Admin API
curl -X POST http://localhost:9000/api/admin/users/{user_id}/role \
  -H "Cookie: auth_token=..." \
  -d '{"role": "volunteer"}'
```
