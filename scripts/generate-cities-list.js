// Скрипт для генерации полного списка городов России
// Использует открытые данные

const fs = require('fs');

// Полный список всех городов России по регионам
// Источник: Росстат, статус "город"
const russianCities = {
  regions: [
    {
      name: "Удмуртская Республика",
      cities: ["Ижевск", "Сарапул", "Воткинск", "Глазов", "Можга", "Камбарка"]
    },
    {
      name: "Республика Татарстан", 
      cities: ["Казань", "Набережные Челны", "Нижнекамск", "Альметьевск", "Зеленодольск", "Бугульма", "Елабуга", "Лениногорск", "Чистополь", "Заинск", "Азнакаево", "Менделеевск", "Буинск", "Агрыз", "Арск", "Болгар"]
    },
    // ... добавить остальные регионы
  ]
};

// Сохраняем в JSON
fs.writeFileSync(
  '../shared/data/russia-cities-full.json',
  JSON.stringify(russianCities, null, 2),
  'utf8'
);

console.log('✅ Справочник городов создан!');
console.log(`📊 Регионов: ${russianCities.regions.length}`);
console.log(`🏙️ Городов: ${russianCities.regions.reduce((sum, r) => sum + r.cities.length, 0)}`);
